---
id: 260720-d6u-codex-phase-1-foundation-platform-gate-m
phase: quick-260720-d6u-codex-phase-1-foundation-platform-gate-m
plan: 01
type: quick
title: 安全合并并发布 Phase 1 功能分支
wave: 1
depends_on: []
autonomous: true
files_modified:
  - .git/refs/heads/codex/phase-1-foundation-platform-gate
  - .git/refs/heads/master
  - .planning/STATE.md
  - README.md
requirements:
  - GIT-MERGE-01
must_haves:
  truths:
    - "功能分支的 10 个未发布提交被非强制推送到 GitHub，979 个未跟踪生成文件不进入任何提交。"
    - "master 同时包含原 master 独有提交和功能分支全部历史，没有 force push、rebase 公开历史、hard reset 或覆盖任一侧。"
    - "合并后的离线测试与 Ruff 检查通过，GitHub 上功能分支和 master 的实时 ref 均与对应本地提交 OID 一致。"
  artifacts:
    - path: ".git/refs/heads/codex/phase-1-foundation-platform-gate"
      provides: "已通过测试的功能分支历史，预期执行前尖端为 ce0aa84"
    - path: ".git/refs/heads/master"
      provides: "同时可达 master 原历史与功能分支历史的合并结果"
    - path: ".planning/STATE.md"
      provides: "保留已完成课程交付状态与 master 独有 quick-task 记录的语义合并"
  key_links:
    - from: "codex/phase-1-foundation-platform-gate"
      to: "origin/codex/phase-1-foundation-platform-gate"
      via: "显式非强制 push 后使用 git ls-remote 独立查询"
      pattern: "refs/heads/codex/phase-1-foundation-platform-gate"
    - from: "master"
      to: "codex/phase-1-foundation-platform-gate"
      via: "git merge --no-ff 并用 merge-base --is-ancestor 证明两条历史均可达"
      pattern: "refs/heads/master"
    - from: "master"
      to: "origin/master"
      via: "非强制 push、git ls-remote 与 fetch 后 0/0 分歧检查"
      pattern: "refs/heads/master"
---

<objective>
将 `codex/phase-1-foundation-platform-gate` 的完整功能历史安全发布并合并到 `master`，再推送 `master`，最后直接查询 GitHub 证明两个远程引用均正确。

Purpose: 在不丢失 master 独有的 GitHub/README/quick-task 历史、不污染功能历史、不提交本地生成物的前提下，把已完成的 DebugMate 产品交付整合到主分支。
Output: 已同步的功能分支、一个保留双方历史的 master merge commit、通过的自动检查，以及两个 GitHub ref 的 OID 一致性证据。
</objective>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/quick/260720-cmj-github/260720-cmj-SUMMARY.md
@.worktrees/phase-1-foundation-platform-gate/pyproject.toml
@.worktrees/phase-1-foundation-platform-gate/.gitignore
@C:/Users/20795/Documents/codex 第一次进化/docs/CODEX_EXPERIENCE_PROFILE.md

Planning evidence captured on 2026-07-20:
- `master` is clean at `3a5072e`, equals `origin/master`, and has three commits not reachable from the feature branch: `85f2b4b`, `76a431d`, `3a5072e`.
- `codex/phase-1-foundation-platform-gate` is checked out only at `.worktrees/phase-1-foundation-platform-gate`, has no tracked modifications, is at `ce0aa84`, and is 10 commits ahead of remote tip `b5277f4`.
- The feature worktree has exactly 979 untracked generated files: 970 under `.debugmate-runtime/` and 9 under `--help/`. They must remain local and untracked; do not clean, delete, stage, stash, or commit them.
- `master...feature` reports `3 330` with merge base `2b5fdfb`; this requires a real merge, not a fast-forward assumption.
- Both sides changed `README.md` and `.planning/STATE.md`. If they conflict, README must describe the implemented application from the feature side; STATE must retain the feature side's completed V0.1 status and deliverables while also retaining all master-only quick-task rows (`260719-gy7`, `260719-h5z`, `260720-cmj`) and any newer quick-task bookkeeping present at execution time.
- The feature worktree owns a ready `.venv`; use its Python executable for repository checks and bind imports to the worktree being tested.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revalidate, test, and publish the feature branch without generated files</name>
  <files>.git/refs/heads/codex/phase-1-foundation-platform-gate</files>
  <action>From the repository root, run `git fetch origin --prune` and re-check the root path, origin URL, worktree mapping, branch tips, and both worktree statuses. Require the main worktree to have no product changes other than GSD bookkeeping created for this quick task, and require the feature worktree to have no tracked or staged changes. Enumerate its untracked files with `git -C .worktrees/phase-1-foundation-platform-gate status --porcelain=v1 --untracked-files=all`; confirm every entry is under `.debugmate-runtime/` or `--help/` and the total remains 979 unless live evidence shows only additional clearly generated files. Never use `git add`, `git clean`, stash, or deletion in the feature worktree. Verify `origin/codex/phase-1-foundation-platform-gate` is an ancestor of the local feature tip; if the remote tip moved to unrelated history, stop rather than rebase, overwrite, or force push. Run the complete default offline pytest suite and Ruff against the feature checkout using that worktree's `.venv`. Only after both pass, push explicitly with `git push origin codex/phase-1-foundation-platform-gate:refs/heads/codex/phase-1-foundation-platform-gate` (no force option), then query `git ls-remote --heads` and require its OID to equal the local feature OID. If push appears to time out, inspect active Git processes and `ls-remote` before deciding whether a retry is needed.</action>
  <verify>
    <automated>$repo = (git rev-parse --show-toplevel).Trim(); $featurePath = Join-Path $repo '.worktrees\phase-1-foundation-platform-gate'; $feature = (git -C $featurePath rev-parse HEAD).Trim(); $tracked = @(git -C $featurePath status --porcelain=v1 --untracked-files=no); if ($tracked.Count) { throw "Feature tracked changes exist: $($tracked -join '; ')" }; $untracked = @(git -C $featurePath ls-files --others --exclude-standard); $invalid = @($untracked | Where-Object { $_ -notmatch '^(\.debugmate-runtime|--help)/' }); if ($invalid.Count) { throw "Unexpected untracked paths: $($invalid -join '; ')" }; $env:PYTHONPATH = (Join-Path $featurePath 'src'); & (Join-Path $featurePath '.venv\Scripts\python.exe') -m pytest -q $featurePath; if ($LASTEXITCODE) { throw 'pytest failed' }; & (Join-Path $featurePath '.venv\Scripts\python.exe') -m ruff check (Join-Path $featurePath 'src') (Join-Path $featurePath 'tests'); if ($LASTEXITCODE) { throw 'ruff failed' }; $remote = git ls-remote --heads origin refs/heads/codex/phase-1-foundation-platform-gate; if (-not $remote -or ($remote -split '\s+')[0] -ne $feature) { throw 'Feature remote ref mismatch' }</automated>
  </verify>
  <done>The tested local feature tip is published at the exact GitHub feature ref; the feature worktree has no tracked changes; all 979 known generated files remain untracked and uncommitted.</done>
</task>

<task type="auto">
  <name>Task 2: Merge both histories on master, resolve known semantic conflicts, and publish</name>
  <files>.git/refs/heads/master, README.md, .planning/STATE.md</files>
  <action>Work only in the main worktree on `master`. Fetch again immediately before merging. If `origin/master` advanced, integrate it without rewriting history: fast-forward when local master has no unique commits, otherwise use a normal merge; stop on ambiguous conflicts. Record the pre-feature-merge master OID and the verified feature OID. Merge with `git merge --no-ff codex/phase-1-foundation-platform-gate` so both histories remain explicit. Resolve expected conflicts semantically: for `README.md`, retain the feature branch's implemented/runnable product documentation instead of master's obsolete “planning only” claims; for `.planning/STATE.md`, retain completed V0.1 course-demo status, 26/26 progress, deliverables and machine-verification facts from the feature side, while unioning every master-only quick-task row (`260719-gy7`, `260719-h5z`, `260720-cmj`) and any newer execution-time bookkeeping. Do not blindly choose `--ours` or `--theirs` for STATE. For any other conflict, inspect base/ours/theirs and preserve both compatible changes; if intent is not mechanically clear, stop with the conflict intact and report it. Stage only named resolved conflict files and complete one merge commit; never add the feature worktree's generated paths. Prove both recorded parent histories are ancestors of the merge result and specifically prove `3a5072e`, `85f2b4b`, `76a431d`, and `ce0aa84` are reachable. Run `git diff --check`, the default offline pytest suite, and Ruff from the merged master tree using the feature worktree's Python environment with `PYTHONPATH` pointed at the main worktree `src`. Then push explicitly with `git push origin master:refs/heads/master`, without any force option. On a push timeout, check Git processes and the remote ref before retrying.</action>
  <verify>
    <automated>$repo = (git rev-parse --show-toplevel).Trim(); if ((git branch --show-current).Trim() -ne 'master') { throw 'Expected master' }; foreach ($oid in @('85f2b4b','76a431d','3a5072e','ce0aa84')) { git merge-base --is-ancestor $oid HEAD; if ($LASTEXITCODE) { throw "Required history is not reachable: $oid" } }; $unmerged = @(git diff --name-only --diff-filter=U); if ($unmerged.Count) { throw "Unresolved conflicts: $($unmerged -join ', ')" }; git diff --check; $python = Join-Path $repo '.worktrees\phase-1-foundation-platform-gate\.venv\Scripts\python.exe'; $env:PYTHONPATH = (Join-Path $repo 'src'); & $python -m pytest -q $repo; if ($LASTEXITCODE) { throw 'pytest failed on merged master' }; & $python -m ruff check (Join-Path $repo 'src') (Join-Path $repo 'tests'); if ($LASTEXITCODE) { throw 'ruff failed on merged master' }; $local = (git rev-parse HEAD).Trim(); $remote = git ls-remote --heads origin refs/heads/master; if (-not $remote -or ($remote -split '\s+')[0] -ne $local) { throw 'master remote ref mismatch' }</automated>
  </verify>
  <done>Master contains both complete histories, known documentation/state conflicts are resolved without losing truthful product or quick-task records, merged code passes offline pytest and Ruff, and the result is non-force-pushed to GitHub.</done>
</task>

<task type="auto">
  <name>Task 3: Independently verify both GitHub refs and final repository hygiene</name>
  <files>.git/refs/remotes/origin/master, .git/refs/remotes/origin/codex/phase-1-foundation-platform-gate</files>
  <action>Do not treat push output as proof. Capture local OIDs for `master` and `codex/phase-1-foundation-platform-gate`, then independently query both full refs with one `git ls-remote --heads origin` call and require exact OID equality. Fetch both refs into their normal remote-tracking refs and require ahead/behind `0 0` for each local/remote pair. Re-run ancestry checks proving the feature tip and all three known master-only commits are reachable from final master. Confirm the main worktree is clean apart from the quick workflow's own not-yet-finalized bookkeeping if applicable. Confirm the feature worktree still has no tracked/staged changes and that every untracked item remains confined to `.debugmate-runtime/` or `--help/`; report the counts rather than deleting them. Record remote URL, both branch names, full and short matching OIDs, ancestry results, test commands/results, merge commit, and excluded generated-file counts in the quick-task SUMMARY, without recording credentials.</action>
  <verify>
    <automated>git fetch origin master codex/phase-1-foundation-platform-gate; $master = (git rev-parse master).Trim(); $feature = (git rev-parse codex/phase-1-foundation-platform-gate).Trim(); $refs = @(git ls-remote --heads origin refs/heads/master refs/heads/codex/phase-1-foundation-platform-gate); $map = @{}; foreach ($line in $refs) { $parts = $line -split '\s+'; $map[$parts[1]] = $parts[0] }; if ($map['refs/heads/master'] -ne $master) { throw 'GitHub master mismatch' }; if ($map['refs/heads/codex/phase-1-foundation-platform-gate'] -ne $feature) { throw 'GitHub feature mismatch' }; foreach ($pair in @(@('origin/master','master'), @('origin/codex/phase-1-foundation-platform-gate','codex/phase-1-foundation-platform-gate'))) { $counts = ((git rev-list --left-right --count "$($pair[0])...$($pair[1])").Trim() -split '\s+'); if ([int]$counts[0] -ne 0 -or [int]$counts[1] -ne 0) { throw "Divergence remains for $($pair[1]): $($counts -join '/')" } }; foreach ($oid in @('85f2b4b','76a431d','3a5072e',$feature)) { git merge-base --is-ancestor $oid master; if ($LASTEXITCODE) { throw "Final master lost history: $oid" } }; $featurePath = '.worktrees\phase-1-foundation-platform-gate'; if (@(git -C $featurePath status --porcelain=v1 --untracked-files=no).Count) { throw 'Feature tracked changes remain' }; $invalid = @(git -C $featurePath ls-files --others --exclude-standard | Where-Object { $_ -notmatch '^(\.debugmate-runtime|--help)/' }); if ($invalid.Count) { throw "Unexpected feature untracked paths: $($invalid -join '; ')" }; git status --short --branch</automated>
  </verify>
  <done>Fresh GitHub queries and fetched tracking refs independently prove both branches are at the intended OIDs with 0/0 divergence; final master retains both histories; generated files remain local and excluded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local worktrees → Git index/history | 979 untrusted generated runtime files must not cross into tracked history. |
| Local refs → GitHub origin | Remote refs may move concurrently; publishing must not overwrite unseen commits. |
| Diverged feature/master histories → merged master | Conflict resolution can silently discard truthful documentation or planning history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q-D6U-01 | Tampering | feature worktree/index | mitigate | Require zero tracked changes, allowlist both generated directories, prohibit add/clean/stash, and inspect the exact ref before push. |
| T-Q-D6U-02 | Tampering | remote refs | mitigate | Fetch before each mutation, require ancestor-safe publication, prohibit all force options, and verify OIDs with `ls-remote`. |
| T-Q-D6U-03 | Repudiation | merge result | mitigate | Use a merge commit, retain both parents, assert required commit ancestry, and record full OIDs plus test results in SUMMARY. |
| T-Q-D6U-04 | Information disclosure | GitHub push / summary | mitigate | Never stage runtime directories or print/store credentials; summary records only paths, counts, URLs, and commit OIDs. |
| T-Q-D6U-05 | Denial of service | push timeout/retry | mitigate | Inspect active Git processes and remote refs before retrying so duplicate large uploads are not started blindly. |
</threat_model>

<verification>
Execution is accepted only when both the complete default offline pytest suite and `ruff check src tests` pass on the final merged master, all known tips are ancestors of master, both remote-tracking comparisons are `0 0`, and fresh `git ls-remote` results exactly match both local OIDs. The 979 known generated files are an intentional local exclusion, not a cleanliness failure, provided they remain untracked under only the two allowlisted directories.
</verification>

<success_criteria>
- `origin/codex/phase-1-foundation-platform-gate` equals the tested local feature tip and includes its 10 previously unpublished commits.
- Final `master` reaches `85f2b4b`, `76a431d`, `3a5072e`, and `ce0aa84` without rewriting or force-pushing either branch.
- README describes the implemented product; STATE retains completed V0.1 facts and master-only quick-task history.
- Offline pytest and Ruff pass against the merged master.
- Fresh `ls-remote` and fetched remote-tracking refs independently prove exact equality for both GitHub branches.
- `.debugmate-runtime/` and `--help/` generated files remain local, untracked, uncommitted, and undeleted.
</success_criteria>

<output>
After execution, create `.planning/quick/260720-d6u-codex-phase-1-foundation-platform-gate-m/260720-d6u-SUMMARY.md` and update `.planning/STATE.md` according to the quick workflow. The summary must include the merge commit, both local/remote full OIDs, retained-history ancestry checks, pytest/Ruff results, and the excluded untracked counts.
</output>
