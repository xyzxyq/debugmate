---
id: 260719-h5z-debugmate-readme
type: quick
title: 撰写并发布 DebugMate 项目 README
autonomous: true
files_modified:
  - README.md
requirements:
  - README 准确说明 DebugMate 的项目定位、当前规划状态和后续路线，不将规划能力表述为已可运行功能
must_haves:
  truths:
    - "仓库根目录提供一份专业、清晰的中文 README，说明 DebugMate 的目的、规划架构、能力边界与隐私原则。"
    - "README 明确项目仍处于规划与 Phase 1 执行阶段，当前没有可运行应用、安装命令或演示入口。"
    - "README 中的目录说明、技术栈和路线图均可追溯到当前已跟踪文件及 .planning 的权威资料。"
  artifacts:
    - path: "README.md"
      provides: "DebugMate 的中文项目说明与当前状态声明"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: ".planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md"
      via: "相对 Markdown 链接和基于这些资料的事实表述"
      pattern: "\.planning/(PROJECT|REQUIREMENTS|ROADMAP|STATE)\.md"
---

<objective>
为 DebugMate 创建并发布一份与当前仓库事实一致的中文 README。

Purpose: 让课程评阅者和协作者能迅速理解项目目标、可复现性设计、当前进度与后续实现边界，而不会把规划内容误解为已交付功能。
Output: 根目录 `README.md`，以及仅包含该文件的已验证 Git 提交与远端同步。
</objective>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md

Planning facts verified on 2026-07-19: Git 已跟踪内容仅含 `AGENTS.md`、`.gitignore` 与 `.planning/` 规划资产；根目录尚无 `README.md` 或产品代码。STATE 表明项目处于 Phase 1（工程骨架与平台能力闸门）执行中，完成计划数为 0。`master` 工作树在规划时干净，且已有私有 GitHub 远端。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 基于权威规划资料撰写事实准确的中文 README</name>
  <files>README.md</files>
  <action>创建根目录 `README.md`，使用专业、简洁的中文 Markdown。必须包含：一句话定位与项目目的；面向 AI 专业学习场景的报错诊断问题说明；明确标注“当前状态：规划完成、Phase 1 执行中，尚无可运行应用/安装步骤/在线演示”；规划中的端到端架构与数据流（本地脱敏与 Pydantic 校验 → Dify Cloud 工作流/RAG/视觉/TTS → 本地证据、Markdown、确定性 PNG、MP3、Gradio 结果页），所有能力均用“计划/将/目标”措辞；核心能力和约束（证据引用、不确定性、同一结构化诊断对象派生三模态、不得自动执行修复命令、真实证据与默认脱敏、免费优先）；仅依据 `git ls-files` 的当前目录地图，区分现有 `.planning/` 资料、`AGENTS.md` 与未来会生成的产品资产；六阶段路线图；当前开始方式（阅读规划资料、等待 Phase 1 实现，而非编造安装或运行命令）；计划技术栈（Dify Cloud、Python 3.13、Gradio、Pydantic、HTTPX、Pillow、RapidOCR/ONNX Runtime、pytest/Ruff、FFmpeg 与 edge-tts 降级）；隐私与贡献边界。链接 `.planning/PROJECT.md`、`.planning/REQUIREMENTS.md`、`.planning/ROADMAP.md`、`.planning/STATE.md`。不得声称平台能力已验证、软件已实现、测试已通过或产物已生成；不得添加徽章、截图、虚构文件、依赖安装指令、API 密钥、外部链接或未经确认的命令。</action>
  <verify>
    <automated>$readme = Get-Content -LiteralPath README.md -Raw -Encoding UTF8; $required = @('当前状态', 'Phase 1', '尚无可运行应用', '项目目的', '规划', '技术栈', '隐私', '贡献', '.planning/PROJECT.md', '.planning/REQUIREMENTS.md', '.planning/ROADMAP.md', '.planning/STATE.md'); $missing = $required | Where-Object { $readme -notmatch [regex]::Escape($_) }; if ($missing) { throw "README missing required content: $($missing -join ', ')" }; $forbidden = @('pip install', 'python -m', '已部署', '已上线', '已实现', '测试通过'); $present = $forbidden | Where-Object { $readme -match [regex]::Escape($_) }; if ($present) { throw "README makes unsupported implementation claims: $($present -join ', ')" }; git diff --check -- README.md</automated>
  </verify>
  <done>`README.md` 以中文准确区分已存在的规划资产与未来实现，完整覆盖约定栏目，且不包含虚构的运行方式、文件或完成状态。</done>
</task>

<task type="auto">
  <name>Task 2: 校验 Markdown 与仓库事实后提交并发布 README</name>
  <files>README.md</files>
  <action>在提交前重新运行 `git ls-files`，逐项核对 README 的目录地图只描述已跟踪目录和文件；检查相对链接目标均存在，确认项目状态文字与 `.planning/STATE.md` 一致。使用可用的本地 Markdown 链接检查工具；若工具未安装，不新增依赖，改用 PowerShell 解析 README 中以 `.planning/` 开头的相对链接并验证路径存在。运行 `git diff --check`，检查工作区仅包含预期的 `README.md` 变更。仅暂存 `README.md`，用 `docs: add DebugMate project README` 创建原子提交，然后执行 `git push origin master`。推送后用 `git ls-remote --heads origin master` 比对远端 SHA 与本地 `HEAD`；不得使用 `git add -A`，不得改动 `.planning/STATE.md`、路线图或任何产品文件。</action>
  <verify>
    <automated>$tracked = git ls-files; $readme = Get-Content -LiteralPath README.md -Raw -Encoding UTF8; $links = [regex]::Matches($readme, '\]\((\.planning/[^)#]+)') | ForEach-Object { $_.Groups[1].Value }; $missingLinks = $links | Where-Object { -not (Test-Path -LiteralPath $_) }; if ($missingLinks) { throw "Broken README planning links: $($missingLinks -join ', ')" }; git diff --check HEAD -- README.md; $unexpected = git status --short | Where-Object { $_ -notmatch 'README\.md$' }; if ($unexpected) { throw "Unexpected worktree changes: $($unexpected -join '; ')" }; $local = git rev-parse HEAD; $remote = (git ls-remote --heads origin master).Split([char]9)[0]; if ($local -ne $remote) { throw 'README commit is not synchronized to origin/master' }; git status --short</automated>
  </verify>
  <done>README 的链接和目录描述与当前仓库一致，Markdown 无空白错误，提交仅包含 README，且本地与 `origin/master` 指向相同提交。</done>
</task>

</tasks>

<verification>
完成 Task 2 的自动化校验。确认 README 明确写明当前仍无可运行应用，所有 `.planning` 链接存在，`git diff --check` 无输出，工作区干净，并且 `origin/master` 的 SHA 与本地 `HEAD` 相同。
</verification>

<success_criteria>
- 根目录有一份专业、清晰的中文 README，覆盖项目目的、规划架构/流程、能力与约束、现有目录地图、路线图、当前开始方式、技术栈、隐私与贡献。
- README 只陈述当前仓库与规划资料可证明的事实，并醒目标明 Phase 1 及无可运行应用状态。
- README 已作为唯一变更提交并发布到现有私有远端。
</success_criteria>

<output>
完成后，在同一 quick 目录创建执行摘要，并按 quick-workflow 约定更新 `.planning/STATE.md` 的 Quick Tasks Completed 记录。
</output>
