# Quick Task 260831-s7t: 修复 Dify Knowledge Retrieval records.segment 结构适配并完成真实验收

## Objective

让 Dify 云端 Knowledge Retrieval 的真实 `records[].segment` 返回结构能够进入本地严格 `DiagnosisResult` 契约，同时保留来源、定位和 knowledge build 绑定；完成离线回归与真实云端验收。

## Tasks

### 1. 更新 Dify 知识证据净化器

- **Files:** `platform/dify/app.dsl.yml`
- **Action:** 在知识证据净化 Python 节点中兼容 Dify 检索返回的 `segment.content`、`segment.id`、`segment.document.name` 与 `segment.document.doc_metadata`，同时兼容已有顶层字段；保留 HTTPS、source_id、locator、摘要、标题和 build ID 的 fail-closed 校验。
- **Verify:** DSL 结构测试、YAML/节点契约检查。
- **Done:** Dify 真实检索记录可生成最多 4 条带可追溯元数据的 `retrieval_trace.hits`。

### 2. 增加回归覆盖并完成验收

- **Files:** `tests/platform/test_dify_dsl.py`, `.planning/STATE.md`
- **Action:** 增加 records.segment 形状的净化测试；运行相关测试与 Ruff；发布更新后的 DSL 后运行真实 Dify cloud acceptance。
- **Verify:** 离线测试通过；云端验收通过并产出新的脱敏 evidence。
- **Done:** 本地与 Dify 云端使用同一严格输出契约，且检索命中非空、来源可验证。
