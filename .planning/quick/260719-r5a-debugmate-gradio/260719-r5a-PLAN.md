---
quick_id: 260719-r5a
description: 将 DebugMate Gradio 前端重构为深色开发者命令中心，保留全部业务与无障碍语义
mode: quick-full
date: 2026-07-19
must_haves:
  truths:
    - 深色命令中心在 1366x768 首屏突出状态、诊断摘要和结果区，不再由长输入表单主导。
    - 现有服务合同、回调输出顺序、elem_id、回放、纠错、下载和失败降级语义保持不变。
    - 键盘、200% 缩放、长内容、同次 ZIP 下载与完成/部分/失败状态仍通过真实 Edge 验证。
    - 课程截图、PPT 和视频使用改版后的真实界面，不继续展示旧 UI。
  artifacts:
    - path: src/debugmate/ui/app.py
      provides: 深色 token、命令栏、控制轨、诊断主画布和结果工作区
    - path: tests/ui/test_app.py
      provides: 结构、token、组件身份和回调合同测试
    - path: tests/ui/test_browser.py
      provides: 真实 Edge 布局、无障碍和长内容回归
    - path: evidence/course-v0.1/screenshots/
      provides: 完成与部分完成新版真实截图
  key_links:
    - from: src/debugmate/ui/presentation.py
      to: src/debugmate/ui/app.py
      via: ComponentViewModel 保持现有输出映射
    - from: evidence/course-v0.1/screenshots/
      to: scripts/build-course-ppt.py
      via: 固定截图路径重新生成 PPT
    - from: evidence/course-v0.1/screenshots/
      to: scripts/build-course-video.py
      via: 固定截图路径重新生成视频
---

# Quick Task 260719-r5a Plan

## Task 1 — 深色命令中心结构与视觉系统

**files:** `src/debugmate/ui/app.py`, `tests/ui/test_app.py`

**action:** 先将旧三列 CSS 合同测试改为深色 token、命令栏、控制轨、诊断画布、结果区和折叠纠错区合同并验证 RED；随后仅重组 `build_app()` 的 Gradio 容器与 `elem_classes`，保留所有组件实例、`elem_id`、回调输出数组和状态映射顺序。新增石墨/青色/琥珀色 token、细分隔线、紧凑按钮、表格、标签页、输入控件和三档响应式网格；事实表保持关键回溯行首屏可见；把抽取字段输入、修改草稿和新运行确认放入默认关闭的 Accordion。部分完成继续默认显示报告，并通过失败详情说明失败产物和重试范围，不新增 tab 状态输出。

**verify:** `.\.venv\Scripts\python.exe -m pytest -q tests\ui\test_app.py tests\ui\test_view_state.py tests\ui\test_callbacks.py`; `.\.venv\Scripts\python.exe -m ruff check src\debugmate\ui\app.py tests\ui\test_app.py`

**done:** 单元测试确认新版结构与全部原有组件/回调合同共存，Ruff 通过。

## Task 2 — 真实 Edge 布局与无障碍回归

**files:** `tests/ui/test_browser.py`, `src/debugmate/ui/app.py`

**action:** 更新只与旧三列几何绑定的断言，使 1366px 验证控制轨、诊断画布、结果工作区同屏；1024px 验证双列重排；768px 验证单列顺序。保留并运行 VQ-13、VQ-15、VQ-14、长内容和 V0.1 下载测试，修复发现的视觉溢出、焦点或 tab 可达性问题，不放宽内容/身份/安全断言。

**verify:** `.\.venv\Scripts\python.exe -m pytest -q -m browser tests\ui\test_browser.py -k "vq_13 or vq_15 or vq_14 or long_content or v01_download or responsive or gap_01"`

**done:** 代表性真实 Edge 场景全部通过，页面无整体横向滚动，关键操作可见可达。

## Task 3 — 新版真实证据与课程材料同步

**files:** `evidence/course-v0.1/`, `deliverables/`, `.planning/STATE.md`

**action:** 运行 `.\scripts\run-phase4-truth-state-qa.ps1 -TestExpression 'vq_02 or vq_06 or vq_07'`，从最新 runner staging 目录选择并人工查看三张当前代码全页截图，覆盖课程证据固定文件；用 `Get-FileHash -Algorithm SHA256` 和文件长度更新 evidence manifest。使用 bundled Python 精确路径运行 PPT 脚本、项目 venv 运行视频脚本；通过 PowerShell 打开 PPTX ZIP 统计 13 个 slide XML，通过 ffprobe 验证视频时长 >180 秒并重算所有 manifest 哈希。写 SUMMARY 与 VERIFICATION，并在 STATE 的 Quick Tasks Completed 表记录本任务。

**verify:** `.\scripts\run-phase4-truth-state-qa.ps1 -TestExpression 'vq_02 or vq_06 or vq_07'`; `& 'C:\Users\20795\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts\build-course-ppt.py`; `.\.venv\Scripts\python.exe scripts\build-course-video.py`; `ffprobe -v error -show_entries format=duration -of default=nw=1 deliverables\DebugMate-V0.1-demo.mp4`; manifest SHA-256 recomputation; `git diff --check`

**done:** 新版截图、PPT、视频和 manifests 哈希一致，快速任务状态记录完整。
