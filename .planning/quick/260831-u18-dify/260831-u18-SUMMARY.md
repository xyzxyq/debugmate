# Quick Task 260831-u18 Summary

## Completed

- Added the current `knowledge_build_id` to the sanitized retrieval JSON passed to the diagnosis LLM.
- Updated the Dify safety sink to accept evidence only when every evidence item is bound to the expected knowledge build.
- Invalid-build evidence is cleared and treated as no evidence, which caps confidence at `0.70`, clears fixes, and replaces unsafe installation advice.
- Added regression coverage for prompt build identity and the invalid-build safety path.

## Verification

- `pytest -q tests/platform/test_dify_dsl.py`: 27 passed.
- `ruff check tests/platform/test_dify_dsl.py`: passed.
- Authoritative DSL YAML parse: passed; 9 workflow nodes detected.
- GitHub `origin/master`: matches commit `f255464bcaf64519472a6498ae890da491c4a96e`.

## Remaining manual step

Re-import and publish `platform/dify/app.dsl.yml` in Dify, then rerun the fixed case. For the expedited closeout, accept the project once the returned envelope is strict-valid, has non-empty retrieval hits, uses the expected knowledge build ID in evidence, and does not recommend installing the fake package.
