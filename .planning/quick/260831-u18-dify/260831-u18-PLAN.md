# Quick Task 260831-u18: 修复 Dify 证据构建版本绑定与安装建议安全收口

## Objective

让 LLM 获得正确的知识构建版本，并拒绝错误构建证据与未知包安装建议，避免云端结果无法通过严格合同。

## Tasks

### 1. 修复证据版本传递与安全收口

- **Files:** `platform/dify/app.dsl.yml`
- **Action:** 将 `knowledge_build_id` 注入 LLM 使用的检索 JSON；安全节点只接受当前构建的 evidence，错误构建视为无证据并清空 evidence、fixes 与安装建议。
- **Verify:** DSL 单元测试覆盖 prompt evidence build ID 和错误 build 收口。
- **Done:** 错误 build 不会进入最终诊断，未知包不会被建议安装。

### 2. 回归验证、提交并同步

- **Files:** `tests/platform/test_dify_dsl.py` 和 GSD 记录。
- **Action:** 运行 pytest、Ruff、YAML 解析，提交并推送 GitHub。
- **Verify:** 测试通过且远端 master 与本地一致。
- **Done:** 用户重新发布 DSL 后可进行最终云端验收。
