---
phase: 01-foundation-platform-gate
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/debugmate/hashing.py
  - src/debugmate/evidence.py
  - src/debugmate/settings.py
  - evidence/.gitkeep
  - tests/test_evidence.py
autonomous: true
requirements: [EVID-01, EVID-02]
user_setup: []
must_haves:
  truths:
    - "A successful case is published only when a complete valid manifest exists"
    - "A failed case remains explicitly failed and cannot look complete"
    - "Every published artifact hash can be recomputed from disk"
    - "Settings and logs never serialize Dify secret values"
  artifacts:
    - path: "src/debugmate/evidence.py"
      provides: "Atomic evidence bundle writer and manifest validation"
      contains: "class EvidenceBundle"
    - path: "src/debugmate/hashing.py"
      provides: "Canonical JSON and file SHA-256 helpers"
      contains: "def sha256_file"
    - path: "tests/test_evidence.py"
      provides: "Atomicity, failure-state, hash and secret safety tests"
  key_links:
    - from: "src/debugmate/evidence.py"
      to: "src/debugmate/contracts.py"
      via: "case_id and schema version validation"
      pattern: "DiagnosisRecord|case_id"
---

<objective>
Build the tamper-evident, atomic evidence store and safe settings boundary.

Purpose: Make every run auditable and prevent partial or secret-bearing artifacts from becoming course evidence.
Output: Hashing utilities, strict manifest/evidence writer, environment settings, and integration tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-platform-gate/01-CONTEXT.md
@.planning/phases/01-foundation-platform-gate/01-RESEARCH.md
@.planning/phases/01-foundation-platform-gate/01-VALIDATION.md
@.planning/phases/01-foundation-platform-gate/01-01-SUMMARY.md
@src/debugmate/contracts.py
</context>

<threat_model>

| ID | Threat | Severity | Structural control | Verification |
|----|--------|----------|--------------------|--------------|
| T2-01 | API key enters manifest, repr, or exception text | Critical | SecretStr settings, redacted repr, manifest allowlist, recursive secret scanner | `test_settings_do_not_serialize_secrets` and grep gate |
| T2-02 | Half-written directory appears successful | High | Write under `.tmp-<case_id>`, validate all artifacts, atomically replace final directory | interruption/failure tests assert no successful final bundle |
| T2-03 | Artifact modified after manifest | High | SHA-256 + byte size for every artifact and verification function | tamper test changes one byte and must fail |
| T2-04 | Absolute local paths leak | High | Manifest stores POSIX relative paths only and rejects absolute/parent traversal | path validation tests |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical hashing and secret-safe settings</name>
  <files>src/debugmate/hashing.py, src/debugmate/settings.py, tests/test_evidence.py</files>
  <read_first>src/debugmate/contracts.py, .env.example, .gitignore, .planning/phases/01-foundation-platform-gate/01-CONTEXT.md</read_first>
  <action>Implement `canonical_json_bytes(value)` using UTF-8, `sort_keys=True`, compact separators and `ensure_ascii=False`; `sha256_bytes`, `sha256_file`, and `artifact_metadata(root, path, mime)` returning POSIX relative path, MIME, byte count and lowercase 64-hex SHA-256. Reject paths outside root. Implement Pydantic Settings-style `DebugMateSettings` without adding pydantic-settings: load environment using `os.environ`, wrap API keys with `SecretStr`, default base URL `https://api.dify.ai/v1` and user `debugmate-local`, provide `cloud_configured` property, and expose only `safe_summary()` containing booleans and non-secret URL/user. Add recursive `find_secret_leaks(value, forbidden_values)` that scans strings but never logs the forbidden value itself. Start `tests/test_evidence.py` with focused tests for deterministic canonical JSON, file hashing, path rejection, non-empty secret not present in repr/model dump/safe summary, and leak detector returning field paths only.</action>
  <verify>.\.venv\Scripts\python.exe -m pytest -q tests/test_evidence.py -k "hash or secret or path"</verify>
  <acceptance_criteria>
    - `hashing.py` contains `canonical_json_bytes`, `sha256_file`, and `artifact_metadata`.
    - `settings.py` contains `SecretStr`, `cloud_configured`, and `safe_summary`; no function returns `get_secret_value()` in a serializable mapping.
    - Absolute paths and `..` traversal raise a typed error.
    - Focused pytest exits 0 and no assertion includes the literal test secret in failure messages.
  </acceptance_criteria>
  <done>Hashing is deterministic and settings expose capability state without exposing credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Implement atomic evidence bundles and manifest verification</name>
  <files>src/debugmate/evidence.py, evidence/.gitkeep, tests/test_evidence.py</files>
  <read_first>src/debugmate/contracts.py, src/debugmate/hashing.py, src/debugmate/settings.py, .planning/phases/01-foundation-platform-gate/01-RESEARCH.md</read_first>
  <action>Define strict `ArtifactEntry`, `CapabilityEvidence`, `RunManifest`, and `EvidenceBundle` models/classes. Fix `MANIFEST_VERSION="1.0.0"` and status enum `running|passed|failed|blocked`. `EvidenceBundle.begin(root, case_id)` creates sibling `.tmp-<case_id>` only; `write_json(relative_path, value)` and `write_bytes` reject absolute/traversal paths; `finalize(manifest)` recomputes every artifact, writes `manifest.json` last, validates case ID/schema/status/timestamps, and atomically publishes to `root/<case_id>` with `Path.replace`. `fail(error_code, safe_message)` writes a failed manifest without traceback, Authorization header or secret. Implement `verify_bundle(path)` to recompute hashes, reject missing/unlisted files except manifest, and return structured issues. Tests must cover passed bundle, failed bundle, duplicate final path, missing manifest, simulated exception before finalize, tamper, unlisted artifact, case mismatch, absolute path rejection and secret scanning. Keep runtime evidence ignored except `.gitkeep`.</action>
  <verify>.\.venv\Scripts\python.exe -m pytest -q tests/test_evidence.py; .\.venv\Scripts\python.exe -m ruff check src/debugmate/hashing.py src/debugmate/settings.py src/debugmate/evidence.py tests/test_evidence.py</verify>
  <acceptance_criteria>
    - `evidence.py` contains `MANIFEST_VERSION = "1.0.0"`, `class RunManifest`, `class EvidenceBundle`, and `def verify_bundle`.
    - Manifest artifacts use relative POSIX paths, SHA-256 and byte counts.
    - `manifest.json` is written after all other artifacts and no `passed` bundle appears when finalize is interrupted.
    - Tampering or adding an unlisted file makes `verify_bundle` return failure.
    - Full evidence tests and Ruff exit 0.
  </acceptance_criteria>
  <done>Evidence bundles are atomic, tamper-evident, case-linked and secret-safe.</done>
</task>

</tasks>

<verification>
- [ ] `.\.venv\Scripts\python.exe -m pytest -q tests/test_evidence.py` exits 0.
- [ ] `.\.venv\Scripts\python.exe -m pytest -q -m "not cloud"` exits 0.
- [ ] `.\.venv\Scripts\python.exe -m ruff check src tests` exits 0.
- [ ] A one-byte artifact mutation is detected by `verify_bundle`.
- [ ] `git status --short -- evidence` shows only the tracked `.gitkeep`.
</verification>

<success_criteria>
- All evidence records are tied to a valid case ID.
- Passed, failed and blocked runs are distinguishable by machine-readable status.
- No secret or absolute local path is written to the manifest.
- Published evidence can be independently rehashed and verified.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-platform-gate/01-02-SUMMARY.md`.
</output>
