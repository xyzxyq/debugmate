---
phase: 01-foundation-platform-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - .env.example
  - README.md
  - src/debugmate/__init__.py
  - src/debugmate/contracts.py
  - src/debugmate/adapters/__init__.py
  - src/debugmate/adapters/base.py
  - src/debugmate/adapters/fixture.py
  - fixtures/cases/module_not_found/input.json
  - fixtures/cases/module_not_found/diagnosis.json
  - tests/test_contracts.py
  - tests/test_fixture_adapter.py
autonomous: true
requirements: [INP-04, EVID-02]
user_setup: []
must_haves:
  truths:
    - "Every generated case ID matches ^case_[0-9a-f]{32}$"
    - "DiagnosisRecord rejects extra fields and wrong strict types"
    - "The offline fixture backend returns a schema-valid diagnosis with the requested case ID"
  artifacts:
    - path: "src/debugmate/contracts.py"
      provides: "DiagnosisRecord v1 and case ID contract"
      contains: "class DiagnosisRecord"
    - path: "src/debugmate/adapters/base.py"
      provides: "Narrow DiagnosisBackend protocol"
      contains: "class DiagnosisBackend"
    - path: "fixtures/cases/module_not_found/diagnosis.json"
      provides: "Safe deterministic diagnosis fixture"
      contains: "ModuleNotFoundError"
  key_links:
    - from: "src/debugmate/adapters/fixture.py"
      to: "src/debugmate/contracts.py"
      via: "Fixture result validation"
      pattern: "DiagnosisRecord"
---

<objective>
Create the installable Python skeleton, freeze `DiagnosisRecord v1`, and provide a deterministic offline backend.

Purpose: Establish the platform-independent contract and case identity before any cloud integration.
Output: Python package metadata, strict Pydantic models, fixture adapter, safe ModuleNotFoundError fixture, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-platform-gate/01-CONTEXT.md
@.planning/phases/01-foundation-platform-gate/01-RESEARCH.md
@.planning/phases/01-foundation-platform-gate/01-VALIDATION.md
</context>

<threat_model>

| ID | Threat | Severity | Structural control | Verification |
|----|--------|----------|--------------------|--------------|
| T1-01 | Fixture contains real identity or secret | High | Use only `C:\\Users\\student\\demo` and package name `demo_missing_pkg`; reject key-like fields from fixture metadata | `tests/test_fixture_adapter.py` scans fixture text for banned patterns |
| T1-02 | Cloud or fixture output silently drifts from schema | High | `ConfigDict(strict=True, extra='forbid')`; generated JSON Schema is the only schema source | `tests/test_contracts.py` rejects extra fields, strings for numeric confidence, and invalid version |
| T1-03 | Case artifacts cannot be linked | High | `case_<uuid4 hex>` generated once and injected into every returned record | `tests/test_contracts.py` asserts format and equality across request/result |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Python 3.13 src-layout and quality tooling</name>
  <files>pyproject.toml, .gitignore, .env.example, README.md, src/debugmate/__init__.py, src/debugmate/adapters/__init__.py</files>
  <read_first>AGENTS.md, .planning/research/STACK.md, .planning/phases/01-foundation-platform-gate/01-RESEARCH.md</read_first>
  <action>Create `pyproject.toml` for package `debugmate` with `requires-python = ">=3.13,<3.14"`, runtime dependencies `pydantic==2.13.4`, `httpx==0.28.1`, `python-dotenv==1.2.2`, and optional dev dependencies `pytest==9.1.1`, `pytest-cov`, `ruff==0.15.21`. Configure setuptools `src` discovery, pytest marker `cloud`, and Ruff target `py313` with line length 100. Create `.gitignore` entries for `.venv/`, `.env`, `.artifacts/`, `evidence/*` except `evidence/.gitkeep`, `__pycache__/`, `.pytest_cache/`, `.ruff_cache/`, and coverage files. Create `.env.example` with empty `DIFY_BASE_URL=https://api.dify.ai/v1`, `DIFY_API_KEY=`, `DIFY_DATASET_API_KEY=`, `DIFY_USER=debugmate-local`. Create README sections `## 项目定位`, `## Phase 1`, `## 本地开发`, `## 安全边界`; state that fixture is not a cloud result and no repair command is executed. Export `__version__ = "0.1.0"`.</action>
  <verify>python -m venv .venv; .\.venv\Scripts\python.exe -m pip install -e ".[dev]"; .\.venv\Scripts\python.exe -m ruff check pyproject.toml src tests 2&gt;$null; if ($LASTEXITCODE -eq 0) { exit 0 } else { .\.venv\Scripts\python.exe -m ruff check src tests }</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `requires-python = ">=3.13,<3.14"`, `pydantic==2.13.4`, and `[tool.pytest.ini_options]` with marker `cloud`.
    - `.env.example` contains the four Dify variable names with no non-empty secret.
    - `.gitignore` contains `.env`, `.venv/`, and `evidence/*` plus `!evidence/.gitkeep`.
    - `README.md` contains all four required Chinese headings and the phrase `fixture 不是云端运行结果`.
    - `.\.venv\Scripts\python.exe -c "import debugmate; print(debugmate.__version__)"` prints `0.1.0`.
  </acceptance_criteria>
  <done>The editable package installs on local Python 3.13 and the repository has explicit secret/evidence ignore rules.</done>
</task>

<task type="auto">
  <name>Task 2: Define the strict DiagnosisRecord v1 contract test-first</name>
  <files>src/debugmate/contracts.py, tests/test_contracts.py</files>
  <read_first>.planning/phases/01-foundation-platform-gate/01-CONTEXT.md, .planning/phases/01-foundation-platform-gate/01-RESEARCH.md, pyproject.toml</read_first>
  <action>Write failing tests first for `new_case_id()`, `DiagnosisRecord`, nested `RootCauseCandidate`, `Citation`, `CommandStep`, and `CapabilityStatus`. Implement models using `ConfigDict(strict=True, extra="forbid")`. Set `SCHEMA_VERSION = "1.0.0"`; case ID regex `^case_[0-9a-f]{32}$`; categories enum values `dependency_environment`, `path_permission`, `python_runtime`, `tensor_shape_dtype`, `cuda_memory`, `model_loading`, `unknown`; confidence `0.0..1.0`. `CommandStep` fields are `command`, `platform`, `impact`, `expected_result`, `rollback` and contain no execution method. Add `diagnosis_schema()` returning `DiagnosisRecord.model_json_schema()` and `schema_sha256()` over canonical JSON (`sort_keys=True`, UTF-8, compact separators). Ensure tests cover successful roundtrip and failures for extra fields, invalid IDs, wrong confidence types, missing rollback, and mismatched schema version.</action>
  <verify>.\.venv\Scripts\python.exe -m pytest -q tests/test_contracts.py</verify>
  <acceptance_criteria>
    - `src/debugmate/contracts.py` contains `SCHEMA_VERSION = "1.0.0"`, `class DiagnosisRecord`, `class CommandStep`, `def new_case_id`, and `def schema_sha256`.
    - No model permits extra fields or implicit string-to-float confidence conversion.
    - `CommandStep` has no `run`, `execute`, `subprocess`, or shell invocation method.
    - `pytest -q tests/test_contracts.py` exits 0 with tests for each named invalid case.
  </acceptance_criteria>
  <done>A single generated schema strictly governs all diagnosis data and case identity.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the narrow backend protocol and offline fixture</name>
  <files>src/debugmate/adapters/base.py, src/debugmate/adapters/fixture.py, fixtures/cases/module_not_found/input.json, fixtures/cases/module_not_found/diagnosis.json, tests/test_fixture_adapter.py</files>
  <read_first>src/debugmate/contracts.py, .planning/phases/01-foundation-platform-gate/01-RESEARCH.md, tests/test_contracts.py</read_first>
  <action>Define `DiagnosisBackend` as a runtime-checkable Protocol with exact methods `upload_file(path: Path, user: str)`, `run_workflow(inputs: dict[str, object], user: str)`, `synthesize_audio(text: str, user: str)`, and `capability_probe()`. Define typed result models or dataclasses without importing Dify. Implement `FixtureBackend` loading UTF-8 JSON from `fixtures/cases/<name>`, replacing only the fixture `case_id` with the caller-provided valid case ID, then validating through `DiagnosisRecord.model_validate`. The fixture input must describe a Python `ModuleNotFoundError: No module named 'demo_missing_pkg'` under fictional path `C:\\Users\\student\\demo\\main.py`. Mark all fixture capability statuses explicitly and include `backend="fixture"`; never emit `pass` for cloud-only capabilities. Tests must assert schema validity, stable fixture content, case propagation, missing fixture errors, and absence of patterns `sk-`, `Bearer `, email addresses, real username `20795`, or `DIFY_API_KEY` values.</action>
  <verify>.\.venv\Scripts\python.exe -m pytest -q tests/test_contracts.py tests/test_fixture_adapter.py; .\.venv\Scripts\python.exe -m ruff check src tests</verify>
  <acceptance_criteria>
    - `base.py` contains all four exact protocol methods and no platform-specific imports.
    - `fixture.py` validates through `DiagnosisRecord.model_validate` and sets `backend` to `fixture`.
    - Fixture files contain `demo_missing_pkg`, `C:\\Users\\student\\demo`, and no banned secret/identity pattern.
    - Focused pytest and Ruff commands both exit 0.
  </acceptance_criteria>
  <done>The project can produce a genuine schema-valid offline diagnosis clearly labeled as fixture data.</done>
</task>

</tasks>

<verification>
- [ ] `.\.venv\Scripts\python.exe -m pytest -q tests/test_contracts.py tests/test_fixture_adapter.py` exits 0.
- [ ] `.\.venv\Scripts\python.exe -m ruff check src tests` exits 0.
- [ ] `git grep -n -I -E "20795|Bearer |sk-[A-Za-z0-9_-]{8,}" -- fixtures src tests` returns no matches.
- [ ] `DiagnosisRecord.model_json_schema()` is JSON serializable and its SHA-256 is stable across two calls.
</verification>

<success_criteria>
- All tasks completed and focused tests pass.
- A new case ID is unique, valid, and propagated through the fixture result.
- Strict schema failures are tested rather than silently repaired.
- Fixture data is safe, deterministic, and unmistakably non-cloud.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-platform-gate/01-01-SUMMARY.md`.
</output>
