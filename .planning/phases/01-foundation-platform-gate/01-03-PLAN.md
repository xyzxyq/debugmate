---
phase: 01-foundation-platform-gate
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/debugmate/adapters/dify.py
  - src/debugmate/probe.py
  - src/debugmate/cli.py
  - scripts/run_phase1_probe.ps1
  - platform/dify/README.md
  - platform/dify/app.dsl.yml.example
  - platform/dify/capability-matrix.json
  - prompts/README.md
  - contracts/diagnosis-record-v1.schema.json
  - knowledge/manifest.schema.json
  - tests/test_probe_cli.py
  - README.md
autonomous: true
requirements: [EVID-01, EVID-02]
user_setup:
  - service: dify-cloud
    why: "A real cloud capability probe requires the user's Dify account and app/dataset credentials. Offline implementation and fixture verification continue without them."
    env_vars:
      - name: DIFY_API_KEY
        source: "Dify app -> API Access -> API Key"
      - name: DIFY_DATASET_API_KEY
        source: "Dify Knowledge -> API -> API Key"
      - name: DIFY_BASE_URL
        source: "Use https://api.dify.ai/v1 unless the account uses a different official endpoint"
    dashboard_config:
      - task: "Create a minimal Workflow app with file input, vision extraction, knowledge retrieval and structured JSON output"
        location: "Dify Studio"
        details: "Use only the fictional ModuleNotFoundError probe; do not upload personal logs"
      - task: "Configure a TTS provider if one is available in the account"
        location: "Dify model provider settings"
        details: "Do not purchase credits without explicit approval"
must_haves:
  truths:
    - "The fixture probe completes offline and writes a seven-capability machine-readable report"
    - "Cloud probe statuses are pass, fail, blocked, or not-tested and never inferred from fixture success"
    - "Dify credentials never appear in CLI output, evidence, examples, or tracked files"
    - "The repository contains versioned Dify reconstruction placeholders and a concrete human setup checklist"
  artifacts:
    - path: "src/debugmate/probe.py"
      provides: "Seven-capability probe orchestration"
      contains: "CAPABILITY_IDS"
    - path: "src/debugmate/adapters/dify.py"
      provides: "Narrow Dify HTTP adapter"
      contains: "class DifyBackend"
    - path: "scripts/run_phase1_probe.ps1"
      provides: "Windows fixture/cloud probe entry point"
      contains: "fixture-probe"
    - path: "platform/dify/capability-matrix.json"
      provides: "Versioned capability gate structure"
      contains: "C07"
  key_links:
    - from: "src/debugmate/cli.py"
      to: "src/debugmate/probe.py"
      via: "probe subcommands"
      pattern: "fixture-probe|cloud-probe"
    - from: "src/debugmate/probe.py"
      to: "src/debugmate/evidence.py"
      via: "probe result evidence bundle"
      pattern: "EvidenceBundle"
---

<objective>
Add the Dify seam and a truthful Phase 1 capability probe with offline fallback and auditable reporting.

Purpose: Decide the cloud path using real evidence while keeping the project executable without an account.
Output: Dify adapter, fixture/cloud probe CLI, PowerShell runner, capability matrix, platform reconstruction docs, tests, and user setup metadata.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundation-platform-gate/01-CONTEXT.md
@.planning/phases/01-foundation-platform-gate/01-RESEARCH.md
@.planning/phases/01-foundation-platform-gate/01-VALIDATION.md
@.planning/phases/01-foundation-platform-gate/01-01-SUMMARY.md
@.planning/phases/01-foundation-platform-gate/01-02-SUMMARY.md
@src/debugmate/adapters/base.py
@src/debugmate/evidence.py
</context>

<threat_model>

| ID | Threat | Severity | Structural control | Verification |
|----|--------|----------|--------------------|--------------|
| T3-01 | Authorization header/API key is logged | Critical | `httpx` request construction in one adapter, header redaction, safe exception mapping, no request dumps | test uses sentinel key and scans stdout/evidence |
| T3-02 | Fixture success is presented as Dify success | High | Separate `fixture-probe`/`cloud-probe`, backend field and immutable status source | CLI tests assert C01-C07 are not `pass` for fixture |
| T3-03 | 4xx/auth/quota errors trigger wasteful retries | High | Retry only connect/timeout once; 401/403/429 map directly to fail/blocked | mocked response tests assert call count |
| T3-04 | Temporary media URL or incomplete DSL is treated as durable evidence | High | Download bytes immediately, hash locally, mark DSL import/export manual until evidence file exists | capability matrix requires evidence path for pass |
| T3-05 | Online probe uploads personal information | High | Accept only fixture case in Phase 1; reject arbitrary input path in cloud CLI | CLI parser has no arbitrary upload argument and tests verify |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Dify adapter seam with safe error mapping</name>
  <files>src/debugmate/adapters/dify.py, tests/test_probe_cli.py</files>
  <read_first>src/debugmate/adapters/base.py, src/debugmate/contracts.py, src/debugmate/settings.py, src/debugmate/evidence.py, .planning/phases/01-foundation-platform-gate/01-RESEARCH.md</read_first>
  <action>Implement `DifyBackend` with injected `httpx.Client` for tests. Add methods matching the protocol: upload to `/files/upload`, run workflow at `/workflows/run` using streaming when configured or blocking for the tiny probe, TTS at `/text-to-audio`, and capability probe orchestration hooks. Create headers only in `_headers()` and return `{"Authorization": "Bearer <secret>"}` only to httpx; never include headers in exceptions or results. Define safe errors `DifyNotConfigured`, `DifyAuthError`, `DifyQuotaError`, `DifyTransportError`, `DifyContractError`. Retry `httpx.ConnectError` and timeout once with no fixed sleep in tests; do not retry 400/401/403/404/429. Validate workflow JSON through `DiagnosisRecord`; audio must start with MP3 ID3/frame bytes or be recorded as contract failure. Mocked tests use sentinel secret `SECRET_SENTINEL_DO_NOT_LOG` and assert it never appears in exception, stdout, caplog or serialized results.</action>
  <verify>.\.venv\Scripts\python.exe -m pytest -q tests/test_probe_cli.py -k "dify or secret or retry"</verify>
  <acceptance_criteria>
    - `dify.py` contains all five safe error classes and `class DifyBackend`.
    - Only `_headers()` creates an `Authorization` value and no public result model has a header field.
    - Mocked 401/403/429 tests make exactly one HTTP call; connect/timeout tests make at most two.
    - `SECRET_SENTINEL_DO_NOT_LOG` is absent from captured output, logs, exceptions and JSON.
  </acceptance_criteria>
  <done>Dify integration is isolated behind the agreed port and fails safely without leaking credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Build the seven-capability probe, CLI and atomic evidence report</name>
  <files>src/debugmate/probe.py, src/debugmate/cli.py, scripts/run_phase1_probe.ps1, platform/dify/capability-matrix.json, tests/test_probe_cli.py</files>
  <read_first>src/debugmate/adapters/fixture.py, src/debugmate/adapters/dify.py, src/debugmate/evidence.py, fixtures/cases/module_not_found/input.json, .planning/phases/01-foundation-platform-gate/01-VALIDATION.md</read_first>
  <action>Define ordered `CAPABILITY_IDS = ("C01","C02","C03","C04","C05","C06","C07")` and strict result status `pass|fail|blocked|not-tested`. Implement `run_fixture_probe(output_root)` creating a new case, validating fixture diagnosis, and publishing a bundle whose cloud-only capabilities are `not-tested`; it may pass only local contract/evidence checks recorded separately. Implement `run_cloud_probe(settings, output_root)` using only the fixed fictional module-not-found fixture; map missing env to all `blocked`, each actual Dify result to evidence-backed status, and require an existing relative evidence path plus SHA-256 before any `pass`. Implement `python -m debugmate.cli fixture-probe --output <path>`, `cloud-probe --output <path>`, `verify-bundle <path>`, and `export-schema --output <path>`. CLI writes final report path and safe status counts as JSON, not secrets. PowerShell script uses `$PSScriptRoot`, `-LiteralPath`, `.venv\Scripts\python.exe`, UTF-8, runs fixture probe always and cloud probe only when `DIFY_API_KEY` exists, then runs offline pytest/Ruff. Version `capability-matrix.json` with all seven IDs, descriptions, status `not-tested`, and empty evidence path.</action>
  <verify>.\.venv\Scripts\python.exe -m pytest -q tests/test_probe_cli.py; .\scripts\run_phase1_probe.ps1</verify>
  <acceptance_criteria>
    - `probe.py` contains the seven ordered IDs and refuses `pass` without evidence path/hash.
    - Fixture probe exits 0 offline, generates `probe-results.json` and `manifest.json`, and reports C01-C07 as `not-tested` rather than cloud success.
    - Missing Dify env makes cloud probe exit with a documented blocked code and seven `blocked` statuses without traceback.
    - PowerShell script contains `$PSScriptRoot`, `-LiteralPath`, `fixture-probe`, and conditional `DIFY_API_KEY` logic.
    - Focused tests and script offline path exit 0.
  </acceptance_criteria>
  <done>A single command produces a truthful, hash-linked Phase 1 capability report without requiring cloud credentials.</done>
</task>

<task type="auto">
  <name>Task 3: Add platform reconstruction docs, schema assets and final Phase 1 checks</name>
  <files>platform/dify/README.md, platform/dify/app.dsl.yml.example, prompts/README.md, contracts/diagnosis-record-v1.schema.json, knowledge/manifest.schema.json, README.md, tests/test_probe_cli.py</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-foundation-platform-gate/01-CONTEXT.md, src/debugmate/contracts.py, src/debugmate/cli.py, platform/dify/capability-matrix.json</read_first>
  <action>Document the exact Dify setup order, required environment variable names, seven capability criteria, 4-hour stop rule, DSL export/import manual evidence requirement, and prohibition on paid credits without approval. Keep `app.dsl.yml.example` as a clearly labeled non-runnable placeholder containing only expected input/output variable names and no key/model ID; actual exported DSL will replace it after human setup. Always export `DiagnosisRecord` JSON Schema via CLI to `contracts/diagnosis-record-v1.schema.json`. Always create `knowledge/manifest.schema.json` as a separate strict JSON Schema for future knowledge-source manifest entries with required fields `title`, `url`, `product`, `version_scope`, `platform`, `retrieved_at`, `sha256`, and `license_or_terms_note`; do not claim that a source manifest has already been populated. `prompts/README.md` records future V1–V4 locations but no fabricated production prompt. Update root README with exact fixture probe command, cloud prerequisites, status semantics, evidence directory, and current limitations. Extend tests to ensure tracked example files contain no secret and capability pass requires evidence. Run full offline tests, Ruff, `git diff --check`, schema export twice with identical hash, fixture probe, and bundle verification.</action>
  <verify>.\.venv\Scripts\python.exe -m pytest -q -m "not cloud"; .\.venv\Scripts\python.exe -m ruff check .; git diff --check; .\.venv\Scripts\python.exe -m debugmate.cli fixture-probe --output .artifacts\phase1-final</verify>
  <acceptance_criteria>
    - `platform/dify/README.md` lists C01-C07, the 4-hour stop rule, DSL re-import evidence, and no-paid-credit rule.
    - Example DSL contains no `api_key`, `authorization`, `token:`, personal path, or real model credential.
    - `contracts/diagnosis-record-v1.schema.json` is deterministically generated from `DiagnosisRecord` and `knowledge/manifest.schema.json` requires the eight declared source fields.
    - Root README contains exact fixture/cloud commands and definitions of `pass`, `fail`, `blocked`, `not-tested`.
    - Full offline pytest, Ruff and `git diff --check` exit 0.
    - Final fixture bundle verifies and remains labeled `backend=fixture`.
  </acceptance_criteria>
  <done>The repository can reconstruct the Phase 1 contract/probe path and clearly separates implemented offline evidence from pending cloud evidence.</done>
</task>

</tasks>

<verification>
- [ ] `.\.venv\Scripts\python.exe -m pytest -q -m "not cloud"` exits 0.
- [ ] `.\.venv\Scripts\python.exe -m ruff check .` exits 0.
- [ ] `.\scripts\run_phase1_probe.ps1` exits 0 without Dify credentials and labels cloud capabilities not-tested/blocked.
- [ ] `git grep -n -I -E "SECRET_SENTINEL_DO_NOT_LOG|Bearer |sk-[A-Za-z0-9_-]{8,}|20795" -- . ':!.planning/**'` returns no tracked product matches.
- [ ] `platform/dify/capability-matrix.json` contains exactly C01-C07 and no capability is pass without a non-empty evidence reference.
</verification>

<success_criteria>
- All tasks and offline validation pass.
- The Dify seam is testable without network and safe under auth/quota/transport failures.
- Fixture and cloud probe statuses cannot be confused.
- Platform configuration, schema and probe criteria are versioned in Git.
- If user credentials are available, the same command records real capability evidence; otherwise the limitation is explicit and auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-platform-gate/01-03-SUMMARY.md` and, when external setup is still required, `01-USER-SETUP.md`.
</output>
