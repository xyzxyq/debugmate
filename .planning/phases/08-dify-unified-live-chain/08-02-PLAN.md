---
phase: 08-dify-unified-live-chain
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/debugmate/knowledge/sync.py
  - src/debugmate/cli.py
  - scripts/build_knowledge.ps1
  - tests/knowledge/test_coverage_sync.py
  - tests/knowledge/test_dify_readback.py
autonomous: true
requirements: [KNOW-03]
must_haves:
  truths:
    - "默认测试和 dry-run 不访问网络，缺少 DIFY_DATASET_API_KEY 只阻塞显式真实同步/readback。"
    - "真实同步只有在封闭计划、完整远端预检和零意外删除后才可执行。"
    - "17 个源完成索引、metadata 写入和分页回读后，数量、hash、配置必须精确一致。"
  artifacts:
    - path: "src/debugmate/knowledge/sync.py"
      provides: "分页 inventory、索引等待、metadata 写入和精确 readback"
      contains: "DifyReadbackManifest"
    - path: "tests/knowledge/test_dify_readback.py"
      provides: "知识 API 对抗 fixture 与 17 源验收"
  key_links:
    - from: "knowledge/sources.json"
      to: "src/debugmate/knowledge/sync.py"
      via: "sealed 17-source build and sync plan"
      pattern: "document_count"
    - from: "src/debugmate/knowledge/sync.py"
      to: "Dify Knowledge API"
      via: "index completion -> metadata update -> paginated readback"
      pattern: "readback"
---

<objective>
把已有 sealed sync 扩展为可审计的 Dify 17 源重建、同步、索引等待、metadata/config/count 精确回读链。

Purpose: 完整关闭 KNOW-03，同时保证无 dataset key 时离线开发与 local_fallback 不被阻塞。
Output: fail-closed Knowledge API adapter、sanitized readback attestation、显式 CLI/PowerShell cloud gate 与测试。
</objective>

<execution_context>
@C:/Users/20795/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20795/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dify-unified-live-chain/08-CONTEXT.md
@.planning/phases/08-dify-unified-live-chain/08-RESEARCH.md
@.planning/phases/08-dify-unified-live-chain/08-VALIDATION.md
@.planning/phases/08-dify-unified-live-chain/08-01-SUMMARY.md
@knowledge/sources.json
@src/debugmate/knowledge/sync.py
@src/debugmate/cli.py

<interfaces>
Existing sealed contract:
```python
def create_sync_plan(build_path: Path, remote_manifest: RemoteManifest | dict[str, object]) -> SyncPlan: ...
def execute_sync(plan: SyncPlan, *, client: httpx.Client, dataset_key: str | None, dataset_id: str | None, confirm_delete: bool, dry_run: bool, ...) -> SyncExecutionResult: ...
```
`DifySyncConfig` is fixed at chunk_size=800, chunk_overlap=120, high_quality, semantic_search, top_k=3, score_threshold=0.5.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement paginated preflight, indexing wait, metadata write and exact readback</name>
  <files>src/debugmate/knowledge/sync.py, tests/knowledge/test_coverage_sync.py, tests/knowledge/test_dify_readback.py</files>
  <read_first>src/debugmate/knowledge/sync.py, src/debugmate/knowledge/build.py, src/debugmate/knowledge/models.py, knowledge/sources.json, tests/knowledge/test_coverage_sync.py, .planning/phases/08-dify-unified-live-chain/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: list-documents pagination must collect the whole inventory and reject duplicate source/document IDs or an unrecognized remote document before mutation.
    - Test 2: create/update batch IDs are polled to completed; timeout/error fails and metadata/readback does not run early.
    - Test 3: required string metadata fields are created/listed and written via the documented batch metadata endpoint, never assumed from create-by-text.
    - Test 4: exact 17-document source/content/source hashes, source metadata, build ID and fixed retrieval config read back successfully.
    - Test 5: count/config/hash/metadata drift, missing page, unexpected delete, raw-ID leakage or absent key fails closed.
  </behavior>
  <action>Per D8-06, keep `create_sync_plan()` sealing/deletion confirmation and extend `sync.py` with explicit Knowledge API functions for paginated document inventory, dataset detail, metadata field list/create, create/update text, bounded indexing-status polling, batch metadata update, paginated document readback and sanitized attestation. Sequence strictly: validate immutable 17-source build -> fetch complete remote inventory -> seal zero-unexpected-delete plan -> create/update -> poll every batch to `completed` -> create/resolve fields -> batch-write `source_id`, `content_sha256`, `source_sha256`, `source_url`, `product`, `version_scope`, `platform`, `retrieved_at`, `knowledge_build_id` -> read dataset/config/documents -> exact compare. Remove unsupported `doc_metadata` from create/update payload. Use a bounded deadline/backoff injectable for tests. Attestation may contain dataset/document fingerprints and response hashes but never raw dataset/document IDs. No method may call the network during dry-run.</action>
  <acceptance_criteria>
    - `sync.py` no longer places `doc_metadata` in the create/update text payload.
    - Tests prove all 17 `knowledge/sources.json` source IDs and hashes are required in readback.
    - Unexpected remote documents require explicit delete confirmation before the first mutating request.
    - Indexing `error`/deadline prevents metadata writes and returns no verified attestation.
    - The successful attestation contains fingerprints/config/build ID and contains no raw `dataset_id` or `document_id` value.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest tests/knowledge/test_coverage_sync.py tests/knowledge/test_dify_readback.py -q</automated>
  </verify>
  <done>The complete Dify Knowledge transaction-like sequence is mock-tested, exact and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: Expose explicit offline and live knowledge gates without implicit network</name>
  <files>src/debugmate/cli.py, scripts/build_knowledge.ps1, tests/knowledge/test_dify_readback.py</files>
  <read_first>src/debugmate/cli.py, scripts/build_knowledge.ps1, pyproject.toml, src/debugmate/settings.py, src/debugmate/knowledge/sync.py, .planning/phases/08-dify-unified-live-chain/08-VALIDATION.md</read_first>
  <action>Make the CLI separation explicit: `knowledge-sync` remains dry-run by default and must not fetch inventory or dispatch HTTP unless `--execute` is passed; real execution requires `DIFY_DATASET_API_KEY`, a dataset binding supplied server-side, and the sealed local build. Add a readback/attestation output option that writes only the sanitized strict object atomically. Update `build_knowledge.ps1` so its default path builds/tests locally and never supplies `--execute`; add an explicit switch for Phase 08 cloud synchronization that fails with a value-free `dataset_key_missing`/`dataset_binding_missing` code before any request. Do not print the key, base URL value, dataset ID, provider body or document IDs. Add CLI tests that poison HTTP in default/dry-run paths and assert no call occurs.</action>
  <acceptance_criteria>
    - Running `python -m debugmate.cli knowledge-sync <build>` without `--execute` cannot call an HTTP transport.
    - Missing `DIFY_DATASET_API_KEY` produces a value-free typed failure only on explicit execute/readback.
    - `scripts/build_knowledge.ps1` has a default offline path and an explicit Phase 08 cloud switch.
    - Attestation output is written atomically and is strict-validated before success.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest tests/knowledge -q</automated>
  </verify>
  <done>Offline work remains credential-free while the explicit cloud command is ready to close KNOW-03 when the dataset key is available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git knowledge build → Dify dataset | Only a sealed 17-source build may authorize remote changes. |
| Dify readback → local attestation | Remote counts, metadata and configuration are untrusted until exact comparison. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-KNOWLEDGE | Tampering/DoS | sync planner/executor | mitigate | Full paginated preflight, sealed plan, explicit delete confirmation and exact post-sync readback tests. |
| T-08-LEAK | Information disclosure | CLI/attestation | mitigate | Fingerprint remote IDs, value-free errors and serialized secret/raw-ID scan. |
| T-08-DUPLICATE | DoS | indexing/write calls | mitigate | One operation per sealed item and bounded status polling; no hidden retry of mutations. |
</threat_model>

<verification>
Run focused tests and `\.venv\Scripts\python.exe -m ruff check src/debugmate/knowledge src/debugmate/cli.py tests/knowledge`. Then run the default `knowledge-sync` with an HTTP-poison fixture and confirm zero network calls. Missing `DIFY_DATASET_API_KEY` is an expected final-cloud blocker, not an offline implementation failure.
</verification>

<success_criteria>
- The entire 17-source sync/readback algorithm is implemented and tested offline.
- Real mutation is explicit and cannot proceed with an incomplete key/binding or unexpected delete.
- No key or raw remote identifier is exposed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dify-unified-live-chain/08-02-SUMMARY.md`.
</output>
