---
phase: 08-dify-unified-live-chain
plan: 06
type: execute
wave: 4
depends_on: [08-05]
files_modified:
  - src/debugmate/ui/serve.py
  - src/debugmate/ui/app.py
  - src/debugmate/ui/presentation.py
  - tests/ui/test_dify_live.py
  - tests/ui/test_app.py
  - tests/ui/test_view_state.py
  - tests/ui/test_real_input.py
  - tests/results/test_result_e2e.py
  - tests/results/test_tts_chain.py
autonomous: true
requirements: [MULTI-03, UX-01]
must_haves:
  truths:
    - "启动时只依据完整、严格、本地可验证配置选择 dify 或 local_fallback，构造过程零网络；replay 始终独立离线。"
    - "第一次 outbound request 只能发生在当前 preview 被消费和确认之后；确认文案明确发送脱敏 payload 且可能消耗额度。"
    - "页面明确显示 dify/local_fallback/replay 和粗粒度真实阶段，不做视觉重设计、不虚构百分比或节点完成。"
    - "有效 Dify diagnosis 复用现有 Markdown/PNG/MP3/ZIP；无效 diagnosis 不暴露制品，媒体后段失败保留 partial。"
  artifacts:
    - path: "src/debugmate/ui/serve.py"
      provides: "no-I/O Dify vs local fallback dependency construction"
      contains: "DifyLiveWorkflow"
    - path: "src/debugmate/ui/app.py"
      provides: "Phase 7 UI contract with backend truth and live stages"
      contains: "Dify 工作流运行中"
    - path: "tests/ui/test_dify_live.py"
      provides: "backend, consent, stale-session and no-fake-artifact regression"
  key_links:
    - from: "src/debugmate/ui/app.py"
      to: "src/debugmate/cloud/workflow.py"
      via: "consume_current -> approval -> service only"
      pattern: "consume_current"
    - from: "src/debugmate/cloud/workflow.py"
      to: "existing result composer"
      via: "strict DiagnosisRunOutcome execution_backend=dify"
      pattern: "execution_backend"
---

<objective>
把配置好的 Dify live workflow 接入 Phase 7 的批准 callback 和 Phase 4 的既有三模态结果页，同时保持 fallback/replay/backend/failure 真相。

Purpose: 用户在不接触 key/backend selector、也不丢失原 UI 合同的情况下完成真实 live 诊断。
Output: no-I/O dependency construction、最小 UI 状态/文案接线和全链离线回归。
</objective>

<execution_context>
@C:/Users/20795/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20795/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dify-unified-live-chain/08-CONTEXT.md
@.planning/phases/08-dify-unified-live-chain/08-RESEARCH.md
@.planning/phases/07-real-input-privacy-ui/07-VERIFICATION.md
@.planning/phases/08-dify-unified-live-chain/08-05-SUMMARY.md
@src/debugmate/ui/serve.py
@src/debugmate/ui/app.py
@src/debugmate/results/service.py

<interfaces>
Phase 7 locked callback order:
```text
input edit -> invalidate authority
preview -> opaque one-time token
confirm -> consume_current -> approve_preview -> diagnose_events(approved)
```
Stable result surfaces (redacted input, six extraction fields, citations, report, PNG, audio, ZIP) must remain in the same existing UI contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Select configured Dify or honest local fallback with zero construction I/O</name>
  <files>src/debugmate/ui/serve.py, tests/ui/test_dify_live.py, tests/ui/test_app.py</files>
  <read_first>src/debugmate/ui/serve.py, src/debugmate/settings.py, src/debugmate/cloud/workflow.py, src/debugmate/knowledge/sync.py, src/debugmate/results/service.py, tests/ui/test_app.py, .planning/phases/08-dify-unified-live-chain/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: complete local app configuration plus verified knowledge attestation constructs Dify dependencies without an HTTP call.
    - Test 2: incomplete/invalid app config constructs the existing local chain with execution_backend local_fallback and a value-free reason.
    - Test 3: absence of DIFY_DATASET_API_KEY alone does not break offline construction or a runtime that already has a valid readback attestation; it blocks only sync/final acceptance.
    - Test 4: once Dify workflow starts, auth/quota/transport/contract failures remain Dify failures and never invoke the local service.
    - Test 5: replay remains allowlisted/offline and execution_backend replay.
  </behavior>
  <action>Refactor `serve.py` dependency assembly into a strict no-I/O factory. Read environment values server-side through `DebugMateSettings`; construct `DifyLiveWorkflow` only when application origin/key/user, current DSL/prompt/schema hashes, configured dataset fingerprint and a locally verified readback attestation for the expected build are complete. Do not require the dataset API key for ordinary offline work or for runtime use of an already verified attestation; its absence is reserved for KNOW-03/final live gate. Otherwise construct the existing local workflow/composer and label it `local_fallback` with a safe reason code. Preserve replay as separate offline `replay`. Constructors must not call app parameters, upload, workflow, TTS, dataset or socket APIs. Do not add a UI backend/key selector. Once a Dify service is selected and starts, never catch it into local success.</action>
  <acceptance_criteria>
    - A poisoned HTTP/socket test proves Dify and fallback construction perform zero outbound calls.
    - `serve.py` can construct exactly `dify` or `local_fallback` for live and `replay` for replay.
    - Missing dataset key does not fail default offline tests or local_fallback construction.
    - A Dify auth/quota/timeout test asserts the local workflow/composer was not invoked.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest tests/ui/test_dify_live.py tests/ui/test_app.py -q</automated>
  </verify>
  <done>Backend selection is local, deterministic and truthful, with no construction-time network or post-dispatch silent fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire consent, coarse live stages and backend truth into the existing Phase 7 page</name>
  <files>src/debugmate/ui/app.py, src/debugmate/ui/presentation.py, tests/ui/test_dify_live.py, tests/ui/test_view_state.py, tests/ui/test_real_input.py</files>
  <read_first>src/debugmate/ui/app.py, src/debugmate/ui/presentation.py, src/debugmate/ui/local_live.py, src/debugmate/results/contracts.py, tests/ui/test_real_input.py, .planning/phases/07-real-input-privacy-ui/07-VERIFICATION.md, .planning/phases/08-dify-unified-live-chain/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: confirmation copy states the redacted payload goes to configured Dify and may consume quota only when backend is dify.
    - Test 2: outbound adapter is unreachable before current token consume; stale/edited/duplicate token remains blocked.
    - Test 3: coarse stages are local preview, optional upload, Dify running, local validation, then existing result stages; no percentage/speculative node completion.
    - Test 4: input/replay/duplicate actions are disabled during blocking live run and no cancel claim/control is shown.
    - Test 5: failed invalid cloud diagnosis shows backend/stage/safe retry guidance and no report/PNG/audio/ZIP.
  </behavior>
  <action>Keep the Phase 7 two-region student workbench, stable component IDs, input fields, preview contract, result surfaces, CSS tokens and responsive behavior; make no visual redesign. Extend the self-contained presentation/view mapping to render the explicit execution backend (`Dify 实时诊断`, `本地降级`, `固定回放`) and safe typed failure stage. For Dify confirmation only, say that the redacted payload will be sent to the configured Dify workflow and may consume quota. Preserve callback order `consume_current -> approve -> diagnose`; no network path may be reachable earlier. Emit only truthful coarse statuses: `本地脱敏预览`, optional `上传脱敏截图`, `Dify 工作流运行中`, `本地严格校验`, then existing presentation/report/card/audio/publish stages. Do not show percentages, raw IDs, provider messages, API details or vision/retrieval completion before validated trace. Disable inputs/replay/duplicate actions while blocking; do not add Stop/Cancel or imply page close cancels Dify. Keep stale-session lease checks before terminal view publication.</action>
  <acceptance_criteria>
    - UI source contains `Dify 工作流运行中` and contains no progress percentage or blocking-run cancellation claim.
    - Backend labels come from `execution_backend`, not artifact/audio/filename inference.
    - Tests assert zero network before one-time consume and disabled actions during a blocking run.
    - Invalid diagnosis failed state exposes no report/card/audio/download component value.
    - Existing stable Phase 7 IDs and four-input/preview tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest tests/ui/test_dify_live.py tests/ui/test_view_state.py tests/ui/test_real_input.py tests/ui/test_app.py -q</automated>
  </verify>
  <done>The existing page truthfully represents consent, backend, blocking progress and failure without redesign or leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove verified Dify outcomes reuse the existing MP3 and ZIP result chain</name>
  <files>tests/results/test_result_e2e.py, tests/results/test_tts_chain.py, tests/ui/test_dify_live.py</files>
  <read_first>src/debugmate/results/service.py, src/debugmate/results/audio.py, src/debugmate/results/publisher.py, tests/results/test_result_e2e.py, tests/results/test_tts_chain.py, tests/ui/test_dify_live.py, .planning/phases/08-dify-unified-live-chain/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: one strict mocked Dify outcome generates report, PNG, 30–60 second recap/MP3 contract and full ZIP from the same identity.
    - Test 2: execution backend dify remains dify when audio backend falls through to edge_tts or sapi.
    - Test 3: valid diagnosis plus TTS failure yields existing audio-partial result/ZIP; invalid diagnosis yields no media/ZIP.
    - Test 4: view and ZIP manifest carry matching case, diagnosis, schema, generation, backend and artifact hashes.
  </behavior>
  <action>Add integration fixtures/tests only; do not create another renderer or TTS pipeline. Feed a strict accepted `DiagnosisRunOutcome(execution_backend='dify')` through `ResultApplicationService` and the existing report/card/recap/TTS/publisher chain. Assert recap text is derived from the same diagnosis, playable MP3 validation remains delegated to the existing chain, and full/partial ZIP semantics do not change. Verify backend truth stays orthogonal to Dify/Edge/SAPI audio backend and that every view/manifest/download identity matches. Keep real Dify/TTS/Edge calls marker-isolated for Plan 07.</action>
  <acceptance_criteria>
    - No production renderer, card layout, recap contract or TTS fallback order changes in this task.
    - Mocked Dify complete path verifies Markdown, PNG, MP3 and ZIP hashes/identity.
    - Dify + SAPI fallback fixture reports execution backend dify and audio backend sapi.
    - Invalid Dify diagnosis fixture produces zero artifact/download paths.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest tests/results/test_result_e2e.py tests/results/test_tts_chain.py tests/ui/test_dify_live.py -q</automated>
  </verify>
  <done>The existing MULTI-03/UX-01 result surfaces work with strict Dify outcomes without media duplication or visual change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Server config → selected service | UI must not choose or spoof backend/key state. |
| Browser preview token → outbound Dify | Stale or duplicate browser state is untrusted. |
| Service terminal state → DOM/download | Failed diagnosis must not expose stale/fabricated artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-RACE | Tampering | UI callbacks | mitigate | Existing revision lease/atomic consume plus terminal current-lease check; stale tests. |
| T-08-LEAK | Information disclosure | UI/view/download | mitigate | Backend enum and safe codes only; DOM/fixture secret/raw-ID scans. |
| T-08-DUPLICATE | DoS | blocking actions | mitigate | Disable duplicate/replay/input actions and no fake cancellation; interaction tests. |
| T-08-INJECTION | Elevation | displayed provider data | mitigate | Only strict accepted local projections reach components; invalid-envelope no-artifact tests. |
</threat_model>

<verification>
Run all task commands, `\.venv\Scripts\python.exe -m pytest tests/privacy tests/diagnosis tests/results tests/ui -q`, `\.venv\Scripts\python.exe -m pytest -q`, and `\.venv\Scripts\python.exe -m ruff check src tests`. Default pytest must select no cloud/browser/tts tests and make no network call. Confirm UI CSS/layout and frozen Phase 10 assets are unchanged.
</verification>

<success_criteria>
- Configured live, local fallback and replay are explicit and non-confusable.
- The Phase 7 approval boundary is still the sole outbound authorization.
- One strict Dify outcome reaches all existing Phase 4 outputs, including MP3/ZIP.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dify-unified-live-chain/08-06-SUMMARY.md`.
</output>
