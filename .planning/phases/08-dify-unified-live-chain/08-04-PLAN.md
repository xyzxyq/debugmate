---
phase: 08-dify-unified-live-chain
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/debugmate/diagnosis/workflow.py
  - src/debugmate/results/contracts.py
  - src/debugmate/results/publisher.py
  - src/debugmate/results/loader.py
  - src/debugmate/results/service.py
  - tests/diagnosis/test_workflow.py
  - tests/results/test_contracts.py
  - tests/results/test_publisher.py
  - tests/results/test_loader.py
  - tests/results/test_service.py
  - tests/results/test_backend_provenance.py
autonomous: true
requirements: [UX-01, EVID-01]
must_haves:
  truths:
    - "ResultMode 仍只有 live|replay，execution backend 作为独立严格字段贯穿 outcome、manifest、loader、service 和 view state。"
    - "live 结果明确为 dify 或 local_fallback，replay 明确为 replay；不得从文件名、媒体 backend 或产物存在性推断。"
    - "失败结果不暴露制品，后续媒体失败继续保持 Phase 4 已验证的 partial 语义。"
  artifacts:
    - path: "src/debugmate/results/contracts.py"
      provides: "backend-aware ResultManifest and ResultViewState"
      contains: "execution_backend"
    - path: "tests/results/test_backend_provenance.py"
      provides: "backend orthogonality and migration regression"
  key_links:
    - from: "src/debugmate/diagnosis/workflow.py"
      to: "src/debugmate/results/contracts.py"
      via: "explicit execution_backend copied, never inferred"
      pattern: "execution_backend"
    - from: "src/debugmate/results/publisher.py"
      to: "src/debugmate/results/loader.py"
      via: "self-contained manifest backend verification"
      pattern: "execution_backend"
---

<objective>
在不改变 `ResultMode`、音频 backend 或 Phase 4 视觉/媒体合同的前提下，加入端到端 execution-backend truth。

Purpose: 防止 UI 和证据把 local fallback、Dify diagnosis、replay 或 TTS fallback 混为一谈。
Output: 严格 backend provenance 字段、manifest/loader/service 迁移和完整回归测试。
</objective>

<execution_context>
@C:/Users/20795/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20795/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dify-unified-live-chain/08-CONTEXT.md
@.planning/phases/08-dify-unified-live-chain/08-RESEARCH.md
@.planning/phases/08-dify-unified-live-chain/08-01-SUMMARY.md
@src/debugmate/diagnosis/workflow.py
@src/debugmate/results/contracts.py
@src/debugmate/results/service.py

<interfaces>
Existing contracts:
```python
class ResultMode(StrEnum):
    LIVE = "live"
    REPLAY = "replay"

class DiagnosisRunOutcome(StrictFrozenModel):
    backend: str
    # ...strict diagnosis/run identity...
```
New orthogonal contract from Plan 01: `ExecutionBackend = dify|local_fallback|replay`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict execution backend to diagnosis and result contracts</name>
  <files>src/debugmate/diagnosis/workflow.py, src/debugmate/results/contracts.py, tests/diagnosis/test_workflow.py, tests/results/test_contracts.py, tests/results/test_backend_provenance.py</files>
  <read_first>src/debugmate/cloud/contracts.py, src/debugmate/diagnosis/workflow.py, src/debugmate/results/contracts.py, tests/diagnosis/test_workflow.py, tests/results/test_contracts.py, .planning/phases/08-dify-unified-live-chain/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: DiagnosisRunOutcome requires execution_backend in dify|local_fallback|replay while retaining provider/generator backend separately.
    - Test 2: ResultMode remains exactly live|replay and cannot encode local fallback.
    - Test 3: live accepts dify/local_fallback, replay requires replay; invalid cross-products fail.
    - Test 4: audio backend/fallback can differ from execution backend without changing it.
    - Test 5: running, completed, partial and failed view states carry the explicit backend.
  </behavior>
  <action>Per D8-13, import/reuse Plan 01 `ExecutionBackend` and add required `execution_backend` to `DiagnosisRunOutcome`, `ResultManifest` and `ResultViewState`. Preserve current `backend` on diagnosis as provider/generator identity and preserve `AudioResult.backend`/attempts as TTS identity; do not rename them into execution truth. Add exact cross-field validators: `ResultMode.LIVE` permits only `dify|local_fallback`; `ResultMode.REPLAY` requires `replay`; replay fixture rules remain unchanged. Update constructors/fixtures in the named tests with explicit values and add negative tests for inference from filename, artifact availability or audio backend. If manifest version must change, bump it once and migrate all readers/writers in Task 2; do not accept both ambiguous shapes.</action>
  <acceptance_criteria>
    - `ResultMode` source still defines only `LIVE='live'` and `REPLAY='replay'`.
    - `DiagnosisRunOutcome`, `ResultManifest` and `ResultViewState` each contain a required `execution_backend` field.
    - Tests prove a Dify diagnosis with SAPI audio fallback remains `execution_backend='dify'`.
    - Tests reject replay+dify and live+replay combinations.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest tests/diagnosis/test_workflow.py tests/results/test_contracts.py tests/results/test_backend_provenance.py -q</automated>
  </verify>
  <done>The backend contract is explicit and orthogonal before any UI/service consumer is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate backend truth through publisher, loader and service without media regression</name>
  <files>src/debugmate/results/publisher.py, src/debugmate/results/loader.py, src/debugmate/results/service.py, tests/results/test_publisher.py, tests/results/test_loader.py, tests/results/test_service.py, tests/results/test_backend_provenance.py</files>
  <read_first>src/debugmate/results/contracts.py, src/debugmate/results/publisher.py, src/debugmate/results/loader.py, src/debugmate/results/service.py, src/debugmate/results/audio.py, tests/results/test_service.py, .planning/phases/04-multimodal-results-ui/04-12-SUMMARY.md</read_first>
  <behavior>
    - Test 1: publisher copies execution backend into result identity/manifest and result ID derivation, preventing cross-backend cache reuse.
    - Test 2: loader verifies backend from the signed/self-contained manifest and never derives it from outcome.backend or files.
    - Test 3: service preserves explicit backend across running, completed, partial and safe failed states.
    - Test 4: existing report/PNG/MP3/ZIP complete and card/audio partial-result paths remain unchanged.
    - Test 5: replay/local/Dify outcomes cannot overwrite or reuse each other's immutable result.
  </behavior>
  <action>Thread the required `execution_backend` argument through `ResultBundlePublisher`, `_result_id`, `_manifest`, `publish_result_bundle`, verified loader output, `ResultApplicationService` event/view creation and all affected fixtures. Include backend in canonical result identity/cache discrimination so identical diagnosis/media under different execution backends cannot be mislabeled or reused. Remove any branch that derives cloud/local truth from `DiagnosisRunOutcome.backend`, mode, filename, artifact presence or audio backend. For pre-diagnosis failures, produce a self-contained failed view with the selected execution backend and no artifacts; retain existing Phase 4 rules for valid diagnosis followed by card/TTS failure. Update manifest version exactly once if required and reject stale ambiguous manifests rather than guessing.</action>
  <acceptance_criteria>
    - `publisher.py`, `loader.py` and `service.py` pass `execution_backend` explicitly at every ResultManifest/ResultViewState construction.
    - No backend decision in these files tests filenames, artifact presence or audio backend.
    - Backend provenance tests cover complete, card-partial, audio-partial and failed states.
    - Existing media/service/ZIP regression tests pass without visual or renderer changes.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest tests/results/test_backend_provenance.py tests/results/test_publisher.py tests/results/test_loader.py tests/results/test_service.py tests/results/test_media.py tests/results/test_tts_chain.py -q</automated>
  </verify>
  <done>Every verified result and view carries a non-inferred backend while existing multimodal/partial semantics remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Diagnosis outcome → result publication | Backend metadata controls user-visible truth and cache identity. |
| Result manifest → loader/view | A stale or tampered manifest must not relabel a result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-RACE | Tampering | service/cache/result store | mitigate | Backend included in immutable identities and no cross-backend overwrite/reuse; tests. |
| T-08-LEAK | Information disclosure | view/manifest | mitigate | Only strict backend enum is exposed; provider IDs/bodies remain absent. |
| T-08-CITATION | Spoofing | UI/result truth | mitigate | Explicit backend field, never artifact/audio inference; negative provenance tests. |
</threat_model>

<verification>
Run both task commands, `\.venv\Scripts\python.exe -m ruff check src/debugmate/diagnosis/workflow.py src/debugmate/results tests/results`, and the default offline suite. Inspect the diff to confirm no renderer CSS/layout, PPTX, MP4, SRT or final screenshot changed.
</verification>

<success_criteria>
- Backend truth is strict, end-to-end and cache-safe.
- Phase 4 multimodal and Phase 7 state contracts remain compatible.
- Failed cloud states cannot expose fabricated artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dify-unified-live-chain/08-04-SUMMARY.md`.
</output>
