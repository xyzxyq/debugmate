---
phase: 08-dify-unified-live-chain
plan: 07
type: execute
wave: 5
depends_on: [08-02, 08-03, 08-05, 08-06]
files_modified:
  - platform/dify/app.dsl.yml
  - platform/dify/README.md
  - tests/fixtures/cloud/phase8-live-case.json
  - tests/fixtures/phase8/terminal-module-not-found-redacted.png
  - tests/cloud/test_dify_live_cloud.py
  - tests/ui/test_phase8_live_browser.py
  - scripts/run-phase8-live-qa.ps1
  - scripts/verify-phase8-security-scope.ps1
  - evidence/dify-live/phase8/manifest.json
  - evidence/dify-live/phase8/knowledge-readback.json
  - evidence/dify-live/phase8/live-run.json
  - evidence/dify-live/phase8/checksums.sha256
autonomous: true
requirements: [KNOW-03, KNOW-04, DIAG-02, MULTI-03, UX-01, EVID-01]
user_setup:
  - service: dify
    why: "Run the one real Phase 08 application and 17-source knowledge acceptance gate."
    env_vars:
      - name: DIFY_API_KEY
        source: "Published DebugMate workflow application API key"
      - name: DIFY_DATASET_API_KEY
        source: "Dify Knowledge API dataset key; absence blocks KNOW-03/final acceptance only"
      - name: DIFY_BASE_URL
        source: "Exact approved HTTPS Dify API origin"
      - name: DIFY_USER
        source: "Stable non-secret DebugMate API user value"
    dashboard_config:
      - task: "Import/publish the current platform/dify/app.dsl.yml only if the Application API cannot perform the required publish/import action"
        location: "Dify Studio workflow application"
must_haves:
  truths:
    - "当前 17 源 build 已真实同步并精确 readback，任何 unexpected delete 或配置/hash 漂移均阻止验收。"
    - "当前 published DSL 的 singular image_input、direct retrieval trace 和 same-run strict diagnosis 已真实 smoke。"
    - "一个 synthetic-but-real 脱敏案例通过真实 Edge 页面生成 Markdown、PNG、可播放 MP3 和可重开 ZIP，零 skip。"
    - "正式 evidence 只含安全投影/指纹/hash/not_reported，不含 key、approval、raw remote ID/body 或未脱敏输入。"
    - "PPTX、MP4、SRT、课程最终截图和 Phase 9 对照资产保持冻结。"
  artifacts:
    - path: "scripts/run-phase8-live-qa.ps1"
      provides: "explicit cloud + Edge zero-skip owned-loopback acceptance runner"
      contains: "Assert-JUnitZeroIssues"
    - path: "evidence/dify-live/phase8/manifest.json"
      provides: "sanitized atomic Phase 08 acceptance manifest"
    - path: "evidence/dify-live/phase8/knowledge-readback.json"
      provides: "exact 17-source sanitized attestation"
    - path: "tests/fixtures/phase8/terminal-module-not-found-redacted.png"
      provides: "committed synthetic redacted PNG required by the real upload gate"
  key_links:
    - from: "tests/fixtures/cloud/phase8-live-case.json"
      to: "tests/fixtures/phase8/terminal-module-not-found-redacted.png"
      via: "fixture screenshot_path plus exact lowercase screenshot_sha256"
      pattern: "screenshot_sha256"
    - from: "tests/fixtures/phase8/terminal-module-not-found-redacted.png"
      to: "Dify workflow"
      via: "real Phase 7 preview/approval and mandatory singular image upload"
      pattern: "upload_file_id"
    - from: "Dify run envelope"
      to: "evidence/dify-live/phase8/live-run.json"
      via: "strict allowlisted local validation before promotion"
      pattern: "workflow_run_fingerprint"
    - from: "evidence/dify-live/phase8/live-run.json"
      to: "Markdown/PNG/MP3/ZIP"
      via: "same local result identity and checksums"
      pattern: "artifact_sha256"
---

<objective>
以当前真实 Dify dataset/workflow 和 Microsoft Edge 完成 Phase 08 的零跳过最终验收，并只发布可复核、无秘密的 evidence bundle。

Purpose: 把离线合同证明提升为一条真实、同次、可追溯的 Dify→严格诊断→三模态 UI/ZIP 课程工程证据。
Output: fresh DSL/readback、显式 cloud/Edge runners、sanitized Phase 08 evidence 与安全/冻结范围门禁。
</objective>

<execution_context>
@C:/Users/20795/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20795/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dify-unified-live-chain/08-CONTEXT.md
@.planning/phases/08-dify-unified-live-chain/08-RESEARCH.md
@.planning/phases/08-dify-unified-live-chain/08-VALIDATION.md
@.planning/phases/08-dify-unified-live-chain/08-02-SUMMARY.md
@.planning/phases/08-dify-unified-live-chain/08-03-SUMMARY.md
@.planning/phases/08-dify-unified-live-chain/08-05-SUMMARY.md
@.planning/phases/08-dify-unified-live-chain/08-06-SUMMARY.md
@platform/dify/app.dsl.yml
@scripts/run-phase7-real-input-qa.ps1

<interfaces>
Explicit live commands must use markers:
```powershell
.\.venv\Scripts\python.exe -m pytest -q -m cloud tests/cloud/test_dify_live_cloud.py
.\.venv\Scripts\python.exe -m pytest -q -m browser tests/ui/test_phase8_live_browser.py
```
Default `pytest -q` continues to exclude cloud/network/browser/tts/ocr.
</interfaces>
</context>

<decision_coverage>
Full-fidelity locked-decision map (no partial coverage): D8-01/D8-02/D8-03 → 08-06 Tasks 1–2; D8-04/D8-05/D8-07/D8-10/D8-16 → 08-03 Tasks 1–3; D8-06 → 08-02 Tasks 1–2 and 08-07 Task 1; D8-08/D8-09/D8-11 → 08-05 Tasks 1–2; D8-12/D8-13/D8-14/D8-15 → 08-04 Tasks 1–2 and 08-06 Tasks 1–2; D8-17/D8-18/D8-19 → 08-07 Tasks 1–3; D8-20 → 08-01 Task 1. Every mapping is Full. Phase 9 representative comparisons and every Phase 10 media/final-screenshot item remain excluded.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Perform current 17-source readback and published-app contract smoke</name>
  <files>platform/dify/app.dsl.yml, platform/dify/README.md, tests/fixtures/cloud/phase8-live-case.json, tests/fixtures/phase8/terminal-module-not-found-redacted.png, tests/cloud/test_dify_live_cloud.py, evidence/dify-live/phase8/knowledge-readback.json</files>
  <read_first>platform/dify/app.dsl.yml, platform/dify/README.md, knowledge/sources.json, tests/fixtures/cloud/phase8-live-case.json, tests/fixtures/phase8/terminal-module-not-found-redacted.png, src/debugmate/knowledge/sync.py, src/debugmate/adapters/dify.py, tests/diagnosis/test_dify_diagnosis_cloud.py, .planning/phases/08-dify-unified-live-chain/08-CONTEXT.md</read_first>
  <action>Create the committed synthetic, already-redacted PNG `tests/fixtures/phase8/terminal-module-not-found-redacted.png` with no real username/path/token/approval material. Bind it from `tests/fixtures/cloud/phase8-live-case.json` using repository-relative `screenshot_path` and the exact lowercase `screenshot_sha256`; tests must reject a mismatch. Under explicit cloud marker and value-free env readiness checks, rebuild all 17 Git sources, fetch complete remote inventory, refuse unexpected deletes, execute the sealed sync, wait for indexing, write metadata and save only the strict sanitized `knowledge-readback.json`. Query current app parameters, require upload of this committed PNG with verified `image/png`, and run the exact singular `image_input`; the Phase 08 real cloud and Edge acceptance path must fail if upload is absent and must not substitute a text-only run. Validate current direct retrieval result keys, update only the sanitizer allowlist if required, export the resulting published DSL fresh, and verify DSL hash/dataset fingerprint/prompt/schema/build bindings. If API cannot perform import/publish/export, pause only for the unavoidable Dify console action, then resume automation. Usage/cost is numeric only if reported; otherwise literal `not_reported`. Never print/store keys, base URL value, raw dataset/document/upload/run IDs or provider body.</action>
  <acceptance_criteria>
    - `knowledge-readback.json` proves exactly 17 unique source IDs with matching content/source hashes, metadata, build and retrieval config.
    - Cloud test is marked `cloud`, fails rather than skips when invoked by the Phase 08 runner, and is absent from default pytest.
    - Current app accepts the authoritative singular `image_input` and returns direct trace plus strict diagnosis in one run envelope.
    - The cloud/Edge gate verifies the committed PNG SHA-256, performs one real image upload, and fails rather than accepting a text-only substitute.
    - Fresh `app.dsl.yml` passes static tests and contains no secret/raw remote identifier.
    - README records the current product-chain evidence separately from historical C01-C07 capability evidence.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q -m cloud tests/cloud/test_dify_live_cloud.py</automated>
  </verify>
  <done>KNOW-03 and the current Application API shape are proven against the configured real Dify resources, or the task reports the missing dataset key as the precise blocker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build a zero-skip owned-loopback Edge runner and security/frozen-scope gate</name>
  <files>tests/ui/test_phase8_live_browser.py, scripts/run-phase8-live-qa.ps1, scripts/verify-phase8-security-scope.ps1</files>
  <read_first>scripts/run-phase7-real-input-qa.ps1, scripts/verify-phase7-security-scope.ps1, tests/ui/test_browser.py, tests/ui/test_qa_scenarios.py, src/debugmate/ui/app.py, .planning/phases/08-dify-unified-live-chain/08-VALIDATION.md</read_first>
  <behavior>
    - Test 1: runner owns loopback server lifetime, requires explicit cloud/browser readiness and rejects skipped/error/failure JUnit results.
    - Test 2: Edge follows real four-field input -> redacted preview -> confirmation -> Dify backend -> result/download flow.
    - Test 3: runner verifies backend dify, same identities, MP3 with FFprobe, ZIP reopen/checksums and safe evidence allowlist.
    - Test 4: injected key/raw ID/provider body/personal path, stale evidence or changed frozen asset blocks promotion.
    - Test 5: runner promotes one staged evidence directory atomically only after every gate passes.
  </behavior>
  <action>Adapt the Phase 7 owned-loopback/JUnit/atomic-promotion pattern into `run-phase8-live-qa.ps1`; do not reuse or overwrite Phase 7/course evidence. Require explicit env readiness, run offline preflight, explicit cloud smoke, then Microsoft Edge browser test with zero skip. Browser automation must exercise the real approval callback and supported downloads; verify backend `dify`, coarse state truth, report/PNG/audio/ZIP presence, FFprobe MP3 validity, ZIP reopen/member/checksum/manifest equality and no stale-session overwrite. Create `verify-phase8-security-scope.ps1` to scan source/evidence/DOM/download/ZIP for forbidden key values, approval fields, raw remote IDs/provider bodies and personal paths, and to assert frozen Phase 10 targets (PPTX, MP4, SRT, course final screenshots/media manifests) have exact baseline hashes/no new files. Use `-LiteralPath`, exact resolved roots and stop on every PowerShell error. Stage evidence under a new runtime directory and atomically promote only after all gates pass.</action>
  <acceptance_criteria>
    - Runner invokes cloud and Edge tests with explicit markers and rejects any skipped JUnit testcase.
    - Runner uses an owned `127.0.0.1` process and always cleans it up without claiming Dify cancellation.
    - FFprobe, ZIP reopen/checksums, backend identity, secret/raw-ID scan and frozen-media checks are mandatory before promotion.
    - Formal output path is only `evidence/dify-live/phase8`; Phase 7/C01-C07/course evidence is read-only.
    - Injection tests prove evidence promotion is atomic and fail-closed.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest tests/ui/test_phase8_live_browser.py tests/platform/test_dify_live_evidence.py -q -m "not browser"</automated>
  </verify>
  <done>A deterministic runner can produce formal Phase 08 evidence only from a zero-skip real cloud+Edge run.</done>
</task>

<task type="auto">
  <name>Task 3: Run and atomically publish the one real Dify-to-MP3/ZIP evidence bundle</name>
  <files>evidence/dify-live/phase8/manifest.json, evidence/dify-live/phase8/live-run.json, evidence/dify-live/phase8/checksums.sha256, platform/dify/README.md</files>
  <read_first>scripts/run-phase8-live-qa.ps1, scripts/verify-phase8-security-scope.ps1, evidence/dify-live/phase8/knowledge-readback.json, platform/dify/app.dsl.yml, .planning/phases/08-dify-unified-live-chain/08-CONTEXT.md</read_first>
  <action>Run `scripts/run-phase8-live-qa.ps1` against the current published workflow and the committed synthetic redacted case. Require zero skipped cloud/Edge tests. Promote only safe projections: request/redaction/upload hashes; receipt and upload/workflow/repair attempt statuses; workflow/node fingerprints; DSL/prompt/schema/knowledge/model hashes; sanitized extraction facts/direct trace; accepted strict diagnosis; local run/result/artifact identities; reported numeric usage or `not_reported`; exact Markdown/PNG/MP3/ZIP SHA-256. The stable `manifest.json` must bind one QA run identity, evidence time, source commit/worktree scope and every inner hash; `checksums.sha256` must verify byte-for-byte. Run FFprobe and reopen ZIP through the product verifier. Update README with commands and evidence paths only after promotion. Do not create or refresh representative-case matrices, V1-V4 comparison, final screenshots, PPTX, narration, SRT or MP4.</action>
  <acceptance_criteria>
    - Phase 08 cloud JUnit and Edge JUnit both report zero skipped/failures/errors.
    - Formal manifest binds the exact 17-source attestation, DSL/prompt/schema/build, strict diagnosis, backend dify and all four local output hashes.
    - MP3 passes FFprobe and ZIP passes supported reopen/member/checksum validation.
    - Security/scope script reports zero secret/raw-ID/provider-body/personal-path findings and frozen targets unchanged.
    - `live-run.json` contains no raw input, approval ID/token/signature, key, base URL, dataset/document/upload/workflow ID or provider body.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-phase8-live-qa.ps1</automated>
  </verify>
  <done>One current real Dify+Edge chain is reproducibly evidenced end-to-end; all six Phase 08 requirements have direct current proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local QA runner → real Dify services | Keys and remote IDs must remain transient while real side effects are authorized explicitly. |
| Real browser/downloads → formal evidence | DOM/files/ZIP may contain stale or leaked data until scanned and identity-checked. |
| Staging → tracked evidence | Only a complete zero-skip run may be atomically promoted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-ORIGIN | Spoofing/Information disclosure | real app/knowledge calls | mitigate | Exact approved HTTPS origin and no redirects; cloud preflight. |
| T-08-KNOWLEDGE | Tampering/DoS | 17-source sync | mitigate | Sealed plan, no unexpected delete, completed indexing and exact readback. |
| T-08-LEAK | Information disclosure | DOM/evidence/ZIP | mitigate | Value-free env probes, allowlist projection and recursive secret/raw-ID scan. |
| T-08-RACE | Tampering | browser/formal promotion | mitigate | Current lease, one QA run ID, staging and atomic promotion. |
| T-08-DUPLICATE | DoS | cloud runner | mitigate | One approved case; receipt and ambiguity rules; runner never retries ambiguous workflow. |
| T-08-INJECTION | Elevation | live model output | mitigate | Strict local validation and command-safety/security gates before media/evidence. |
</threat_model>

<verification>
Before the real runner, execute `\.venv\Scripts\python.exe -m pytest -q` and Ruff; neither may make an implicit live call. After promotion, rerun `scripts/verify-phase8-security-scope.ps1`, product result/ZIP verifier, exact checksums, FFprobe and frontmatter evidence inventory. Missing `DIFY_DATASET_API_KEY` or app-ready configuration is a reported external blocker for this plan only; it does not invalidate Plans 01–06.
</verification>

<success_criteria>
- KNOW-03 is closed by a current exact 17-source real readback.
- Same-run Dify retrieval/diagnosis and local Markdown/PNG/MP3/ZIP are proven in one current Edge flow.
- Formal evidence contains no secrets, raw remote IDs/bodies or unredacted input.
- Phase 9/10 and all final media remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dify-unified-live-chain/08-07-SUMMARY.md`.
</output>
