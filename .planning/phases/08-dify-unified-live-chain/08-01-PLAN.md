---
phase: 08-dify-unified-live-chain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/debugmate/dify_live_evidence.py
  - src/debugmate/cloud/__init__.py
  - src/debugmate/cloud/contracts.py
  - src/debugmate/cloud/receipts.py
  - scripts/export-phase8-tracked-inventory.ps1
  - scripts/capture_dify_c03_c04_c06.ps1
  - tests/test_probe_cli.py
  - tests/platform/test_dify_live_evidence.py
  - tests/cloud/test_run_envelope.py
  - tests/cloud/test_receipts.py
autonomous: true
requirements: [DIAG-02, EVID-01]
must_haves:
  truths:
    - "产品 Python 源码不再拥有 shell/subprocess 能力来判断 Git tracked 状态。"
    - "Dify envelope、usage、attempt 与 receipt 在任何 HTTP 或 UI 接线前已有严格、限长、可测试的合同。"
    - "receipt 只能 started -> succeeded|uncertain|failed，终态不可重写且不保存批准材料或远端原始 ID。"
  artifacts:
    - path: "src/debugmate/cloud/contracts.py"
      provides: "严格 Dify run envelope、safe usage、attempt 与错误分类合同"
      contains: "class DifyRunEnvelope"
    - path: "src/debugmate/cloud/receipts.py"
      provides: "原子 durable receipt store"
      contains: "class DifyReceiptStore"
    - path: "scripts/export-phase8-tracked-inventory.ps1"
      provides: "外部 QA 生成的 tracked path + SHA-256 inventory"
  key_links:
    - from: "scripts/export-phase8-tracked-inventory.ps1"
      to: "src/debugmate/dify_live_evidence.py"
      via: "严格 JSON inventory 输入，不在产品代码中启动 Git"
      pattern: "tracked_inventory"
    - from: "src/debugmate/cloud/receipts.py"
      to: "src/debugmate/cloud/contracts.py"
      via: "仅接受严格 receipt/attempt 状态模型"
      pattern: "DifyReceipt"
---

<objective>
先关闭 Phase 7 留下的 command-safety 红灯，并冻结 Phase 08 所需的 provider envelope、usage、attempt 和 durable receipt 合同。

Purpose: 消除不可信诊断命令附近的进程执行能力，并让后续 HTTP、知识、service 与 UI 计划共享同一组安全接口。
Output: commandless evidence validator、外部 tracked inventory 工具、严格 cloud 合同与原子 receipt store。
</objective>

<execution_context>
@C:/Users/20795/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20795/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dify-unified-live-chain/08-CONTEXT.md
@.planning/phases/08-dify-unified-live-chain/08-RESEARCH.md
@.planning/phases/08-dify-unified-live-chain/08-VALIDATION.md

<interfaces>
Current evidence seam from `src/debugmate/dify_live_evidence.py`:
```python
def validate_candidate_tree(repository_root: Path, evidence_root: Path) -> dict[str, str]: ...
def validate_published_tree(repository_root: Path, evidence_root: Path) -> dict[str, str]: ...
```

Locked new receipt states per D8-11:
```python
ReceiptStatus = Literal["started", "succeeded", "uncertain", "failed"]
# only started -> one terminal status; terminal receipts are immutable
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove product subprocess capability and inject a hash-bound tracked inventory</name>
  <files>src/debugmate/dify_live_evidence.py, scripts/export-phase8-tracked-inventory.ps1, scripts/capture_dify_c03_c04_c06.ps1, tests/test_probe_cli.py, tests/platform/test_dify_live_evidence.py</files>
  <read_first>src/debugmate/dify_live_evidence.py, tests/diagnosis/test_command_safety.py, tests/platform/test_dify_live_evidence.py, tests/test_probe_cli.py, scripts/capture_dify_c03_c04_c06.ps1, .planning/phases/08-dify-unified-live-chain/08-CONTEXT.md</read_first>
  <behavior>
    - Test 1: importing and calling the product validator never imports/calls subprocess, os.system, shell=True or another process primitive.
    - Test 2: a sorted inventory of repository-relative regular-file paths and lowercase SHA-256 values authorizes the exact evidence set.
    - Test 3: missing, extra, duplicate, absolute, parent-traversal, symlink/reparse or hash-mismatched inventory entries fail closed.
  </behavior>
  <action>Per D8-20, delete `_git_tracked()` and every `subprocess` dependency from `dify_live_evidence.py`; do not broaden the command-safety allowlist. Change `validate_candidate_tree()` and `validate_published_tree()` to require a strict externally supplied `tracked_inventory` whose entries contain only `path` (forward-slash repository-relative path) and `sha256` (`^[0-9a-f]{64}$`). Canonicalize by rejecting absolute/`..` paths, links/reparse points, duplicates, unsorted entries, absent files, extra candidate files and hash mismatch. Add a required CLI `--tracked-inventory <inventory.json>` (inventory-file) argument to every CLI subcommand that reaches either validator; parse and strict-validate the file before validation. Add `scripts/export-phase8-tracked-inventory.ps1` that uses native Git only in the QA layer, resolves the repository root, enumerates only explicitly supplied candidate paths, rejects non-tracked/ignored/link targets, computes SHA-256, sorts paths ordinally and emits UTF-8 JSON without secrets. Update `scripts/capture_dify_c03_c04_c06.ps1` to generate the inventory and pass `--tracked-inventory` on every validator call. Update `tests/test_probe_cli.py` and platform tests to cover missing/malformed inventory, candidate and published validation, and the historical C03/C04/C06 path layout so existing evidence commands remain compatible. Preserve existing validation semantics; do not touch DSL, UI, media or historical evidence.</action>
  <acceptance_criteria>
    - `src/debugmate/dify_live_evidence.py` contains no `import subprocess`, `subprocess.`, `os.system`, `Popen` or `shell=True`.
    - `validate_published_tree` has a required tracked inventory parameter and rejects an inventory hash mismatch in a focused test.
    - `scripts/export-phase8-tracked-inventory.ps1` emits only `path` and `sha256` for each entry and uses `-LiteralPath` for filesystem access.
    - Every CLI/PowerShell caller of `validate_candidate_tree` or `validate_published_tree` supplies the required `--tracked-inventory` file; no compatibility path silently fabricates inventory.
    - `tests/test_probe_cli.py` retains tests for the historical C03/C04/C06 directory layout and fails when the inventory argument is absent or invalid.
    - The command-safety test passes without adding `dify_live_evidence.py` to an allowlist.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest tests/diagnosis/test_command_safety.py tests/platform/test_dify_live_evidence.py tests/test_probe_cli.py -q</automated>
  </verify>
  <done>The previous 60-pass/1-fail focused baseline is fully green and Git inventory acquisition exists only outside product Python.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define bounded cloud contracts and an atomic terminal receipt store</name>
  <files>src/debugmate/cloud/__init__.py, src/debugmate/cloud/contracts.py, src/debugmate/cloud/receipts.py, tests/cloud/test_run_envelope.py, tests/cloud/test_receipts.py</files>
  <read_first>src/debugmate/adapters/base.py, src/debugmate/diagnosis/workflow.py, src/debugmate/results/outcome_store.py, src/debugmate/results/contracts.py, .planning/phases/08-dify-unified-live-chain/08-CONTEXT.md, .planning/phases/08-dify-unified-live-chain/08-VALIDATION.md</read_first>
  <behavior>
    - Test 1: envelope version 1.0.0 accepts exactly case_id, diagnosis, extraction_facts, retrieval_trace and contract; unknown/deep/oversized/duplicate trace data is rejected.
    - Test 2: usage preserves documented numeric values when present and the literal `not_reported` when absent; zero is never synthesized.
    - Test 3: attempts are exactly upload, workflow or contract_repair and expose only safe fingerprints/status/latency.
    - Test 4: receipt identity is SHA-256 of canonical approval-identity fingerprint + preview hash, persists `started` before dispatch and permits one immutable terminal transition.
    - Test 5: restart, duplicate begin, terminal rewrite, corrupt JSON, link/reparse root and approval/raw-ID fields fail closed.
  </behavior>
  <action>Create strict/frozen Pydantic models with `extra='forbid'`: `ExecutionBackend` (`dify|local_fallback|replay`), `ReportedMetric` (nonnegative strict number or literal `not_reported`), `DifyUsage`, `DifyAttemptKind` (`upload|workflow|contract_repair`), `DifyAttempt`, `RetrievalHit`, `RetrievalTrace`, `DifyRunEnvelope`, `CloudFailureCode`, and `DifyReceipt`. Apply D8-05/D8-16 limits: envelope version `1.0.0`, maximum 4 retrieval hits, 2,000 characters per hit summary, 128 characters per safe failure detail, SHA-256 fingerprints instead of raw IDs, and strict DiagnosisRecord schema version `1.1.0`. Implement `DifyReceiptStore` as one JSON file per receipt beneath a validated non-link root, with sibling temp write, flush/fsync and atomic replace; persist only receipt hash, case ID, preview hash, backend, safe attempt fingerprints/status/timestamps and accepted local result identity. Never persist approval ID/signature/token, key, provider body/header/message or raw remote ID. Enforce `started -> succeeded|uncertain|failed`; all terminal states are immutable. Provide pure constructor/hash helpers for downstream plans.</action>
  <acceptance_criteria>
    - `src/debugmate/cloud/contracts.py` contains `class DifyRunEnvelope`, `class DifyUsage`, `class DifyAttempt`, `class DifyReceipt`, and `ExecutionBackend` with exactly `dify`, `local_fallback`, `replay`.
    - Retrieval trace validation enforces at most 4 hits and summary length at most 2,000 characters.
    - Usage tests assert missing provider fields serialize as `not_reported`, not `0`.
    - Receipt tests prove duplicate begin and every terminal-to-any transition fails.
    - Serialized receipt fixtures contain no approval token/signature, API key, provider body, or UUID-like raw remote identifier.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest tests/cloud/test_run_envelope.py tests/cloud/test_receipts.py tests/diagnosis/test_command_safety.py -q</automated>
  </verify>
  <done>All downstream Phase 08 code can import one strict envelope/attempt/receipt/backend contract and durable receipt behavior is adversarially proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| QA process → product validator | External Git inventory is untrusted until strict path/hash validation. |
| Approval authority → receipt disk | Secret approval material must collapse to a non-secret fingerprint before persistence. |
| Provider envelope → local models | Arbitrary JSON must be bounded and rejected before publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-08-COMMAND | Elevation | `dify_live_evidence.py` | mitigate | Remove all process capability; command-safety test must pass without allowlist expansion. |
| T-08-LEAK | Information disclosure | cloud contracts/receipts | mitigate | Strict allowlist fields and SHA-256 fingerprints; scan serialized fixtures for forbidden approval/provider fields. |
| T-08-DUPLICATE | Tampering/DoS | receipt transitions | mitigate | Atomic `started` record and immutable terminal state machine; restart/duplicate tests. |
| T-08-SIZE | DoS | run envelope | mitigate | Strict entry/string limits, `extra='forbid'`, bounded candidate contract tests. |
</threat_model>

<verification>
Run the two task commands, then `\.venv\Scripts\python.exe -m ruff check src/debugmate/cloud src/debugmate/dify_live_evidence.py tests/cloud tests/platform/test_dify_live_evidence.py`. Confirm `git diff --name-only` contains no deliverables, PPTX, MP4, SRT, final screenshots, Phase 9 or Phase 10 paths.
</verification>

<success_criteria>
- The pre-existing command-safety red test is green without weakening policy.
- Strict versioned cloud and receipt interfaces exist before any consumer plan.
- Threat mitigations are covered by executable tests, not prose-only checks.
</success_criteria>

<output>
After completion, create `.planning/phases/08-dify-unified-live-chain/08-01-SUMMARY.md`.
</output>
