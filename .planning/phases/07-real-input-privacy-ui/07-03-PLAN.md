---
phase: 07-real-input-privacy-ui
plan: 03
type: tdd
wave: 2
depends_on: ["07-02"]
files_modified:
  - src/debugmate/ui/serve.py
  - tests/ui/test_real_input.py
  - tests/ui/test_local_live.py
  - tests/diagnosis/test_extraction_providers.py
autonomous: true
requirements: [INP-01, INP-02, SAFE-01, UX-01]
must_haves:
  truths:
    - "Ordinary app assembly creates exactly one production RapidOcrBackend shared by preview and approved-screenshot extraction."
    - "Both live and replay services are construction-time local-only: no Dify, Edge TTS, HTTPX or outbound socket adapter is imported, constructed or probed."
    - "Approved screenshot extraction resolves under the same redacted root, rechecks SHA-256 and exposes the established six corrigible fields without changing diagnostic semantics."
  artifacts:
    - path: "src/debugmate/ui/serve.py"
      provides: "Phase 07 local-only dependency composition with shared RapidOCR"
      contains: "RapidOcrBackend"
    - path: "tests/ui/test_real_input.py"
      provides: "Construction poison and shared-backend identity tests"
    - path: "tests/diagnosis/test_extraction_providers.py"
      provides: "Approved redacted screenshot six-field integration"
  key_links:
    - from: "src/debugmate/ui/serve.py"
      to: "src/debugmate/diagnosis/providers.py"
      via: "shared RapidOcrBackend injected into ProductionExtractionProvider"
      pattern: "ProductionExtractionProvider\([\s\S]*ocr_backend"
    - from: "src/debugmate/ui/serve.py"
      to: "src/debugmate/ui/app.py"
      via: "same OCR and redacted root supplied to real preview assembly"
      pattern: "build_app"
---

<objective>
Build the ordinary Phase 07 service graph with shared production OCR and a hard construction-time local-only boundary.

Purpose: Make screenshot preview and downstream six-field extraction real while guaranteeing Phase 08 cloud dependencies are not even constructed.
Output: A local service/app dependency assembly that shares one RapidOCR instance and one root-confined redacted artifact root.
</objective>

<execution_context>
@C:/Users/20795/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20795/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-real-input-privacy-ui/07-UI-SPEC.md
@.planning/phases/07-real-input-privacy-ui/07-RESEARCH.md
@src/debugmate/privacy/rapidocr_backend.py
@src/debugmate/diagnosis/providers.py
@src/debugmate/ui/serve.py
@src/debugmate/ui/app.py
@src/debugmate/results/service.py
@tests/diagnosis/test_extraction_providers.py
@tests/ui/test_local_live.py
@tests/ui/test_real_input.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Construct one shared RapidOCR and one trusted redacted root</name>
  <files>src/debugmate/ui/serve.py, tests/ui/test_real_input.py, tests/diagnosis/test_extraction_providers.py</files>
  <read_first>
    - src/debugmate/privacy/rapidocr_backend.py
    - src/debugmate/privacy/text_redactor.py
    - src/debugmate/diagnosis/providers.py
    - src/debugmate/diagnosis/workflow.py
    - src/debugmate/ui/serve.py
    - tests/diagnosis/test_extraction_providers.py
    - tests/ui/test_real_input.py
  </read_first>
  <behavior>
    - "Ordinary construction creates one lazy RapidOcrBackend object and injects object identity into preview and ProductionExtractionProvider."
    - "Screenshot-only approved input reaches OCR extraction from the rehashed redacted PNG and can yield exception type, traceback line, package/module, version, device and path candidates."
    - "RapidOCR initialization/inference failure remains `ocr_unavailable` at preview and never silently creates an empty approved screenshot."
  </behavior>
  <action>RED: run shared-instance/source and extraction integration tests. GREEN: delete `_NoopOcr` from ordinary `serve.py`; create a single lazy `RapidOcrBackend()` in the ordinary dependency factory, create one absolute runtime redacted root, and inject the same backend/root into both the Phase 07 preview builder/store seam and `ProductionExtractionProvider`. Keep fakes/noop only inside isolated tests. Ensure approved screenshot extraction continues to call `_verified_screenshot` for root confinement, file existence, SHA-256 and image validation before OCR. Do not add VLM/Dify in this phase and do not reinterpret candidates; preserve the established six-field correction flow. REFACTOR construction into an explicit local dependency object/factory only if that makes identity and poison tests simpler.</action>
  <acceptance_criteria>
    - `src/debugmate/ui/serve.py` contains `RapidOcrBackend` and does not contain `class _NoopOcr` or instantiate any noop OCR in ordinary assembly.
    - A test proves the preview OCR backend `is` the extraction provider OCR backend.
    - Approved redacted screenshot tests prove root confinement + rehash occurs before OCR and the six existing field IDs remain unchanged.
    - OCR failure returns a stable safe code/copy path and zero approved/diagnosis calls; raw exception/path is absent.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/diagnosis/test_extraction_providers.py tests/ui/test_real_input.py -k "ocr or screenshot or extraction or shared"</automated>
  </verify>
  <done>One production OCR instance powers both safe preview redaction and verified screenshot fact extraction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce construction-time local-only live and replay composition</name>
  <files>src/debugmate/ui/serve.py, tests/ui/test_real_input.py, tests/ui/test_local_live.py</files>
  <read_first>
    - src/debugmate/ui/serve.py
    - src/debugmate/results/tts/dify.py
    - src/debugmate/results/tts/edge.py
    - src/debugmate/results/tts/sapi.py
    - src/debugmate/results/service.py
    - tests/ui/test_local_live.py
    - tests/ui/test_real_input.py
  </read_first>
  <behavior>
    - "Poisoning Dify/Edge constructors, HTTPX client constructors and socket connection factories before local service/app construction yields zero poison calls for live and replay."
    - "Live and replay use only local rule/fixture inputs and local SAPI; unavailable SAPI produces the existing honest partial state."
    - "No Dify settings, API keys, cloud status or Edge fallback is read or displayed in Phase 07 ordinary mode."
  </behavior>
  <action>RED: poison every cloud/network construction boundary before `_local_service`, composer and app construction for both modes. GREEN: remove ordinary `DifyTtsAdapter` and `EdgeTtsAdapter` imports/branches from `serve.py`; remove the unsafe `replay_local_only=False` default and make all Phase 07 composition explicitly local-only with `TtsFallbackChain((SapiTtsAdapter(...),), local_only=True)`. Preserve QA-only local failure injection without importing network adapters. Do not merely avoid calling constructed adapters—tests must prove zero construction. Keep `share=False`, literal loopback host and no external assets.</action>
  <acceptance_criteria>
    - Ordinary `serve.py` source has no imports of `results.tts.dify`, `results.tts.edge`, `DifyTtsAdapter` or `EdgeTtsAdapter`.
    - Live and replay poison tests assert zero adapter construction, zero HTTPX client construction and zero outbound `socket.create_connection`.
    - Replay still loads allowlisted fixed fixtures and is labeled replay; local audio failure remains partial rather than cloud fallback.
    - Existing local result identity, report/card/audio/ZIP behavior remains unchanged when SAPI succeeds.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/ui/test_local_live.py tests/ui/test_real_input.py -k "local_only or construction or replay or network"; .\.venv\Scripts\python.exe -m ruff check src/debugmate/ui/serve.py tests/ui/test_local_live.py tests/ui/test_real_input.py</automated>
  </verify>
  <done>Phase 07 live and replay construction contain no cloud/network adapters and remain truthfully local.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| validated upload → RapidOCR | Untrusted image bytes enter CPU-heavy OCR only after local validation. |
| approved screenshot reference → extraction provider | Server-owned relative path is resolved and rehashed before a second OCR read. |
| service construction → external adapters | Imports/constructors can violate the no-cloud phase even without a user call. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-07-CLOUD | Information Disclosure / Repudiation | high | `ui/serve.py` | mitigate/block | Remove cloud/Edge imports and poison constructors before both live/replay assembly. |
| T-07-OCR | Information Disclosure | high | RapidOCR failure | mitigate/block | Existing safe `OcrUnavailable`; no Noop fallback, raw exception or path. |
| T-07-PATH | Tampering / EoP | high | approved screenshot extraction | mitigate/block | Shared trusted root, `resolve_artifact_path`, file check, SHA-256 compare and image validation before OCR. |
| T-07-IMAGE-DOS | Denial of Service | high | screenshot OCR | mitigate/block | Existing 10 MiB/20 MP real-byte gates execute before OCR; no bypass via extension/MIME. |
</threat_model>

<verification>
Run focused OCR/extraction and construction-poison tests, explicit OCR smoke, then all local-live tests and scoped Ruff. OCR smoke failure blocks screenshot completion; it is not a passable fallback.
</verification>

<success_criteria>
- Ordinary screenshot input uses production RapidOCR twice safely: preview redaction and approved-field extraction.
- The same root and backend identity are used end-to-end with rehashing before downstream reads.
- Dify/Edge/network adapters are absent at construction time for both live and replay.
</success_criteria>

<output>
After completion, create `.planning/phases/07-real-input-privacy-ui/07-03-SUMMARY.md`.
</output>
