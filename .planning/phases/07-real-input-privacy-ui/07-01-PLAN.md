---
phase: 07-real-input-privacy-ui
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - .planning/phases/07-real-input-privacy-ui/07-EXECUTION-BASELINE.json
  - scripts/assert-phase7-frozen-scope.ps1
  - scripts/assert-phase7-red.ps1
  - tests/ui/test_real_input.py
  - tests/privacy/test_preview_integration.py
  - tests/ui/test_browser.py
autonomous: true
requirements: [INP-01, INP-02, SAFE-01, UX-01]
must_haves:
  truths:
    - "Phase 07 tests can be collected in the pinned Python 3.13 environment; dependency installation is recorded only as execution readiness, not product completion."
    - "RED tests precisely freeze the four-field input, screenshot audit/hash, revision/token races, construction-time local-only behavior, orthogonal UI state, and Phase 07 Edge evidence contract before production code changes."
    - "Environment-gated OCR and Edge checks remain explicit gates/skips and are never counted as passing product behavior when their dependencies are unavailable."
  artifacts:
    - path: "tests/ui/test_real_input.py"
      provides: "RED contract suite for real inputs, revisions, callbacks, local-only assembly and state combinations"
    - path: "tests/privacy/test_preview_integration.py"
      provides: "RED screenshot audit and preview-hash binding cases"
    - path: "tests/ui/test_browser.py"
      provides: "Phase 07 selector and evidence namespace contracts without changing Phase 04 captures"
    - path: ".planning/phases/07-real-input-privacy-ui/07-EXECUTION-BASELINE.json"
      provides: "Execution-start commit and frozen-file SHA-256 baseline for the final scope gate"
    - path: "scripts/assert-phase7-frozen-scope.ps1"
      provides: "Immediate schema, target coverage, SHA-256 and clean frozen-state validator"
    - path: "scripts/assert-phase7-red.ps1"
      provides: "Fail-expecting RED semantic verifier with clean collection/error classification"
  key_links:
    - from: "tests/ui/test_real_input.py"
      to: "src/debugmate/ui/local_live.py"
      via: "direct store race tests"
      pattern: "revision|publish_if_current|consume_current"
    - from: "tests/privacy/test_preview_integration.py"
      to: "src/debugmate/privacy/text_redactor.py"
      via: "build_preview assertions"
      pattern: "screenshot_audit|preview_hash"
    - from: "scripts/assert-phase7-frozen-scope.ps1"
      to: ".planning/phases/07-real-input-privacy-ui/07-EXECUTION-BASELINE.json"
      via: "exact schema/coverage/hash validation immediately after capture and at final gate"
      pattern: "07-EXECUTION-BASELINE.json"
    - from: "scripts/assert-phase7-red.ps1"
      to: "tests/ui/test_real_input.py and tests/privacy/test_preview_integration.py"
      via: "collect-success plus expected named RED-failure classification"
      pattern: "test_phase7_contract_"
---

<objective>
Repair the pinned local test environment and establish failing Phase 07 contracts before changing production code.

Purpose: Prevent false-green implementation caused by the currently incomplete `.venv`, and make every privacy, race, UI and browser obligation executable from the start.
Output: A dependency-ready local environment plus focused RED tests. Environment repair is readiness only and satisfies no product requirement by itself.
</objective>

<execution_context>
@C:/Users/20795/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20795/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-real-input-privacy-ui/07-CONTEXT.md
@.planning/phases/07-real-input-privacy-ui/07-UI-SPEC.md
@.planning/phases/07-real-input-privacy-ui/07-RESEARCH.md
@.planning/phases/07-real-input-privacy-ui/07-VALIDATION.md
@pyproject.toml
@tests/privacy/test_preview_integration.py
@tests/ui/test_app.py
@tests/ui/test_local_live.py
@tests/ui/test_browser.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair and prove the pinned Phase 07 execution environment</name>
  <files>.venv/ (local environment only), .planning/phases/07-real-input-privacy-ui/07-EXECUTION-BASELINE.json, scripts/assert-phase7-frozen-scope.ps1</files>
  <read_first>
    - pyproject.toml
    - .planning/phases/07-real-input-privacy-ui/07-RESEARCH.md
    - .planning/phases/07-real-input-privacy-ui/07-VALIDATION.md
    - scripts/run-phase4-local-live-qa.ps1
  </read_first>
  <action>Before any production/test edit, write `.planning/phases/07-real-input-privacy-ui/07-EXECUTION-BASELINE.json` with exact keys `schema_version`, `baseline_commit`, `captured_at_utc`, and `frozen_files`; obtain `baseline_commit` from `git rev-parse HEAD`, and map every currently tracked file under `deliverables/**`, `evidence/ui/phase4/**`, `evidence/course-v0.1/**`, plus every tracked `*.pptx`, `*.mp4`, `*.srt` and Phase 08–10 planning file to its lowercase SHA-256. Fail if the worktree already has changes in any frozen target; do not silently baseline modified media. Create `scripts/assert-phase7-frozen-scope.ps1` with parameters `-BaselinePath` and optional `-RepositoryRoot`; use `Resolve-Path -LiteralPath`, require the exact four-key JSON schema, require `schema_version=1`, a 40-lowercase-hex commit and UTC-Z timestamp, independently enumerate the complete tracked frozen target set with `git ls-files`, require exact key-set equality with `frozen_files`, require every stored/current hash to match `^[0-9a-f]{64}$` and byte-for-byte SHA-256 equality, and fail non-zero if any frozen target is dirty relative to `baseline_commit` or the working tree. It must use only PowerShell-native path operations/`-LiteralPath`, fail on git/JSON/hash errors, explicitly `exit 1` on mismatch and `exit 0` only after all checks. Immediately invoke this validator after baseline creation. Then run `.\.venv\Scripts\python.exe -m pip install -e ".[dev]"` in the repository root and prove Python is 3.13.x and exact imports/versions are available for `debugmate`, `gradio==6.20.0`, `pydantic==2.13.4`, `rapidocr==3.9.1`, `playwright==1.61.0`, `Pillow==12.3.0`, `pytest==9.1.1`, and `ruff==0.15.21`. Run `pip check`. Do not modify pins, add a second environment, install Conda/WSL, or claim any INP/SAFE/UX behavior is complete merely because installation succeeds. If Microsoft Edge is missing, record the Edge gate as blocked; do not substitute Chromium and do not mark it passed.</action>
  <acceptance_criteria>
    - `.\.venv\Scripts\python.exe -c "import sys; assert sys.version_info[:2] == (3, 13)"` exits 0.
    - All pinned module imports and exact package-version assertions exit 0.
    - `.\.venv\Scripts\python.exe -m pip check` prints `No broken requirements found.`
    - `07-EXECUTION-BASELINE.json` contains the exact start commit and SHA-256 for every frozen tracked target, and no frozen target was dirty when captured.
    - `scripts/assert-phase7-frozen-scope.ps1` immediately exits 0 on the captured baseline and exits non-zero for malformed schema, missing/extra target, hash mismatch or dirty frozen state fixtures.
    - The execution summary explicitly labels this task `environment readiness only` and does not list INP-01, INP-02, SAFE-01 or UX-01 as implemented by this task.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\assert-phase7-frozen-scope.ps1 -BaselinePath .\.planning\phases\07-real-input-privacy-ui\07-EXECUTION-BASELINE.json -RepositoryRoot .; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -c "import importlib.metadata as m, sys; assert sys.version_info[:2] == (3,13); import debugmate, gradio, pydantic, rapidocr, playwright, PIL; expected={'gradio':'6.20.0','pydantic':'2.13.4','rapidocr':'3.9.1','playwright':'1.61.0','Pillow':'12.3.0','pytest':'9.1.1','ruff':'0.15.21'}; assert all(m.version(k)==v for k,v in expected.items())"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m pip check</automated>
  </verify>
  <done>The pinned environment is importable and internally consistent, with no product-completion claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED privacy, revision, assembly and UI contracts</name>
  <files>tests/ui/test_real_input.py, tests/privacy/test_preview_integration.py, scripts/assert-phase7-red.ps1</files>
  <read_first>
    - src/debugmate/privacy/models.py
    - src/debugmate/privacy/text_redactor.py
    - src/debugmate/privacy/image_models.py
    - src/debugmate/privacy/image_redactor.py
    - src/debugmate/privacy/approval.py
    - src/debugmate/ui/local_live.py
    - src/debugmate/ui/presentation.py
    - src/debugmate/ui/app.py
    - src/debugmate/ui/serve.py
    - tests/privacy/test_preview_integration.py
    - tests/ui/test_local_live.py
    - tests/ui/test_app.py
    - tests/ui/test_view_state.py
    - pyproject.toml
    - scripts/assert-phase7-frozen-scope.ps1
  </read_first>
  <behavior>
    - "Text-only, screenshot-only and text+screenshot inputs reach preview; whitespace-only and code/environment-only inputs reject before OCR/store/service."
    - "Screenshot audit contains provided, ocr_status, finding_count and counts_by_kind without OCR text/boxes/paths, and changes preview_hash."
    - "Session+revision+TTL+one-time token races cover change→approve, approve→change, slow preview N→change, N→N+1, duplicate approve, cross-session, expiry, tamper and replay invalidation."
    - "Both live and replay service construction fail the test if Dify, Edge, HTTPX or socket network constructors are touched."
    - "Mode, PrivacyPreviewState and ResultViewState remain three independent values and exhaust the UI precedence/action/aria-live table."
  </behavior>
  <action>Create `tests/ui/test_real_input.py` with deterministic fakes, barriers/events for actual threaded store races, strict component/config inspection and poison constructors. Name the four top-level missing-contract sentinels exactly `test_phase7_contract_screenshot_audit_hash_binding`, `test_phase7_contract_revision_atomic_consume`, `test_phase7_contract_construction_local_only`, and `test_phase7_contract_orthogonal_state`; additional detailed tests may follow the same `test_phase7_contract_` prefix. Extend `tests/privacy/test_preview_integration.py` with screenshot sensitive/zero-finding audit cases, preview-hash binding, safe OCR failure cleanup and value/path-free serialization assertions. Tests must target the concrete APIs prescribed by the UI spec (`invalidate_and_increment`, `snapshot_revision`, `publish_if_current`, `consume_current`) and fail against current fixed-payload/revision-less code. Preserve existing tests; do not weaken assertions or xfail missing behavior. Create checked-in `scripts/assert-phase7-red.ps1` with no dynamically constructed outer `powershell -Command`: resolve repository/test/python paths using `Resolve-Path -LiteralPath`, invoke pytest collection and require exit 0, invoke the `phase7_contract` tests and require non-zero, join captured stdout/stderr, reject `ImportError`, `ModuleNotFoundError`, `ERROR collecting`, `no tests ran` and pytest internal errors, require all four exact test names in the failing set, reject unexpected failing `test_phase7_contract_*` names, then explicitly exit 0. Script failures and missing tools/paths must exit non-zero. Do not depend on caller variables, command interpolation or shell-expanded globs.</action>
  <acceptance_criteria>
    - `tests/ui/test_real_input.py` exists and contains tests naming all eight mandated race/abuse orders and both live/replay construction-time network poison cases.
    - `tests/privacy/test_preview_integration.py` asserts screenshot audit is included in `preview_hash`, including zero findings and sensitive findings.
    - Focused pytest collects all new tests, fails for missing Phase 07 production behavior, and has no import/collection error.
    - Test source contains no `xfail`, unconditional `skip`, fixed product secret, raw local absolute-path snapshot, or expectation that `_NoopOcr` is valid in ordinary assembly.
    - `scripts/assert-phase7-red.ps1` is checked in, uses `-LiteralPath`, has the exact expected failing-name set, and returns 0 only for clean collection plus those expected RED failures.
  </acceptance_criteria>
  <verify>
    <automated>powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\assert-phase7-red.ps1</automated>
  </verify>
  <done>The Phase 07 behavior suite collects cleanly and is RED only because production contracts are not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze the Phase 07 real Edge evidence namespace</name>
  <files>tests/ui/test_browser.py</files>
  <read_first>
    - .planning/phases/07-real-input-privacy-ui/07-UI-SPEC.md
    - .planning/phases/07-real-input-privacy-ui/07-VALIDATION.md
    - tests/ui/test_browser.py
    - scripts/run-phase4-local-live-qa.ps1
    - scripts/run-phase4-browser-layout-qa.ps1
  </read_first>
  <behavior>
    - "Phase 07 browser tests are selected only by `phase7`/`p7_` names and write only under `evidence/ui/phase7/`."
    - "The ledger allowlist records viewport, state, mode, OCR backend/status, body overflow, screenshot SHA-256 and UTC timestamp; it records no raw input/token/path/OCR text."
    - "P7-VQ-01/02/03/04/07/08/10/11 selectors exist without overwriting Phase 04 or course-final evidence."
  </behavior>
  <action>Add the Phase 07 browser test names, stable selector checks and evidence-ledger validation scaffolding described by the UI spec. At this Wave 0 stage, tests may be RED because the components do not exist, but they must collect. Do not edit or delete existing Phase 04 tests/evidence paths, and do not write screenshots during collection. Use `channel="msedge"` only.</action>
  <acceptance_criteria>
    - `pytest --collect-only -m browser ... -k "phase7 or p7_"` lists Phase 07 tests and exits 0.
    - Source references `evidence/ui/phase7` and does not redirect any existing `evidence/ui/phase4` target.
    - Ledger assertions use an exact field allowlist and reject token, path, raw text, OCR text and secret fields.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest --collect-only -q -m browser tests/ui/test_browser.py -k "phase7 or p7_"</automated>
  </verify>
  <done>The real Edge gate is executable and isolated before UI implementation begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local environment → test claims | Missing packages can turn collection failures or skips into false completion claims. |
| test fixtures → production contract | Weak fakes can bless Noop OCR, browser-owned authority or network construction. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-07-ENV | Repudiation | high | `.venv` and validation output | mitigate/block | Exact import/version and `pip check` gates; environment readiness explicitly cannot satisfy product requirements. |
| T-07-TEST-GAP | Tampering | high | Wave 0 tests | mitigate/block | RED tests enumerate all locked race, OCR, DOM, local-only and Edge contracts; no xfail/weak skip. |
| T-07-EVIDENCE | Information Disclosure | high | browser evidence ledger | mitigate/block | Exact value-free allowlist and Phase 07-only namespace before any screenshot capture. |
</threat_model>

<verification>
Environment imports and `pip check` pass; new tests collect; product behavior tests are demonstrably RED for missing implementation rather than missing dependencies.
</verification>

<success_criteria>
- The executor can start Phase 07 with a trustworthy pinned environment.
- Every production change in later waves has an already-collected RED test target.
- No environment repair or skipped Edge/OCR gate is misreported as product completion.
</success_criteria>

<output>
After completion, create `.planning/phases/07-real-input-privacy-ui/07-01-SUMMARY.md`.
</output>
