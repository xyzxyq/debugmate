---
phase: 07-real-input-privacy-ui
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - tests/ui/test_browser.py
  - scripts/run-phase7-real-input-qa.ps1
  - scripts/verify-phase7-security-scope.ps1
  - evidence/ui/phase7/**
autonomous: true
requirements: [INP-01, INP-02, SAFE-01, UX-01]
must_haves:
  truths:
    - "Real Microsoft Edge proves idle, ready, stale, OCR failure, replay, keyboard, 200% zoom and responsive Phase 07 states against the actual loopback Gradio app."
    - "Engineering evidence is atomic, hash-bound, value-free and isolated under `evidence/ui/phase7`; no Phase 04 or course-final screenshot is touched."
    - "The final runner accepts only the exact nine-scenario/viewport inventory for its own fresh `qa_run_id`; any browser/OCR skip, missing/extra/stale artifact or identity/hash mismatch fails before promotion."
    - "The full offline regression, Ruff, explicit RapidOCR smoke, Edge suite, secret/path scan and frozen-media diff gate all pass before Phase 07 is considered complete."
  artifacts:
    - path: "tests/ui/test_browser.py"
      provides: "Real Edge Phase 07 workflow and accessibility tests"
      contains: "p7_"
    - path: "scripts/run-phase7-real-input-qa.ps1"
      provides: "Owned loopback server, atomic Phase 07 evidence capture and cleanup gate"
      contains: "evidence\\ui\\phase7"
    - path: "scripts/verify-phase7-security-scope.ps1"
      provides: "Non-zero secret/path scan and execution-baseline frozen-file/hash gate"
      contains: "07-EXECUTION-BASELINE.json"
    - path: "evidence/ui/phase7/*.png"
      provides: "Required real Edge Phase 07 engineering screenshots"
    - path: "evidence/ui/phase7/*.json"
      provides: "Value-free screenshot ledgers with exact allowlisted fields and SHA-256"
  key_links:
    - from: "scripts/run-phase7-real-input-qa.ps1"
      to: "tests/ui/test_browser.py"
      via: "explicit `-m browser -k phase7 or p7_` invocation"
      pattern: "test_browser.py"
    - from: "tests/ui/test_browser.py"
      to: "src/debugmate/ui/serve.py"
      via: "real loopback Gradio `/config` and Microsoft Edge"
      pattern: "channel=\"msedge\""
    - from: "scripts/run-phase7-real-input-qa.ps1"
      to: "evidence/ui/phase7/*.png and *.json"
      via: "validated atomic staging/promotion after the final Edge run"
      pattern: "evidence\\ui\\phase7"
    - from: "scripts/verify-phase7-security-scope.ps1"
      to: ".planning/phases/07-real-input-privacy-ui/07-EXECUTION-BASELINE.json"
      via: "baseline commit and frozen-file SHA-256 comparison"
      pattern: "07-EXECUTION-BASELINE.json"
    - from: "scripts/verify-phase7-security-scope.ps1"
      to: "scripts/assert-phase7-frozen-scope.ps1"
      via: "reuse of the Wave 0 exact frozen schema/coverage/hash validator"
      pattern: "assert-phase7-frozen-scope.ps1"
---

<objective>
Close Phase 07 with real Edge engineering QA and full regression/security gates.

Purpose: Prove the actual Windows browser workflow, accessibility, responsive layout and privacy boundary rather than relying only on structural unit tests.
Output: Phase 07 browser tests, an atomic QA runner and fresh engineering evidence under a new namespace only.
</objective>

<execution_context>
@C:/Users/20795/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20795/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-real-input-privacy-ui/07-UI-SPEC.md
@.planning/phases/07-real-input-privacy-ui/07-VALIDATION.md
@tests/ui/test_browser.py
@scripts/run-phase4-local-live-qa.ps1
@scripts/run-phase4-browser-layout-qa.ps1
@src/debugmate/ui/serve.py
@src/debugmate/ui/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the real Phase 07 Microsoft Edge scenarios</name>
  <files>tests/ui/test_browser.py</files>
  <read_first>
    - .planning/phases/07-real-input-privacy-ui/07-UI-SPEC.md
    - tests/ui/test_browser.py
    - src/debugmate/ui/app.py
    - src/debugmate/ui/serve.py
  </read_first>
  <behavior>
    - "P7-VQ-01 idle, P7-VQ-02 valid text+synthetic screenshot ready, P7-VQ-03 each-field stale, P7-VQ-04 OCR unavailable and P7-VQ-07 replay render exact truthful state."
    - "P7-VQ-08 covers 1024×768 and 768×1024; P7-VQ-09 covers 375×812; no body horizontal overflow."
    - "P7-VQ-10 keyboard order/focus and P7-VQ-11 200% zoom keep both actions and errors reachable."
    - "Computed status contrast includes ancestor opacity/compositing and remains ≥4.5:1; grayscale retains icon+literal text."
  </behavior>
  <action>RED is already established by Wave 0. GREEN the browser tests against the implemented ordinary `debugmate.ui.serve` app using Playwright `chromium.launch(channel="msedge")`. Generate a deterministic synthetic terminal screenshot as test input, never as product evidence; interact through native upload/input controls. The user's raw values are expected only inside the four native editable controls `#error-input`, `#screenshot-input`, `#code-input`, and `#environment-input`; leakage assertions must exclude those user-owned edit surfaces and prove the values are not copied into preview/state/status/metadata/fixed HTML/result/log/download/config/browser storage. Assert exact copy, component state, redacted-only screenshot, value-free audit, no raw path/token outside the native upload control, zero body overflow, focus order, 40px controls, computed AA contrast, responsive order and 200% zoom geometry. Define all required engineering scenarios P7-VQ-01/02/03/04/07/08/10/11, but in this task run only collection plus a focused non-browser selector/ledger smoke; the complete Edge run and captures occur once in Task 3. Do not activate QA-only server state injection for claims that must exercise the real form/OCR/store callbacks.</action>
  <acceptance_criteria>
    - Focused collection lists every required real Edge scenario using `channel="msedge"`; no Chromium fallback is present.
    - Tests exercise actual input/change/preview/approve/replay callbacks and assert raw values remain only in the four user-owned native editable controls, with zero copies in preview/state/status/metadata/HTML/result/log/download/config/storage.
    - All required viewport, keyboard, zoom, contrast and grayscale states are asserted with exact Phase 07 copy.
    - Any missing Edge/OCR dependency is reported as a blocking gate or explicit environment skip, never counted as a passing behavior.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest --collect-only -q -m browser tests/ui/test_browser.py -k "phase7 or p7_"; .\.venv\Scripts\python.exe -m pytest -q tests/ui/test_browser.py -m "not browser" -k "p7_selector or p7_ledger or p7_dom_scope"</automated>
  </verify>
  <done>The full real-input/privacy Edge acceptance matrix and scoped DOM assertions are implemented and collect cleanly; execution is deferred to the single final runner in Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Build and dry-validate the atomic Phase 07 evidence runner</name>
  <files>scripts/run-phase7-real-input-qa.ps1, tests/ui/test_browser.py</files>
  <read_first>
    - scripts/run-phase4-local-live-qa.ps1
    - scripts/run-phase4-browser-layout-qa.ps1
    - scripts/assert-phase7-frozen-scope.ps1
    - tests/ui/test_browser.py
    - .planning/phases/07-real-input-privacy-ui/07-UI-SPEC.md
  </read_first>
  <action>Create `scripts/run-phase7-real-input-qa.ps1` using the established PowerShell-native owned-process and atomic staging/backup/promote/restore pattern. It must be capable of launching `.venv\Scripts\python.exe -m debugmate.ui.serve --host 127.0.0.1 --port {owned-port}` hidden, verifying exact PID/start-time/command ownership and `/config`, running the explicit production RapidOCR smoke and only Phase 07 Edge tests with separate JUnit XML files, and treating any `skipped`, `errors` or `failures` count greater than zero in either JUnit result as non-zero runner failure. At runner start issue `qa_run_id=p7qa_` plus 32 lowercase hex and record `run_started_at_utc`; pass the ID to browser captures without exposing it as product state. Require exactly this inventory and no other formal capture: `P7-VQ-01@1366x768`, `P7-VQ-02@1366x768`, `P7-VQ-03@1366x768`, `P7-VQ-04@1366x768`, `P7-VQ-07@1366x768`, `P7-VQ-08-1024@1024x768`, `P7-VQ-08-768@768x1024`, `P7-VQ-10@1366x768`, `P7-VQ-11@1366x768`. Validate each ledger against the exact allowlist `{evidence_version, qa_run_id, scenario_id, viewport, privacy_state, result_status, mode, ocr_backend, ocr_status, case_id_sha256, source_run_id_sha256, result_id_sha256, screenshot_sha256, body_horizontal_overflow, verified_at_utc}`; require the current `qa_run_id`, a unique expected scenario/viewport pair, `verified_at_utc` within current runner start/finish, one ledger and one nonempty PNG per inventory item, exact PNG SHA-256 equality, and zero extra files. Before promotion reject any pre-existing/stale file in staging, any `.staging`/`.backup` residue, any prior-run `qa_run_id`, and any raw token/path/input/OCR text/secret field. Atomically promote the complete 9-ledger/9-PNG set only after all checks; partial sets never promote. Always stop only the captured process, verify port closure and restore environment variables. Never enumerate/delete broad directories, never overwrite `evidence/ui/phase4`, `evidence/course-v0.1`, `deliverables`, PPTX, MP4 or SRT. Add focused non-browser tests that dot-source/import runner helpers and exercise JUnit skip rejection, exact inventory/missing/extra/stale/current-run/time/hash validation plus transaction promote/rollback/cleanup with temporary files and injected failures. Do not start OCR/Edge, run the long runner or publish formal evidence in this task; that happens once in Task 3.</action>
  <acceptance_criteria>
    - Script uses `-LiteralPath`, explicit absolute project paths, hidden owned process and recorded PID/start-time validation.
    - Evidence targets are exclusively under `evidence\ui\phase7`; source contains no write target under Phase 04, course evidence or deliverables.
    - Ledger exact allowlist and screenshot SHA-256 equality are checked before atomic promotion.
    - Dry tests prove one skipped OCR/browser testcase, any missing/extra inventory pair, stale `qa_run_id`/timestamp, non-1:1 ledger/PNG set, hash mismatch or staging residue produces non-zero validation and zero promotion.
    - On test/capture/cleanup failure, prior formal evidence is restored and no `.staging`/`.backup` residue is silently accepted.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/ui/test_browser.py -m "not browser" -k "p7_runner or p7_transaction or p7_ledger or p7_inventory or p7_skip_gate"</automated>
  </verify>
  <done>The runner's static contract and transaction/dry-validation behavior are proven without performing the long Edge capture.</done>
</task>

<task type="auto">
  <name>Task 3: Run the complete privacy, OCR, regression and frozen-scope gate</name>
  <files>tests/ui/test_browser.py, scripts/run-phase7-real-input-qa.ps1, scripts/verify-phase7-security-scope.ps1, evidence/ui/phase7/**</files>
  <read_first>
    - pyproject.toml
    - .planning/phases/07-real-input-privacy-ui/07-VALIDATION.md
    - .planning/STATE.md
    - tests/privacy/test_rapidocr_smoke.py
    - tests/ui/test_browser.py
    - .planning/phases/07-real-input-privacy-ui/07-EXECUTION-BASELINE.json
    - scripts/assert-phase7-frozen-scope.ps1
  </read_first>
  <action>Create `scripts/verify-phase7-security-scope.ps1` as a fail-closed gate. It must first invoke checked-in `scripts/assert-phase7-frozen-scope.ps1` against `07-EXECUTION-BASELINE.json`, then verify its `baseline_commit` is an ancestor of current HEAD and fail non-zero on missing, added, modified or extra frozen targets. It must derive changed files from baseline commit through HEAD plus the working tree, then scan changed production files and `evidence/ui/phase7/**` for private-key headers, credential/token assignment with non-redacted values, approval signatures/tokens, `C:\Users\`, `/Users/`, `/home/`, Gradio temp/cache paths and forbidden cloud-success wording; synthetic test inputs are exempt only on the exact source line carrying marker `PHASE7_SYNTHETIC_SECRET`, and evidence has no exemption. Use `rg`/PowerShell match counts and explicitly `exit 1` on any finding, scan error, missing target or baseline mismatch; zero matches must explicitly `exit 0`. Add focused tests that inject a secret, local path, modified frozen hash and missing baseline and assert a non-zero exit for each. This Task 3 is the explicit long phase-level release gate and is exempt from the per-task ≤120-second development feedback target: development tasks retain focused ≤120-second checks, while only here the default full suite and the runner's production OCR + complete Edge inventory run once. Invoke the runner exactly once, require its JUnit OCR/browser skipped counts to be zero and its fresh exact 9-pair evidence inventory to validate, then run the security/scope script. Report each gate separately; any high threat, mandatory skip, missing/extra/stale artifact, OCR/Edge failure, scan finding or frozen mismatch blocks completion.</action>
  <acceptance_criteria>
    - Default offline pytest exits 0 and Ruff exits 0.
    - Explicit `-m ocr` smoke exits 0 with production RapidOCR; a dependency/model failure is blocking, not an accepted skip.
    - Phase 07 Edge runner exits 0 and ledger/screenshot hashes verify.
    - Runner JUnit results contain zero mandatory OCR/browser skips and evidence contains exactly nine current-run ledger/PNG pairs for the locked inventory, with no extra/stale staging artifacts.
    - Frozen-scope diff contains no Phase 08 Dify implementation, Phase 09 evaluation, Phase 10 media, Phase 04 evidence or course-final media changes.
    - Secret/path scan reports zero raw tokens, signatures, private identifiers and local absolute paths outside explicit synthetic test input source.
    - The security/scope gate's injected secret, path, frozen-hash and missing-baseline tests each exit non-zero; the clean repository case exits 0.
  </acceptance_criteria>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/privacy tests/ui/test_real_input.py tests/ui/test_local_live.py tests/ui/test_app.py tests/ui/test_view_state.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m pytest -q tests/ui/test_browser.py -m "not browser" -k "p7_security_scope"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m pytest; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m ruff check src tests; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-phase7-real-input-qa.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify-phase7-security-scope.ps1</automated>
  </verify>
  <done>All automated unit, integration, OCR, Edge, security and frozen-scope gates pass with no high threat left open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| loopback app → real Edge | Actual rendered DOM, browser storage, config and interaction are inspected. |
| Edge capture → repository evidence | Screenshots and ledgers become durable engineering artifacts. |
| Phase 07 changes → frozen project scope | Later-phase implementation and historical media/evidence must remain untouched. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-07-DOM | Information Disclosure | high | real Edge DOM/config/storage | mitigate/block | Sentinel and path/token scans plus actual redacted-only screenshot assertion. |
| T-07-EVIDENCE | Tampering / Repudiation | high | QA PNG/ledger | mitigate/block | Owned server, exact allowlist, SHA-256, UTC, atomic transaction and cleanup verification. |
| T-07-A11Y | Denial of Service | medium | keyboard/zoom/responsive UI | mitigate | Real Edge focus, computed contrast, 200% and four-width gates. |
| T-07-SCOPE | Tampering | high | frozen Phase 04/08–10/media | mitigate/block | Execution-start diff allowlist; any forbidden file modification blocks completion. |
</threat_model>

<verification>
The final gate is the exact automated command sequence in Task 3 plus zero mandatory OCR/browser skips, the exact fresh nine-pair evidence inventory and a clean execution-baseline frozen-scope/hash check. It is deliberately the one long phase-level gate outside the ≤120-second development-feedback target. Engineering screenshots supplement assertions; they never replace them.
</verification>

<success_criteria>
- Real Microsoft Edge demonstrates the complete local input/privacy/replay workflow across required states and viewports.
- Durable Phase 07 evidence contains only allowed hashes/status facts and remains reproducible.
- All high threats are blocked and all existing offline product regressions remain green.
- Phase 08 Dify, Phase 09 evaluation, Phase 10 media and historical evidence remain frozen.
</success_criteria>

<output>
After completion, create `.planning/phases/07-real-input-privacy-ui/07-05-SUMMARY.md`.
</output>
