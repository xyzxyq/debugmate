---
phase: 03-traceable-diagnosis-workflow
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/debugmate/diagnosis/extraction.py
  - src/debugmate/diagnosis/providers.py
  - src/debugmate/diagnosis/correction.py
  - tests/fixtures/diagnosis/extraction_candidates.json
  - tests/diagnosis/test_extraction_providers.py
  - tests/diagnosis/test_extraction_correction.py
  - tests/diagnosis/test_ocr_extraction_smoke.py
  - tests/diagnosis/test_vlm_extraction_cloud.py
autonomous: true
requirements: [INP-02, DIAG-06]
must_haves:
  truths:
    - "Text, OCR and VLM values enter the workflow only as candidates with stable field/source/locator metadata."
    - "A production ExtractionProvider accepts ApprovedRedactedInput, resolves and hash-verifies its approved screenshot, invokes an injected local OcrBackend and optional VLM candidate provider, and returns candidates only."
    - "Local normalization, type/locator checks and privacy rescan occur before any candidate becomes a fact draft."
    - "A correction overlay creates an immutable new CaseFacts revision under the same case_id and never overwrites the original extraction."
    - "Real OCR remains marker-isolated; default tests use fictional deterministic candidates without model or network access."
  artifacts:
    - path: "src/debugmate/diagnosis/extraction.py"
      provides: "Candidate extraction and local fact normalization"
      contains: "class ExtractionRecord"
    - path: "src/debugmate/diagnosis/providers.py"
      provides: "Production approved-input extraction chain composed from local OCR and optional VLM candidate ports"
      contains: "class ProductionExtractionProvider"
    - path: "src/debugmate/diagnosis/correction.py"
      provides: "Optimistic-lock correction overlay and revision creation"
      contains: "def apply_correction("
    - path: "tests/diagnosis/test_extraction_correction.py"
      provides: "Provenance, privacy and immutable correction tests"
      contains: "test_correction_creates_new_revision"
  key_links:
    - from: "src/debugmate/diagnosis/providers.py::ProductionExtractionProvider"
      to: "src/debugmate/diagnosis/workflow.py::DiagnosisWorkflow"
      via: "workflow receives only ApprovedRedactedInput and delegates screenshot resolution/hash verification plus OCR/VLM candidate extraction to the provider"
      pattern: "ProductionExtractionProvider.*ApprovedRedactedInput"
    - from: "src/debugmate/diagnosis/correction.py::apply_correction"
      to: "src/debugmate/diagnosis/workflow.py::rerun"
      via: "new immutable CaseFacts revision and facts_sha256 become the sole rerun input"
      pattern: "revision.*facts_sha256"
---

<objective>
Turn approved redacted text/screenshot-derived candidates into auditable facts and expose a safe correction/revision boundary.

Purpose: satisfy screenshot field extraction and user correction semantics without trusting OCR/VLM output or building Phase 4 UI.
Output: candidate/fact records, normalization and privacy gate, immutable correction overlays, offline fixtures and marker-isolated OCR smoke coverage.
</objective>

<execution_context>
@.planning/phases/03-traceable-diagnosis-workflow/03-CONTEXT.md
@.planning/phases/03-traceable-diagnosis-workflow/03-RESEARCH.md
@.planning/phases/03-traceable-diagnosis-workflow/03-VALIDATION.md
@.planning/phases/02-knowledge-input-safety/02-CONTEXT.md
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [approved redacted input, screenshot hash, candidate provenance, correction history]
trust_boundaries: [OCR/VLM to candidate, candidate to fact draft, user correction to new revision]
threats:
  - id: T3-03
    severity: high
    vector: "OCR/VLM injects fields, hidden instructions, invalid bbox values or sensitive replacements."
    control: "allowlisted field IDs, locator/image-hash validation, local normalization, privacy rescan and correction optimistic lock"
</threat_model>

<tasks>
<task type="auto" id="03-02-01">
<name>Task 1: Wave 0 production OCR/VLM extraction chain and provenance normalization</name>
<files>
src/debugmate/diagnosis/extraction.py
src/debugmate/diagnosis/providers.py
tests/fixtures/diagnosis/extraction_candidates.json
tests/diagnosis/test_extraction_providers.py
tests/diagnosis/test_extraction_correction.py
tests/diagnosis/test_ocr_extraction_smoke.py
tests/diagnosis/test_vlm_extraction_cloud.py
</files>
<read_first>
.planning/phases/03-traceable-diagnosis-workflow/03-CONTEXT.md
.planning/phases/03-traceable-diagnosis-workflow/03-RESEARCH.md
.planning/phases/03-traceable-diagnosis-workflow/03-VALIDATION.md
src/debugmate/contracts.py
src/debugmate/privacy/models.py
src/debugmate/privacy/ocr.py
src/debugmate/privacy/image_models.py
src/debugmate/privacy/output_scan.py
src/debugmate/gateway.py
src/debugmate/diagnosis/extraction.py
src/debugmate/diagnosis/providers.py
tests/fixtures/diagnosis/extraction_candidates.json
tests/diagnosis/test_extraction_providers.py
tests/diagnosis/test_extraction_correction.py
tests/diagnosis/test_ocr_extraction_smoke.py
tests/diagnosis/test_vlm_extraction_cloud.py
tests/privacy/test_rapidocr_smoke.py
</read_first>
<action>
Use strict TDD: first add fictional `tests/fixtures/diagnosis/extraction_candidates.json`, `tests/diagnosis/test_extraction_providers.py`, and failing tests that pass only a strict `ApprovedRedactedInput` into the production provider. Inject a fake `OcrBackend` that returns synthetic `list[OcrToken]` and a fake optional `VlmCandidateProvider`; assert the production chain resolves the approved screenshot under its configured redacted root, verifies the file SHA-256, calls OCR with that verified path, calls VLM only when explicitly configured, and merges candidate-only results. Cover local mapping into all six fields (`exception_type`, `traceback_key_line`, `package`, `version`, `device`, `path`), text spans, OCR four-point bbox + image SHA-256, optional VLM candidate provenance, deterministic ordering/IDs, invalid/unknown field rejection, out-of-bounds bbox rejection, screenshot hash mismatch, prompt-injection text treated as value, privacy rescan failure, OCR failure, and VLM unavailable/failure behavior without silently manufacturing facts.

Add `tests/diagnosis/test_ocr_extraction_smoke.py` with `@pytest.mark.ocr`; when explicitly selected it must instantiate the real RapidOCR-backed `OcrBackend`, run the same `ProductionExtractionProvider` path over a synthetic redacted screenshot, and assert hash-bound OCR candidates, while skipping cleanly when real OCR assets are unavailable. Add `tests/diagnosis/test_vlm_extraction_cloud.py` with `@pytest.mark.cloud`; when explicitly selected it must configure the real VLM candidate provider behind the approved-input/cloud boundary, run the same production provider chain, identify the live backend, and assert the provider returns untrusted candidates rather than `CaseFacts`. It must skip without configured cloud/VLM credentials. Neither smoke test may run in the default suite.

Implement `src/debugmate/diagnosis/providers.py` with an `ExtractionProvider` protocol whose workflow-facing call accepts only `ApprovedRedactedInput`. Add `ProductionExtractionProvider(redacted_root, ocr_backend, vlm_candidate_provider=None)`: it safely resolves the approved relative screenshot path, rehashes bytes against `redacted_screenshot_sha256`, invokes the injected `OcrBackend` itself, consumes `OcrToken.text/box/score`, and optionally invokes `VlmCandidateProvider` only through an explicitly configured approved-input boundary. The VLM port accepts only approved redacted content and returns untrusted `FactCandidate` values. Deterministically map terminal token/line patterns to the six allowlisted field IDs. Dependency injection must keep offline tests deterministic and prevent RapidOCR/cloud initialization by default, but the production chain—not the workflow caller—owns OCR/VLM invocation. Never promote provider output directly to facts.

Implement `src/debugmate/diagnosis/extraction.py` with exact allowlisted field IDs `exception_type`, `traceback_key_line`, `package`, `version`, `device`, `path`; source kinds `text`, `ocr`, `vlm`, `user`; discriminated text/OCR/VLM locators; `FactCandidate`, `ExtractionRecord`, `CaseFact`, and immutable `CaseFacts`. Merge text/OCR/optional-VLM candidates only after local field mapping, normalization, strict type/locator validation and privacy rescan. Stable IDs and `facts_sha256` must derive from canonical JSON containing only redacted normalized values and validated locators, never list indexes or random UUIDs. OCR score remains extraction confidence only. Candidate content never controls field lists, routing, tools or instructions. Run privacy `assert_export_safe` (or a narrower existing safe scan with identical fail-closed behavior) before creating facts. Run tests GREEN.
</action>
<acceptance_criteria>
- `src/debugmate/diagnosis/extraction.py` contains all six exact field IDs and source kinds `text`, `ocr`, `vlm`, `user`.
- `src/debugmate/diagnosis/providers.py` contains `class ExtractionProvider`, `class ProductionExtractionProvider`, and `class VlmCandidateProvider`; the workflow-facing provider method accepts only `ApprovedRedactedInput`, while OCR/VLM dependencies are constructor-injected.
- Offline provider tests prove `ProductionExtractionProvider` invokes a fake `OcrBackend` on the verified approved screenshot and can produce each of the six fields from its synthetic OCR tokens; a fake optional VLM provider is invoked only when configured and remains candidate-only.
- A screenshot hash mismatch, unknown field mapping or invalid bbox fails before `ExtractionRecord` creation.
- The fictional extraction fixture contains no email, token, real username or unredacted absolute personal path.
- Stable candidate/fact IDs are unchanged across repeated runs and do not use list indexes or random UUIDs.
- OCR locators require the approved image SHA-256 and an in-bounds four-point bbox; prompt-like OCR text cannot change the allowed field set.
- `tests/diagnosis/test_ocr_extraction_smoke.py` contains `pytest.mark.ocr`, exercises the real OCR backend through `ProductionExtractionProvider` when explicitly selected, and `pytest -q` deselects it by default.
- `tests/diagnosis/test_vlm_extraction_cloud.py` contains `pytest.mark.cloud`, exercises the live VLM provider through the same production chain when configured, skips without live configuration, and asserts VLM output is candidate-only.
- Focused extraction tests exit 0 without network or OCR model initialization.
</acceptance_criteria>
<done>Offline provider tests pass with all six fields, verified screenshot path/hash, optional VLM isolation and no real OCR/network initialization; explicit `ocr`/`cloud` smoke tests remain deselected by default.</done>
<verify>
<automated>.\.venv\Scripts\python.exe -m pytest -q tests\diagnosis\test_extraction_providers.py tests\diagnosis\test_extraction_correction.py -k "extraction or provenance or normalization or privacy"</automated>
</verify>
</task>

<task type="auto" id="03-02-02">
<name>Task 2: Correction overlay, revision lineage and rerun input</name>
<files>
src/debugmate/diagnosis/correction.py
tests/diagnosis/test_extraction_correction.py
</files>
<read_first>
.planning/phases/03-traceable-diagnosis-workflow/03-CONTEXT.md
.planning/REQUIREMENTS.md
src/debugmate/diagnosis/extraction.py
src/debugmate/diagnosis/correction.py
src/debugmate/contracts.py
src/debugmate/privacy/output_scan.py
src/debugmate/hashing.py
tests/diagnosis/test_extraction_correction.py
</read_first>
<action>
Continue strict TDD by adding failing tests for `CorrectionOverlay(case_id, base_revision, base_facts_sha256, field_id, fact_id, old_value_sha256, replacement, reason)`, stale base revision/hash rejection, missing or mismatched stable target IDs, display-text-only target rejection, unknown field rejection, privacy-leaking replacement rejection, no-op correction rejection, deterministic applied correction ID, unchanged `case_id`, incremented revision, changed facts hash and unchanged original `CaseFacts` bytes.

Implement `src/debugmate/diagnosis/correction.py` with `apply_correction(base: CaseFacts, overlay: CorrectionOverlay) -> CaseFacts`. Require both allowlisted `field_id` and existing `fact_id` to identify the target, verify that they refer to the same base fact, and reject natural-language/display labels as identifiers. Validate correction replacements through the same normalization/type/privacy gate as extracted facts, record old/new value hashes rather than raw pre-redaction secrets, append immutable correction provenance with `source_kind=user`, and create a new revision. Do not call a backend, retriever or generator here; return only the validated facts snapshot that Plan 04 can rerun. Run the complete extraction/correction file GREEN.
</action>
<acceptance_criteria>
- `src/debugmate/diagnosis/correction.py` contains `class CorrectionOverlay` and `def apply_correction(`.
- A stale `base_revision` or `base_facts_sha256` raises a typed correction conflict before mutation.
- Missing, unknown or mismatched `field_id`/`fact_id` targets are rejected; display text alone can never select a correction target.
- Successful correction keeps `case_id`, increments revision by exactly one, changes `facts_sha256`, and leaves serialized base facts unchanged.
- Correction provenance stores stable IDs and before/after hashes; it does not store an unvalidated replacement outside the new fact.
- Sensitive or prompt-injection replacement values are scanned as untrusted data and cannot change workflow configuration.
- `tests/diagnosis/test_extraction_correction.py` exits 0.
</acceptance_criteria>
<done>Correction tests pass and prove stale/mismatched overlays fail before mutation while one valid overlay creates exactly revision+1 with a new facts hash and byte-identical preserved base facts.</done>
<verify>
<automated>.\.venv\Scripts\python.exe -m pytest -q tests\diagnosis\test_extraction_correction.py</automated>
</verify>
</task>
</tasks>

<verification>
- `.\.venv\Scripts\python.exe -m pytest -q tests\diagnosis\test_extraction_correction.py` passes offline.
- `.\.venv\Scripts\python.exe -m pytest -q -m "not cloud and not ocr"` remains green.
- `.\.venv\Scripts\python.exe -m ruff check src\debugmate\diagnosis tests\diagnosis` exits 0.
- No Gradio field editor, report, PNG, MP3, TTS or command execution path is introduced.
</verification>

<success_criteria>
- Six key field types from text/OCR/VLM retain exact provenance and confidence.
- Invalid or sensitive candidates fail before becoming facts.
- Corrections create auditable new revisions suitable for a later rerun API.
</success_criteria>

<output>Create `.planning/phases/03-traceable-diagnosis-workflow/03-02-SUMMARY.md`.</output>
