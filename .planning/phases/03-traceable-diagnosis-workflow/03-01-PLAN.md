---
phase: 03-traceable-diagnosis-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/debugmate/contracts.py
  - src/debugmate/diagnosis/__init__.py
  - src/debugmate/diagnosis/migrations.py
  - contracts/diagnosis-record-v1.1.schema.json
  - fixtures/cases/module_not_found/diagnosis.json
  - tests/diagnosis/__init__.py
  - tests/diagnosis/test_contract_v11.py
  - tests/diagnosis/test_command_safety.py
  - tests/test_contracts.py
autonomous: true
requirements: [SAFE-04, DIAG-02, DIAG-03, DIAG-04]
must_haves:
  truths:
    - "DiagnosisRecord 1.1.0 separates facts, evidence anchors, support links, inference, missing information and limitations with stable IDs."
    - "Every grounded root-cause candidate references existing fact and evidence IDs; ungrounded claims can only be inference."
    - "DiagnosisRecord 1.0.0 remains loadable through an explicit legacy type and migrates without inventing support links."
    - "Command recommendations are inert data with platform, impact, expected result and rollback; unsafe shell constructs reject the whole record."
  artifacts:
    - path: "contracts/diagnosis-record-v1.1.schema.json"
      provides: "Committed current DiagnosisRecord JSON Schema"
      contains: '"const":"1.1.0"'
    - path: "src/debugmate/diagnosis/migrations.py"
      provides: "Pure v1.0.0 to v1.1.0 migration"
      contains: "def migrate_v100_to_v110("
    - path: "tests/diagnosis/test_contract_v11.py"
      provides: "Strict graph, schema drift and migration proofs"
      contains: "test_v100_migration"
  key_links:
    - from: "src/debugmate/contracts.py::diagnosis_schema"
      to: "contracts/diagnosis-record-v1.1.schema.json"
      via: "canonical sorted JSON emitted by diagnosis_schema() is byte-compared with the committed v1.1 snapshot"
      pattern: "diagnosis_schema\\(\\).*diagnosis-record-v1\\.1\\.schema\\.json"
    - from: "src/debugmate/diagnosis/migrations.py::migrate_v100_to_v110"
      to: "src/debugmate/contracts.py::DiagnosisRecordV100/DiagnosisRecord"
      via: "the migration accepts only the frozen legacy model and returns the strict current model"
      pattern: "def migrate_v100_to_v110\\(record: DiagnosisRecordV100\\) -> DiagnosisRecord"
---

<objective>
Migrate the platform-independent diagnosis contract from frozen 1.0.0 to current 1.1.0 before workflow logic is added.

Purpose: make fact/evidence support explicit and machine-verifiable while retaining a safe, one-way legacy loader.
Output: strict Pydantic contracts, committed Schema 1.1 snapshot, migration function, updated baseline fixtures and command safety policy.
</objective>

<execution_context>
@.planning/phases/03-traceable-diagnosis-workflow/03-CONTEXT.md
@.planning/phases/03-traceable-diagnosis-workflow/03-RESEARCH.md
@.planning/phases/03-traceable-diagnosis-workflow/03-VALIDATION.md
@contracts/diagnosis-record-v1.schema.json
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [diagnosis contract, fact graph, citation graph, command recommendations]
trust_boundaries: [legacy fixture to migration, model JSON to strict contract, command text to downstream display]
threats:
  - id: T3-01
    severity: high
    vector: "Extra fields, coerced values, dangling IDs or forged evidence make an unsupported claim appear grounded."
    control: "strict Pydantic, extra=forbid, cross-reference validators and golden Schema/hash tests"
  - id: T3-02
    severity: high
    vector: "A suggested command embeds chaining, substitution, destructive deletion or download-and-execute behavior."
    control: "deny unsafe command tokens, require metadata, expose no execution method or subprocess dependency"
</threat_model>

<tasks>
<task type="auto" id="03-01-01">
<name>Task 1: Wave 0 contract tests and DiagnosisRecord 1.1 migration</name>
<files>
src/debugmate/contracts.py
src/debugmate/diagnosis/__init__.py
src/debugmate/diagnosis/migrations.py
contracts/diagnosis-record-v1.1.schema.json
fixtures/cases/module_not_found/diagnosis.json
tests/diagnosis/__init__.py
tests/diagnosis/test_contract_v11.py
tests/test_contracts.py
</files>
<read_first>
.planning/phases/03-traceable-diagnosis-workflow/03-CONTEXT.md
.planning/phases/03-traceable-diagnosis-workflow/03-RESEARCH.md
.planning/phases/03-traceable-diagnosis-workflow/03-VALIDATION.md
src/debugmate/contracts.py
src/debugmate/diagnosis/__init__.py
src/debugmate/diagnosis/migrations.py
contracts/diagnosis-record-v1.schema.json
contracts/diagnosis-record-v1.1.schema.json
src/debugmate/adapters/base.py
src/debugmate/adapters/fixture.py
fixtures/cases/module_not_found/diagnosis.json
tests/test_contracts.py
tests/diagnosis/__init__.py
tests/diagnosis/test_contract_v11.py
</read_first>
<action>
Use strict TDD: first create `tests/diagnosis/test_contract_v11.py` with failing tests for extra fields, wrong primitive types, duplicate IDs, dangling fact/evidence links, grounded candidates without evidence, inference candidates with applicability/limits, Schema snapshot equality, frozen 1.0 rejection of 1.1 fields, and `migrate_v100_to_v110()` golden output; run it and record the expected RED failure before production edits.

In `src/debugmate/contracts.py`, preserve `DiagnosisRecordV100` with `schema_version: Literal["1.0.0"]`, set current `SCHEMA_VERSION = "1.1.0"`, and define strict current records with these serialized concepts: `ObservedFact(fact_id, field_id, value, source_kind, confidence, locator)`, `EvidenceAnchor(evidence_id, chunk_id, content_summary, source_id, source_url, locator, relevance_score, knowledge_build_id)`, `SupportLink(fact_ids, evidence_ids, support_type)`, and `RootCauseCandidate(candidate_id, cause, claim_kind grounded|inference, fact_ids, evidence_ids, confidence, applicability, counterevidence_or_limits)`. `DiagnosisRecord` must retain category, missing information, checks, fixes, verification steps, overall confidence, limitations and recap text, but facts/evidence/support must use stable IDs and model-level graph validation. Keep `diagnosis_schema()` and `schema_sha256()` pointed only at 1.1.0.

Create `src/debugmate/diagnosis/migrations.py` with pure `migrate_v100_to_v110(record: DiagnosisRecordV100) -> DiagnosisRecord`. IDs must be canonical SHA-256-derived non-sensitive identifiers, migration must be deterministic, and legacy candidates whose fact/citation relationship cannot be proven must become `claim_kind="inference"` with empty evidence IDs rather than guessed links. Generate and commit `contracts/diagnosis-record-v1.1.schema.json` from `diagnosis_schema()`; do not modify the frozen `contracts/diagnosis-record-v1.schema.json`. Upgrade the checked-in module-not-found diagnosis fixture and contract tests to current 1.1 while retaining explicit legacy migration coverage. Adapter/CLI compatibility is finalized in Plan 03-04/03-05 after their candidate/workflow boundaries exist. Then run GREEN tests.
</action>
<acceptance_criteria>
- `src/debugmate/contracts.py` contains `SCHEMA_VERSION = "1.1.0"`, `class DiagnosisRecordV100`, `class EvidenceAnchor`, `class SupportLink`, and `class DiagnosisRecord`.
- `src/debugmate/diagnosis/migrations.py` contains `def migrate_v100_to_v110(` and does not contain `uuid.uuid4` or `new_case_id`.
- `contracts/diagnosis-record-v1.schema.json` still contains `"const":"1.0.0"`; `contracts/diagnosis-record-v1.1.schema.json` contains `"const":"1.1.0"`.
- Contract validators reject duplicate/dangling IDs and reject a grounded root cause with no evidence anchor.
- Migration tests prove identical input produces byte-identical canonical 1.1 JSON and unsupported legacy relationships are marked `inference`.
- `tests/diagnosis/test_contract_v11.py` and `tests/test_contracts.py` exit 0.
</acceptance_criteria>
<done>Focused contract tests pass; the committed 1.1 Schema is byte-equivalent to canonical `diagnosis_schema()` output; the frozen 1.0 Schema remains unchanged; identical legacy input migrates to byte-identical 1.1 JSON.</done>
<verify>
<automated>.\.venv\Scripts\python.exe -m pytest -q tests\diagnosis\test_contract_v11.py tests\test_contracts.py</automated>
</verify>
</task>

<task type="auto" id="03-01-02">
<name>Task 2: Wave 0 command-as-data safety policy</name>
<files>
src/debugmate/contracts.py
tests/diagnosis/test_command_safety.py
</files>
<read_first>
.planning/PROJECT.md
.planning/REQUIREMENTS.md
.planning/phases/03-traceable-diagnosis-workflow/03-CONTEXT.md
.planning/phases/03-traceable-diagnosis-workflow/03-RESEARCH.md
src/debugmate/contracts.py
tests/diagnosis/test_command_safety.py
src/debugmate/cli.py
src/debugmate/adapters/base.py
tests/diagnosis/test_contract_v11.py
</read_first>
<action>
Use strict TDD: write failing `tests/diagnosis/test_command_safety.py` cases proving valid single `CommandStep` values remain serializable data and rejecting blank metadata, newline-separated commands, `&&`, `||`, `;`, pipe-to-shell, PowerShell/cmd command substitution, recursive deletion, format-disk and download-and-execute patterns. Scope the AST/import scan only to command-execution capability: imports or calls of `subprocess`, `os.system`, PowerShell/shell launch APIs, `Popen`, `Start-Process`, `Invoke-Expression`, or equivalent shell entry points. Do not reject generic domain methods named `run` or `execute` elsewhere in the workflow.

Implement a deterministic `CommandStep` validator in `src/debugmate/contracts.py`. Require non-blank `command`, `platform`, `impact`, `expected_result`, and `rollback`; accepted platform values must be an explicit enum such as `windows_powershell`, `windows_cmd`, `linux_bash`, `python`, or `platform_agnostic`. Reject the entire `DiagnosisRecord` for forbidden command patterns. The policy may inspect command text but must never invoke or normalize through a shell. Run the focused test GREEN and rerun contract tests to ensure Schema output remains synchronized.
</action>
<acceptance_criteria>
- `tests/diagnosis/test_command_safety.py` proves at least one valid command per supported platform and rejects chaining, substitution, destructive deletion and download-execute cases.
- `CommandStep` exposes validation/serialization only and contains no method that invokes a command or shell.
- An AST/import test rejects imports/calls of `subprocess`, `os.system`, PowerShell, or shell launch APIs in `CommandStep` and command-handling call sites without flagging unrelated workflow `run`/`execute` method names.
- A record containing one unsafe command fails validation as a whole; no partial record is returned.
- Focused command safety and contract tests exit 0.
</acceptance_criteria>
<done>Focused tests prove valid `CommandStep` values are inert data, every listed unsafe construct rejects the whole record, and the scoped AST/import guard finds zero command-execution imports or calls.</done>
<verify>
<automated>.\.venv\Scripts\python.exe -m pytest -q tests\diagnosis\test_command_safety.py tests\diagnosis\test_contract_v11.py</automated>
</verify>
</task>
</tasks>

<verification>
- `.\.venv\Scripts\python.exe -m pytest -q tests\diagnosis\test_contract_v11.py tests\diagnosis\test_command_safety.py` passes offline.
- `.\.venv\Scripts\python.exe -m ruff check src\debugmate\contracts.py src\debugmate\diagnosis tests\diagnosis` exits 0.
- A generated Schema comparison proves the committed 1.1 file equals `diagnosis_schema()` canonical JSON.
- No UI, report, PNG, MP3, TTS or command execution code is introduced.
</verification>

<success_criteria>
- Current 1.1 diagnosis objects can express exact fact/evidence support and reject forged graphs.
- Legacy 1.0 data has a deterministic, conservative migration path.
- Unsafe command recommendations fail closed and remain non-executable data.
</success_criteria>

<output>Create `.planning/phases/03-traceable-diagnosis-workflow/03-01-SUMMARY.md`.</output>
