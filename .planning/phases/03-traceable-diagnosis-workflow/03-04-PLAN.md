---
phase: 03-traceable-diagnosis-workflow
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - src/debugmate/adapters/base.py
  - src/debugmate/adapters/fixture.py
  - src/debugmate/adapters/dify.py
  - src/debugmate/diagnosis/generation.py
  - tests/diagnosis/test_generation_repair.py
  - tests/diagnosis/test_dify_diagnosis_cloud.py
  - tests/test_fixture_adapter.py
  - platform/dify/README.md
autonomous: true
requirements: [SAFE-04, DIAG-02, DIAG-03, DIAG-04, DIAG-05]
must_haves:
  truths:
    - "Adapters return untrusted bounded candidate envelopes; only the local generator can publish a validated DiagnosisRecord."
    - "A repairable invalid candidate receives exactly one controlled repair; a second failure becomes generation_failed without partial output."
    - "Transport retries and contract repair attempts are separate counters, and external raw bodies/reasoning never cross the adapter boundary."
  artifacts:
    - path: "src/debugmate/diagnosis/generation.py"
      provides: "Strict candidate validation and one-repair budget"
      contains: "MAX_REPAIR_ATTEMPTS = 1"
    - path: "src/debugmate/adapters/base.py"
      provides: "Candidate-only backend port"
      contains: "class CandidateRunResult"
    - path: "tests/diagnosis/test_generation_repair.py"
      provides: "First-pass, repair and typed-failure proofs"
      contains: "generation_failed"
  key_links:
    - from: "src/debugmate/diagnosis/evidence_binding.py::EvidenceAnchor"
      to: "src/debugmate/diagnosis/generation.py::DiagnosisGenerator"
      via: "GenerationRequest carries only validated fact IDs, bound evidence IDs, routing and schema/build versions"
      pattern: "evidence_ids.*GenerationRequest"
    - from: "src/debugmate/adapters/base.py::CandidateRunResult"
      to: "src/debugmate/diagnosis/generation.py::DiagnosisGenerator"
      via: "untrusted candidate payload is locally parsed, schema/semantic/privacy/command validated and optionally repaired once"
      pattern: "CandidateRunResult.*DiagnosisGenerator"
---

<objective>
Narrow fixture/Dify adapters to candidate-only transport and implement local strict generation with exactly one controlled repair.

Purpose: isolate untrusted model/platform output from the publishable DiagnosisRecord boundary before workflow orchestration is added.
Output: candidate port, fixture/Dify adapters, local generator, repair/failure contracts, offline tests and marker-isolated Dify smoke coverage.
</objective>

<execution_context>
@.planning/phases/03-traceable-diagnosis-workflow/03-CONTEXT.md
@.planning/phases/03-traceable-diagnosis-workflow/03-RESEARCH.md
@.planning/phases/03-traceable-diagnosis-workflow/03-VALIDATION.md
@.planning/phases/02-knowledge-input-safety/02-03-SUMMARY.md
@platform/dify/README.md
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [candidate payload, repair budget, DiagnosisRecord boundary, safe failure details]
trust_boundaries: [Dify/fixture transport to candidate envelope, candidate envelope to local generator]
threats:
  - id: T3-07
    severity: high
    vector: "Malformed output, forged support, sensitive content or repeated repair attempts creates a synthetic success."
    control: "strict local schema/semantic/privacy/command validation, bounded issue codes, exactly one repair and typed failure"
</threat_model>

<tasks>
<task type="auto" id="03-04-01">
<name>Task 1: Candidate adapter port and exactly-one controlled repair</name>
<files>
src/debugmate/adapters/base.py
src/debugmate/adapters/fixture.py
src/debugmate/adapters/dify.py
src/debugmate/diagnosis/generation.py
tests/diagnosis/test_generation_repair.py
tests/diagnosis/test_dify_diagnosis_cloud.py
tests/test_fixture_adapter.py
platform/dify/README.md
</files>
<read_first>
.planning/phases/03-traceable-diagnosis-workflow/03-CONTEXT.md
.planning/phases/03-traceable-diagnosis-workflow/03-RESEARCH.md
.planning/phases/03-traceable-diagnosis-workflow/03-VALIDATION.md
src/debugmate/contracts.py
src/debugmate/adapters/base.py
src/debugmate/adapters/fixture.py
src/debugmate/adapters/dify.py
src/debugmate/diagnosis/evidence_binding.py
src/debugmate/privacy/output_scan.py
tests/test_fixture_adapter.py
platform/dify/README.md
</read_first>
<action>
Use strict TDD: add failing tests for first-pass success, JSON parse failure, strict type/Schema failure, semantic case/routing/fact/evidence/build mismatch, unsafe CommandStep, privacy failure, non-repairable failure, one successful repair, a second invalid candidate, transport retry independence and raw-body exclusion. Add `tests/diagnosis/test_dify_diagnosis_cloud.py` with `@pytest.mark.cloud`; it skips without credentials/app configuration and is excluded from the default suite.

Replace adapter publication of `DiagnosisRecord` with bounded `CandidateRunResult(run_id, backend, candidate_payload)`. Dify may unwrap only the documented output JSON value/string and catch expected transport-envelope/JSON/key/type errors; it must never return/log full bodies, headers, secrets, reasoning or chain-of-thought. Implement strict `GenerationRequest` and `DiagnosisGenerator` with `MAX_REPAIR_ATTEMPTS = 1`. Validate current 1.1 Schema plus case ID, final route, fact/evidence support graph, expected knowledge build, CommandStep policy and privacy scan. Repair requests contain only Schema version, bounded issue codes and JSON pointers, and the already-redacted candidate; attempt 2 is final. A second failure returns `status="generation_failed"`, safe issues/completed stages/retry scope, and no partial diagnosis. Keep HTTP transport retry count independent. Update fixtures, adapter tests and Dify documentation, then run GREEN.
</action>
<acceptance_criteria>
- `generation.py` contains `MAX_REPAIR_ATTEMPTS = 1`, `GenerationRequest` and `DiagnosisGenerator`.
- Adapters expose `CandidateRunResult` and never construct or publish `DiagnosisRecord`.
- One repairable invalid candidate causes exactly two generation calls; a second invalid candidate yields only `generation_failed`.
- Repair details contain bounded codes/JSON pointers and no body, headers, secret values or provider reasoning.
- Tests independently assert transport retry count and contract repair count.
- `pytest.mark.cloud` is excluded by default and skips without live configuration.
- Focused generation and adapter tests exit 0 offline.
</acceptance_criteria>
<done>Focused tests pass with one-call success, exactly-two-call repair success, typed second-failure, independent retry counters, and zero raw provider-body/reasoning leakage.</done>
<verify>
<automated>.\.venv\Scripts\python.exe -m pytest -q tests\diagnosis\test_generation_repair.py tests\test_fixture_adapter.py</automated>
</verify>
</task>
</tasks>

<verification>
- `.\.venv\Scripts\python.exe -m pytest -q tests\diagnosis\test_generation_repair.py tests\test_fixture_adapter.py` passes offline.
- `.\.venv\Scripts\python.exe -m pytest -q -m "not cloud and not ocr"` does not initialize live Dify/OCR.
- No workflow/CLI/evidence publication, UI, report, PNG, MP3, TTS or command execution is introduced.
</verification>

<success_criteria>
- Backend output is only an untrusted bounded candidate envelope.
- Local generation publishes a diagnosis only after all schema, support, privacy and command checks pass.
- Repair is capped at one and failure remains explicit and non-fabricated.
</success_criteria>

<output>Create `.planning/phases/03-traceable-diagnosis-workflow/03-04-SUMMARY.md`.</output>
