---
phase: 03-traceable-diagnosis-workflow
plan: 06
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - src/debugmate/evidence.py
  - src/debugmate/diagnosis/workflow.py
  - src/debugmate/cli.py
  - tests/diagnosis/test_workflow_evidence.py
  - tests/diagnosis/test_workflow_e2e.py
autonomous: true
requirements: [INP-02, INP-03, SAFE-04, DIAG-01, DIAG-02, DIAG-03, DIAG-04, DIAG-05, DIAG-06]
must_haves:
  truths:
    - "Each outcome publishes only privacy-scanned allowlisted JSON/text artifacts with manifest hashes and explicit fixture/Dify backend identity."
    - "RetrievalTrace identity and EvidenceAnchor support stay bound through generation into the published bundle."
    - "Correction rerun preserves both old and new evidence directories with distinct revision/run/facts hashes."
    - "The full offline gate is blocking and includes v1.1 JSON Schema validation of every committed diagnosis fixture plus a tracked product/fixture secret scan; real Dify/VLM/OCR tests remain marker-isolated non-blocking external gates."
  artifacts:
    - path: "src/debugmate/evidence.py"
      provides: "Atomic Phase 3 outcome evidence publication"
      contains: "manifest.json"
    - path: "tests/diagnosis/test_workflow_evidence.py"
      provides: "Bundle allowlist, lineage, privacy and atomicity proofs"
      contains: "test_corrected_runs_preserve_both_bundles"
  key_links:
    - from: "src/debugmate/knowledge/retrieval.py::RetrievalTrace"
      to: "src/debugmate/diagnosis/evidence_binding.py::bind_retrieval_evidence"
      via: "validated build/source/locator anchors feed DiagnosisGenerator and their IDs/versions are retained in evidence"
      pattern: "RetrievalTrace.*EvidenceAnchor.*DiagnosisGenerator"
    - from: "src/debugmate/diagnosis/correction.py::CaseFacts revision"
      to: "src/debugmate/evidence.py::EvidenceBundle"
      via: "rerun creates a new run directory while the previous manifest and artifact hashes remain immutable"
      pattern: "revision.*run_id.*manifest"
---

<objective>
Publish immutable privacy-safe workflow evidence and run the complete Phase 3 offline quality gates.

Purpose: make every success/early-stop/failure/correction outcome independently auditable without raw external payloads or Phase 4 artifacts.
Output: atomic evidence bundles, manifest lineage, correction preservation proofs and full offline verification.
</objective>

<execution_context>
@.planning/phases/03-traceable-diagnosis-workflow/03-CONTEXT.md
@.planning/phases/03-traceable-diagnosis-workflow/03-RESEARCH.md
@.planning/phases/03-traceable-diagnosis-workflow/03-VALIDATION.md
@.planning/phases/02-knowledge-input-safety/02-03-SUMMARY.md
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [evidence bundle, manifest hashes, backend identity, old/new correction lineage]
trust_boundaries: [workflow outcome to publisher, validated retrieval/generation to evidence, temporary directory to atomic final directory]
threats:
  - id: T3-09
    severity: high
    vector: "Raw provider bodies/chunks/secrets leak into evidence, forged anchors are published, or rerun overwrites prior evidence."
    control: "strict artifact allowlist, privacy scan, anchor revalidation, atomic publish and immutable run-specific directories"
</threat_model>

<tasks>
<task type="auto" id="03-06-01">
<name>Task 1: Atomic evidence bundles and full offline gates</name>
<files>
src/debugmate/evidence.py
src/debugmate/diagnosis/workflow.py
src/debugmate/cli.py
tests/diagnosis/test_workflow_evidence.py
tests/diagnosis/test_workflow_e2e.py
</files>
<read_first>
.planning/phases/03-traceable-diagnosis-workflow/03-CONTEXT.md
.planning/phases/03-traceable-diagnosis-workflow/03-RESEARCH.md
.planning/phases/03-traceable-diagnosis-workflow/03-VALIDATION.md
.planning/phases/02-knowledge-input-safety/02-03-SUMMARY.md
contracts/diagnosis-record-v1.1.schema.json
fixtures/cases/module_not_found/diagnosis.json
src/debugmate/evidence.py
src/debugmate/privacy/output_scan.py
src/debugmate/diagnosis/workflow.py
src/debugmate/diagnosis/evidence_binding.py
src/debugmate/diagnosis/generation.py
src/debugmate/cli.py
src/debugmate/knowledge/retrieval.py
tests/test_evidence.py
tests/diagnosis/test_workflow_evidence.py
tests/diagnosis/test_workflow_e2e.py
</read_first>
<action>
Use strict TDD: add failing evidence tests for completed, needs-information, insufficient-information and generation-failed bundles; atomic temp cleanup; backend label; run/revision/facts hash lineage; artifact SHA-256 verification; privacy failure; forged retrieval rejection; raw candidate/provider body/full chunk absence; and correction preserving both old/new bundles.

Extend EvidenceBundle only for Phase 3 text/JSON artifacts. Publish allowlisted `extraction.json`, `case-facts.json`, `sufficiency.json`, `routing.json`, `retrieval.json`, `diagnosis.json` or `failure.json`, and `manifest.json` according to completed stages. Store validated summaries/IDs/versions only. Manifest contains backend, run/case IDs, facts revision/hash, routing rule version, knowledge build ID, Schema version, generation/transport attempts, node states and artifact hashes. Revalidate RetrievalTrace/EvidenceAnchor binding and output privacy before atomic publish; failures cannot leave a normal or partial diagnosis bundle. Preserve Phase 2 MP3 rejection and create no report/PNG/audio.

Wire workflow/CLI publication, then run full offline pytest, Ruff, pip check and git diff check. As part of the same blocking automated gate, enumerate every tracked `fixtures/**/diagnosis.json`, fail if none exist, and validate each file with `jsonschema` against `contracts/diagnosis-record-v1.1.schema.json`. Then scan tracked product/config/contract/fixture/knowledge/prompt/platform/script files for sentinel secrets, plausible `sk-` keys, private-key headers and the local username. Use explicit Git pathspec exclusions only for `.planning/**` and `tests/**`, because planning records and privacy/security tests intentionally contain fictional secret strings; do not exclude `src/**`, `contracts/**`, `fixtures/**`, `knowledge/**`, `prompts/**`, `platform/**`, `scripts/**`, `README.md` or `pyproject.toml`. Marker-isolated `cloud`, `ocr` and live VLM tests may skip without credentials/assets and are not blocking Phase 3 completion; if explicitly run, they must identify the real backend and cannot substitute for offline evidence.
</action>
<acceptance_criteria>
- Completed bundles contain diagnosis/retrieval/manifest; early outcomes contain only their typed state and completed-stage artifacts.
- Manifest has backend, run/case IDs, facts revision/hash, routing version, knowledge build, Schema version, attempts, node states and artifact hashes.
- Generated bundles contain no raw_chunk, response_body, reasoning or chain_of_thought fields.
- Privacy or forged-anchor failure prevents normal publication and leaves no partial diagnosis artifact.
- Corrected rerun leaves both evidence directories verifiable and immutable with distinct identities/hashes.
- Phase 2 MP3 prohibition remains; no report, PNG or audio is emitted.
- Every committed `fixtures/**/diagnosis.json` validates against the committed v1.1 JSON Schema, and the gate fails if the fixture set is empty or any fixture is invalid.
- The tracked-file secret scan is clean across product/config/contracts/fixtures/knowledge/prompts/platform/scripts; only `.planning/**` and `tests/**` are excluded for documented intentional fictional-secret content.
- Full offline pytest, Ruff, pip check, git diff check, v1.1 fixture validation and tracked-file secret scan all exit 0.
</acceptance_criteria>
<done>All four outcome bundles and both correction-run bundles verify by manifest hash; forbidden raw fields are absent; the full offline pytest/Ruff/pip/diff/v1.1-fixture/secret-scan gate exits 0 while external markers remain optional.</done>
<verify>
<automated>.\.venv\Scripts\python.exe -m pytest -q -m "not cloud and not ocr"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m ruff check .; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m pip check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git diff --check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; $diagnosisFixtures = @(git ls-files 'fixtures/**/diagnosis.json'); if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; if ($diagnosisFixtures.Count -eq 0) { Write-Error "No committed diagnosis fixtures found"; exit 1 }; foreach ($fixture in $diagnosisFixtures) { & .\.venv\Scripts\python.exe -m jsonschema -i $fixture contracts\diagnosis-record-v1.1.schema.json; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }; git grep -n -I -E "SECRET_SENTINEL_DO_NOT_LOG|sk-[A-Za-z0-9_-]{8,}|BEGIN( [A-Z0-9]+)* PRIVATE KEY|20795" -- src contracts fixtures knowledge prompts platform scripts README.md pyproject.toml; if ($LASTEXITCODE -eq 0) { Write-Error "Potential secret found in tracked product or fixture files"; exit 1 } elseif ($LASTEXITCODE -ne 1) { exit $LASTEXITCODE }</automated>
</verify>
</task>
</tasks>

<verification>
- The task automated command passes exactly as written.
- `.\.venv\Scripts\python.exe -m pytest -q tests\diagnosis\test_workflow_evidence.py tests\diagnosis\test_workflow_e2e.py` passes focused evidence/E2E checks.
- The automated command validates every committed diagnosis fixture against `contracts/diagnosis-record-v1.1.schema.json` and scans tracked product/fixture files for secret patterns with only the documented planning/test exclusions.
- Explicit live `cloud`/`ocr`/VLM runs are external gates only and skip cleanly when unavailable.
</verification>

<success_criteria>
- Every offline workflow outcome is independently auditable and hash-verifiable.
- Reruns preserve immutable evidence history.
- Phase 3 passes all blocking offline quality gates without relying on Dify/VLM/OCR availability.
</success_criteria>

<output>Create `.planning/phases/03-traceable-diagnosis-workflow/03-06-SUMMARY.md`.</output>
