---
phase: 04-multimodal-results-ui
plan: 07
type: execute
wave: 7
depends_on: ["04-06"]
files_modified:
  - tests/results/test_result_e2e.py
  - tests/results/test_security_abuse.py
  - tests/ui/test_browser.py
  - scripts/run-phase4-browser-qa.ps1
  - evidence/ui/phase4/visual-qa.json
  - evidence/ui/phase4/VQ-01.png
  - evidence/ui/phase4/VQ-02.png
  - evidence/ui/phase4/VQ-06.png
  - evidence/ui/phase4/VQ-08.png
  - evidence/ui/phase4/VQ-10.png
  - evidence/ui/phase4/VQ-11.png
  - evidence/ui/phase4/VQ-13.png
  - evidence/media/phase4/local-sapi.json
  - .planning/phases/04-multimodal-results-ui/04-07-EXECUTION-LOG.md
  - .planning/phases/04-multimodal-results-ui/04-07-GAPS.md
autonomous: true
requirements: [MULTI-01, MULTI-02, MULTI-03, MULTI-04, MULTI-05, UX-01, UX-02, UX-03, UX-04]
must_haves:
  truths:
    - "Completed, PNG-partial, TTS-partial, failed and replay fixtures traverse source verification through disk-reverified result/UI state."
    - "The full offline suite, static/dependency/diff/schema/secret/result/ZIP gates are blocking and every T4-01..T4-14 abuse path is closed."
    - "A real local SAPI MP3 and real browser VQ-01..VQ-15 evidence exist; required screenshots use verified redacted replay/source evidence."
    - "Dify and edge external gates are reported exactly as pass/skip/open and never substituted for local or offline proof."
  artifacts:
    - path: "tests/results/test_result_e2e.py"
      provides: "Five-state source-to-result-to-restore E2E proof"
      contains: "test_completed_result_e2e"
    - path: "tests/results/test_security_abuse.py"
      provides: "T4-01 through T4-14 adversarial closure matrix"
      contains: "T4-14"
    - path: "evidence/ui/phase4/visual-qa.json"
      provides: "VQ-01 through VQ-15 browser evidence ledger"
      contains: "VQ-15"
    - path: ".planning/phases/04-multimodal-results-ui/04-07-EXECUTION-LOG.md"
      provides: "Exact command, media, browser and external-gate execution evidence for independent verification"
      contains: "VQ-15"
  key_links:
    - from: "tests/results/test_result_e2e.py"
      to: "src/debugmate/results/verifier.py::verify_result_bundle"
      via: "each scenario is reread and verified from final disk bytes before assertions"
      pattern: "verify_result_bundle.*render_view_state"
    - from: "tests/ui/test_browser.py"
      to: ".planning/phases/04-multimodal-results-ui/04-UI-SPEC.md::VQ-01..VQ-15"
      via: "real loopback Gradio render, viewport/keyboard/status assertions and screenshots"
      pattern: "VQ-01.*VQ-15"
---

<objective>
Close Phase 4 execution with end-to-end, adversarial, real-media and real-browser evidence for a separate independent GSD verifier.

Purpose: prove the complete multimodal result experience is truthful, safe, reproducible and course-demo ready on the Windows target.
Output: E2E/security suites, VQ-01..15 evidence, local SAPI proof and exact execution logs for a later independent gsd-verifier.
</objective>

<execution_context>
@.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
@.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
@.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
@.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
@docs/superpowers/specs/2026-07-13-phase4-multimodal-results-ui-design.md
@.planning/phases/04-multimodal-results-ui/04-06-SUMMARY.md
@.planning/phases/03-traceable-diagnosis-workflow/03-VERIFICATION.md
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [all Phase 4 contracts/artifacts, UI truth, downloads, replay provenance, immutable evidence]
trust_boundaries: [all T4-01..T4-14 boundaries from source through browser/download]
threats:
  - id: T4-01
    severity: high
    vector: "Forged source or artifact identity."
    control: "E2E source/identity tamper tests and strict verifier"
  - id: T4-02
    severity: high
    vector: "Traversal, symlink or reparse result escape."
    control: "abuse matrix at source/result/download boundaries"
  - id: T4-03
    severity: high
    vector: "Unsafe ZIP member or slip."
    control: "archive name/allowlist/unpack tests"
  - id: T4-04
    severity: high
    vector: "Markdown/URL/HTML injection."
    control: "adversarial report plus real-browser nonexecution inspection"
  - id: T4-05
    severity: high
    vector: "PNG metadata/frame/resource attack."
    control: "artifact mutation/oversize tests and disk reopen"
  - id: T4-06
    severity: high
    vector: "TTS secret exfiltration."
    control: "SafeRecapText boundary and no-value transport/log assertions"
  - id: T4-07
    severity: high
    vector: "Malicious or oversized audio."
    control: "real ffprobe plus malformed/size/stream/duration tests"
  - id: T4-08
    severity: high
    vector: "FFmpeg/PowerShell/diagnosis command execution injection."
    control: "AST/process argument tests and UI absence of execution controls"
  - id: T4-09
    severity: high
    vector: "Arbitrary/stale file download."
    control: "post-render tamper and strict member-ID resolver tests"
  - id: T4-10
    severity: high
    vector: "Replay mislabeled live."
    control: "three-boundary replay assertion and VQ-02"
  - id: T4-11
    severity: high
    vector: "Concurrent duplicate/TOCTOU publication."
    control: "race/interruption/idempotency tests"
  - id: T4-12
    severity: high
    vector: "Raw error/provider/path/secret disclosure."
    control: "value-free scan across manifests, UI config, logs and failures"
  - id: T4-13
    severity: high
    vector: "ZIP bomb/resource exhaustion."
    control: "member/count/total/ratio caps and CRC/unpacked verification"
  - id: T4-14
    severity: high
    vector: "Old/incomplete result restoration."
    control: "version/source/generation drift restore tests"
</threat_model>

<tasks>
<task type="auto" id="04-07-01">
<name>Task 1: Five-state offline E2E and complete threat-abuse matrix</name>
<files>
tests/results/test_result_e2e.py
tests/results/test_security_abuse.py
.planning/phases/04-multimodal-results-ui/04-07-GAPS.md
</files>
<read_first>
.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
src/debugmate/results/service.py
src/debugmate/results/verifier.py
src/debugmate/ui/presentation.py
tests/results/conftest.py
tests/results/test_publisher.py
tests/ui/test_callbacks.py
</read_first>
<action>
Write RED E2E tests for a completed ModuleNotFoundError result, PNG `png_layout_failed` partial, all-TTS `tts_failed` partial, source-invalid failed state and allowlisted replay. Each starts from strict Phase 3 outcome/source evidence, runs public service composition, rereads final result with `verify_result_bundle`, restores view state, resolves eligible download and asserts exact manifest/artifact/identity/replay/fallback/partial truth. Use fake TTS for offline completion but real ffprobe-decodable media; never label it live cloud.

Add a parameterized abuse ledger with one named test per `T4-01..T4-14`, targeting actual public boundaries. Include source/outcome/artifact identity mutation, paths/links, ZIP names/bomb, Markdown/URL/HTML, PNG metadata/frame/oversize, TTS secret/log, audio tags/size/streams/duration, SAPI argv/AST, arbitrary/TOCTOU download, replay labels, concurrency, value leakage and stale restore. This wave may add verification tests only. If any test exposes a production defect, record requirement/threat, reproducer, expected/actual and owning files in `04-07-GAPS.md`, stop the affected gate, and require a later explicit gap plan; do not modify production/CSS in 04-07.
</action>
<acceptance_criteria>
- Five scenarios traverse loader→presentation→renderers→gate→publisher→disk verifier→service/view/download.
- Completed includes report/PNG/real-decodable fake-adapter MP3/full ZIP; partial cases retain exactly valid artifacts and partial ZIP; failed publishes no deliverable result.
- Replay is labeled in state, result manifest and download/archive metadata and never claims live cloud success.
- Exactly T4-01 through T4-14 each have at least one executable public-boundary adversarial test.
- Every final bundle is reread from disk; return values alone are not used as proof.
- Prior Phase 3 evidence bundles remain byte-identical and audio fail-closed.
</acceptance_criteria>
<done>All legal result modes work end to end offline and every declared Phase 4 threat is closed by executable public-boundary evidence.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_result_e2e.py tests\results\test_security_abuse.py</automated>
</verify>
</task>

<task type="auto" id="04-07-02">
<name>Task 2: Blocking offline, schema, secret, result and deterministic ZIP gates</name>
<files>
.planning/phases/04-multimodal-results-ui/04-07-EXECUTION-LOG.md
.planning/phases/04-multimodal-results-ui/04-07-GAPS.md
</files>
<read_first>
.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
pyproject.toml
src/debugmate/results/contracts.py
src/debugmate/results/verifier.py
src/debugmate/cli.py
.planning/phases/03-traceable-diagnosis-workflow/03-VERIFICATION.md
</read_first>
<action>
Run the complete default offline pytest suite, Ruff, pip check and `git diff --check`. Export/round-trip every new strict result contract Schema (or canonical model JSON fixtures if no committed schema file is chosen) and revalidate every generated full/partial bundle through the public verifier. Build each deterministic ZIP twice in separate roots and assert byte equality. Run a tracked product/config/contract/fixture/knowledge/prompt/platform/script scan for sentinel/plausible keys, private-key headers and local username, excluding only `.planning/**`, `tests/**` and generated evidence screenshots where intentional test strings or machine evidence are documented; do not exclude `src/**`, `fixtures/**`, `contracts/**`, `scripts/**`, `pyproject.toml` or UI code.

If any gate fails, reproduce it without modifying production, record exact command/output boundary and owning requirement/files in `04-07-GAPS.md`, mark the execution log gate failed and stop dependent evidence. A later explicit gap plan owns remediation and reruns. Record successful commands/counts/HEAD, bundle IDs/hashes and threat-test mapping in `04-07-EXECUTION-LOG.md`; commit verification/evidence files only.
</action>
<acceptance_criteria>
- Full default offline pytest, Ruff, pip check and diff check exit 0.
- All strict result models reject schema/version drift and round-trip canonical current data.
- Public verifier passes fresh full/partial bundles; repeat ZIP bytes/hashes match exactly.
- Tracked production/fixture/config secret scan is clean with only documented intentional-test/evidence exclusions.
- Execution log lists exact automated counts/IDs/hashes without claiming independent phase verification.
</acceptance_criteria>
<done>All deterministic offline quality, security, identity, artifact and reproducibility gates pass from a clean current tree.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m ruff check src tests; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m pip check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git diff --check; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git grep -n -I -E "SECRET_SENTINEL_DO_NOT_LOG|sk-[A-Za-z0-9_-]{8,}|BEGIN( [A-Z0-9]+)* PRIVATE KEY|20795" -- src contracts fixtures knowledge prompts platform scripts README.md pyproject.toml; if ($LASTEXITCODE -eq 0) { Write-Error "Potential secret found in tracked product files"; exit 1 } elseif ($LASTEXITCODE -ne 1) { exit $LASTEXITCODE } else { exit 0 }</automated>
</verify>
</task>

<task type="auto" id="04-07-03">
<name>Task 3: Fresh real local media and truthful external gates</name>
<files>
evidence/media/phase4/local-sapi.json
.planning/phases/04-multimodal-results-ui/04-07-EXECUTION-LOG.md
.planning/phases/04-multimodal-results-ui/04-07-GAPS.md
</files>
<read_first>
.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
.planning/phases/04-multimodal-results-ui/04-04-SUMMARY.md
src/debugmate/results/tts/sapi.py
src/debugmate/results/media.py
tests/results/test_tts_live.py
</read_first>
<action>
Run the real Windows local `tts` marker again from the current final code, capture safe voice/backend/rate/duration/hash and listen once for silence, clipping or unintelligible Chinese. Do not store the spoken secret-bearing text or an absolute temp path. Run Dify `cloud and tts` and edge `network and tts` gates only under their existing explicit preconditions; record each as passed, failed or clean-skipped/open. A remote skip cannot satisfy the local SAPI gate, and no credential purchase/configuration is authorized.

If local SAPI or ffprobe fails, treat it as blocking: preserve the failing verification result and write a value-free gap entry with reproducer/expected/actual/owning files; do not change adapter/script/media code in this wave. A later explicit gap plan owns remediation. On success, write value-free `evidence/media/phase4/local-sapi.json` and external gate table.
</action>
<acceptance_criteria>
- Current-code local SAPI→FFmpeg MP3 passes real probe as mono, decodable and inclusive 30–60 seconds.
- One listening check records intelligibility/no silence/no clipping, voice identity, rate, duration and SHA-256 safely.
- Dify/edge each have explicit current pass/fail/skip/open status and command; skip is never worded as passed.
- No key, response body, recap content or absolute temp path enters verification/evidence.
- Any local-media failure is recorded as a nonempty gap and no production remediation occurs in this wave.
</acceptance_criteria>
<done>At least one real usable local Chinese audio path is proven from current code and every optional external gate is represented truthfully.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q -m tts tests\results\test_tts_live.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m pytest -q -m "(cloud or network) and tts" tests\results\test_tts_live.py; if (($LASTEXITCODE -ne 0) -and ($LASTEXITCODE -ne 5)) { exit $LASTEXITCODE } else { exit 0 }</automated>
</verify>
</task>

<task type="auto" id="04-07-04">
<name>Task 4: Real-browser VQ-01..VQ-15 and independent-verifier handoff</name>
<files>
tests/ui/test_browser.py
scripts/run-phase4-browser-qa.ps1
evidence/ui/phase4/visual-qa.json
evidence/ui/phase4/VQ-01.png
evidence/ui/phase4/VQ-02.png
evidence/ui/phase4/VQ-06.png
evidence/ui/phase4/VQ-08.png
evidence/ui/phase4/VQ-10.png
evidence/ui/phase4/VQ-11.png
evidence/ui/phase4/VQ-13.png
.planning/phases/04-multimodal-results-ui/04-07-EXECUTION-LOG.md
.planning/phases/04-multimodal-results-ui/04-07-GAPS.md
</files>
<read_first>
.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
src/debugmate/ui/app.py
src/debugmate/ui/presentation.py
src/debugmate/results/service.py
fixtures/replay/index.json
tests/ui/test_app.py
tests/ui/test_callbacks.py
</read_first>
<action>
Create a verification-only loopback harness using pinned Playwright `1.61.0`, explicit `channel="msedge"` and the exact server command `.\.venv\Scripts\python.exe -m debugmate.ui.serve --host 127.0.0.1 --port N`. Port protocol: bind a temporary `TcpListener(IPAddress.Loopback, 0)`, read assigned port, close reservation immediately before `Start-Process -PassThru -WindowStyle Hidden`, capture that PID, and retry the whole reserve/start sequence up to three times only when child exits with bind failure. Poll `GET http://127.0.0.1:N/config` every 250ms for at most 30s; ready means HTTP 200 JSON containing `version` and `components`. In `finally`, close Playwright page/context/browser, stop only captured server PID, wait for exit, confirm port closed and remove temp staging. Explicit navigation/action/screenshot timeouts apply. Mark tests `browser`; execute VQ-01..VQ-15.

Store required screenshots and all fifteen ledger rows. Inspect actual screenshots, not only DOM assertions. Any overlap, crop, body horizontal scroll, hidden action, ambiguous status, broken control or replay deception is blocking: record a focused gap with screenshot/hash/reproducer and owning UI files, stop acceptance, and defer all CSS/component remediation to a later explicit gap plan.

Complete execution log with pointers for each requirement/threat/media/VQ/gate. Run final offline/Ruff/diff/secret scan without changing production. If any gate is red, `04-07-GAPS.md` must be nonempty and handoff is to gap planning rather than acceptance; if none are red, hand evidence to independent verifier. This wave never edits production or CSS.
</action>
<acceptance_criteria>
- Browser test launches real loopback Gradio and `pytest -m browser` exercises the current app, not a static facsimile.
- Harness proves project-pinned Playwright 1.61.0 plus explicit `msedge` channel, bounded health/action timeouts and finally-cleanup of the captured browser/server; it has no global Python 3.10 dependency.
- `visual-qa.json` contains exactly VQ-01 through VQ-15 with current result and evidence; required seven screenshot files exist and hashes match.
- 1366/1024/768 widths and 200% zoom have no body horizontal scroll, overlap/crop or unavailable primary status/action; long content uses local bounded scroll.
- Keyboard focus/order/native tabs/accordion/audio/download work; grayscale statuses retain icon and literal text.
- Replay, partial, failed, fallback and correction confirmation copy exactly match UI-SPEC and use verified redacted evidence.
- Execution evidence maps all nine requirements and fourteen threats to fresh commands/files; unresolved high threat or VQ failure blocks handoff to independent verification.
</acceptance_criteria>
<done>Phase 4 execution has fresh real-browser, real-local-media, offline/security and traceability evidence ready for an independent GSD verifier, without pre-creating its verdict.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q -m browser tests\ui\test_browser.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\scripts\run-phase4-browser-qa.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m pytest; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m ruff check src tests; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; git diff --check</automated>
</verify>
</task>
</tasks>

<verification>
- Every automated command in Tasks 1–4 passes exactly as written.
- After execution, a fresh independent gsd-verifier inspects code, artifacts, result/ZIP output, screenshots and execution logs and alone creates the phase verdict/report.
- External skips remain named open gates but local SAPI and all offline/browser blocking gates pass.
</verification>

<success_criteria>
- All nine Phase 4 requirements are individually achieved and evidenced.
- All T4-01..T4-14 threats are closed with no unresolved high-risk finding.
- The unified page and three modalities are genuine, consistent, downloadable, replay-truthful and visually verified.
</success_criteria>

<output>Create `.planning/phases/04-multimodal-results-ui/04-07-SUMMARY.md`, `04-07-EXECUTION-LOG.md`, `04-07-GAPS.md`, media and visual QA evidence. Do not modify production/CSS; route nonempty gaps to a later explicit gap plan, otherwise hand off to the independent verifier.</output>
