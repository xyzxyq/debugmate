---
phase: 04-multimodal-results-ui
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - src/debugmate/results/consistency.py
  - src/debugmate/results/publisher.py
  - src/debugmate/results/verifier.py
  - src/debugmate/cli.py
  - tests/results/test_consistency.py
  - tests/results/test_publisher.py
autonomous: true
requirements: [MULTI-04, UX-02]
must_haves:
  truths:
    - "Only identity-equal, privacy/media-verified report/card/recap candidates can cross the consistency gate."
    - "Result bundles publish to a separate results/case/result directory atomically and never modify Phase 3 evidence."
    - "Full and partial ZIPs use a frozen acyclic manifest/checksums/publication hash graph, strict member allowlists and fixed metadata/order, and are byte-identical on repeat build."
    - "Every restore/download rereads manifest, confinement and hashes from disk immediately before returning a path."
  artifacts:
    - path: "src/debugmate/results/consistency.py"
      provides: "Cross-modal identity, privacy, citation and availability gate"
      contains: "def validate_result_candidates("
    - path: "src/debugmate/results/publisher.py"
      provides: "Atomic full/partial ResultBundle and deterministic ZIP publisher"
      contains: "def publish_result_bundle("
    - path: "src/debugmate/results/verifier.py"
      provides: "Public on-disk result and download verifier"
      contains: "def verify_result_bundle("
  key_links:
    - from: "src/debugmate/results/consistency.py::ValidatedResultCandidates"
      to: "src/debugmate/results/publisher.py::publish_result_bundle"
      via: "publisher accepts no raw renderer outputs or arbitrary paths"
      pattern: "ValidatedResultCandidates.*publish_result_bundle"
    - from: "src/debugmate/results/verifier.py::resolve_verified_download"
      to: "src/debugmate/results/contracts.py::ResultManifest"
      via: "member ID resolution after fresh manifest/hash/confinement verification"
      pattern: "verify_result_bundle.*resolve_verified_download"
---

<objective>
Bind all modalities through one consistency gate and atomically publish hash-verifiable full/partial bundles and deterministic downloads.

Purpose: prevent identity drift, tampering, path abuse, partial-result deception and non-reproducible course packages.
Output: consistency gate, independent ResultBundle, public verifier, deterministic ZIP and download resolver.
</objective>

<execution_context>
@.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
@.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
@.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
@docs/superpowers/specs/2026-07-13-phase4-multimodal-results-ui-design.md
@.planning/phases/04-multimodal-results-ui/04-04-SUMMARY.md
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [result identity, result directory, deterministic ZIP, checksums, download path]
trust_boundaries: [renderer candidates to gate, temp result directory to final directory, final bundle to ZIP/download]
threats:
  - id: T4-01
    severity: high
    vector: "A forged artifact/source identity is combined into a plausible bundle."
    control: "exact ArtifactIdentity equality plus source revalidation before publication"
  - id: T4-02
    severity: high
    vector: "Traversal or symlink/reparse swaps result members outside the root."
    control: "ID-derived directories, confinement, no symlink/reparse, fresh verification before access"
  - id: T4-03
    severity: high
    vector: "ZIP slip members or unsafe names are published."
    control: "fixed relative POSIX allowlist; reject absolute, drive, backslash, NUL and dot-dot"
  - id: T4-09
    severity: high
    vector: "A UI/download caller obtains an arbitrary server file."
    control: "member IDs only and resolve-after-verify service"
  - id: T4-11
    severity: high
    vector: "Concurrent/interrupted publication overwrites or exposes an incomplete result."
    control: "exclusive temp/final directories, atomic rename, cleanup and immutable verified reuse"
  - id: T4-13
    severity: high
    vector: "A ZIP bomb or oversized member exhausts course/demo resources."
    control: "member count/size/total/ratio caps, CRC and unpacked hash verification"
  - id: T4-14
    severity: high
    vector: "Stale result versions restore as current."
    control: "exact manifest/generation/source identity and unknown-version rejection"
</threat_model>

<tasks>
<task type="auto" id="04-05-01">
<name>Task 1: Cross-modal consistency and publication candidate gate</name>
<files>
src/debugmate/results/consistency.py
tests/results/test_consistency.py
tests/results/test_publisher.py
</files>
<read_first>
src/debugmate/results/contracts.py
src/debugmate/results/report.py
src/debugmate/results/card.py
src/debugmate/results/recap.py
src/debugmate/results/audio.py
src/debugmate/results/media.py
src/debugmate/results/loader.py
src/debugmate/privacy/output_scan.py
tests/results/test_report.py
tests/results/test_card.py
tests/results/test_tts_chain.py
</read_first>
<action>
Write RED tests for each identity field drift (case, source run, diagnosis hash, schema, generation), report/citation mismatch, card/font identity mismatch, recap/audio request mismatch, forged citation, unsafe text, modified PNG, modified/invalid MP3, duplicate artifact name and inconsistent completed/partial availability. Simulate interruption and assert no success object is returned.

Implement `validate_result_candidates(source, presentation, report, citations, card_result, recap, audio_result) -> ValidatedResultCandidates`. Revalidate source identity, exact ArtifactIdentity across every available candidate, citation graph, report/recap privacy, PNG disk verifier and MP3 media probe. A completed candidate requires report/card/recap text/audio; missing card or audio is allowed only as explicit partial with corresponding safe failure and retry scope. Report/source/identity/privacy failure is terminal and cannot publish a partial delivery. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Mutation of any shared identity field or candidate bytes rejects with `artifact_identity_mismatch` or a fixed artifact verification code.
- Completed requires all required modalities; partial names every missing artifact, failed node and retry scope.
- Citation rows exactly match verified diagnosis support and no renderer can add a URL/claim.
- Report/recap are rescanned, PNG reopened, MP3 reprobed immediately before validated-candidate creation.
- Only `ValidatedResultCandidates` can enter publisher; it exposes no arbitrary source path.
</acceptance_criteria>
<done>One strict gate proves same-source identity, safety and real artifact validity before any result publication begins.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_consistency.py</automated>
</verify>
</task>

<task type="auto" id="04-05-02">
<name>Task 2: Atomic full/partial ResultBundle and tamper verifier</name>
<files>
src/debugmate/results/publisher.py
src/debugmate/results/verifier.py
tests/results/test_publisher.py
</files>
<read_first>
src/debugmate/results/consistency.py
src/debugmate/results/contracts.py
src/debugmate/results/loader.py
src/debugmate/evidence.py
src/debugmate/hashing.py
src/debugmate/privacy/output_scan.py
.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
tests/test_evidence.py
tests/results/test_consistency.py
</read_first>
<action>
Write RED abuse/integration tests for the exact full members (`diagnosis.json`, `report.md`, `card.png`, `recap.txt`, `recap.mp3`, `citations.json`, `source-manifest.json`, `result-manifest.json`, `checksums.sha256`) and partial allowlists with missing card/audio. Add explicit cycle tests: reject a result manifest that lists/hashes itself, checksums, ZIP or publication; reject checksums that lists itself; require checksums to include every other ZIP member including result manifest; reject publication inside ZIP or publication without final ZIP hash/result identity. Cover traversal/absolute/drive/backslash/NUL names, symlink/reparse, extra/missing/tampered files, wrong byte counts/MIME/hashes, unsafe copied source data, oversized member/total/count, duplicate result, interrupted write/finalize and temp cleanup.

Implement `ResultBundlePublisher.begin(results_root, case_id, result_identity)` using `results/<case_id>/.tmp-<result_id>` and exclusive final directory. Derive `result_id` only after candidate hashes/backends/status/replay semantics are fixed; never use timestamps. Copy only allowlisted verified source summary, not raw screenshot/provider response/log. Freeze the graph exactly: `result-manifest.json` lists/hashes business payload (`diagnosis`, report/card/recap/audio/citations/source summary as available) and excludes itself, checksums, ZIP and publication; `checksums.sha256` lists every ZIP member except itself and therefore includes `result-manifest.json`; `publication.json` is outside ZIP and alone stores final ZIP hash plus result identity. Write candidates, manifest, sorted checksums and archive, rescan/reopen/reprobe every file, then write publication and atomically rename. Existing final path is immutable: verify-and-reuse identical content or fail; never overwrite. Implement `verify_result_bundle(path)` to rederive this graph and reject cycles, symlink/reparse, unknown versions, identity mismatch, unlisted/missing files, hash/size/MIME/privacy/media failures and partial/full inconsistency. Do not modify `EvidenceBundle` audio fail-closed behavior. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Full and partial member allowlists are exact; partial manifest/file status and missing artifacts agree.
- Result path is `results/case_id/result_id`, derives from verified identity/content and cannot traverse or overwrite.
- Publisher cleans every failed temp sibling and never leaves a final-looking incomplete directory.
- Verifier rejects symlink/reparse, traversal, extra/missing/tampered/oversized/version-drift artifacts after disk reread.
- Source summary contains no raw screenshot, provider body, API log, secret, full chunk or arbitrary file.
- Phase 3 evidence directories and `AudioEvidenceNotReady` behavior remain unchanged.
- Result manifest/checksums/publication membership exactly follows the frozen acyclic graph and every forbidden self/cross-cycle fixture rejects.
</acceptance_criteria>
<done>Independent immutable full/partial result directories publish atomically and survive complete public on-disk revalidation.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_publisher.py -k "identity or atomic or duplicate or path or tamper or partial"</automated>
</verify>
</task>

<task type="auto" id="04-05-03">
<name>Task 3: Deterministic safe ZIP and archive verification</name>
<files>
src/debugmate/results/publisher.py
src/debugmate/results/verifier.py
tests/results/test_publisher.py
</files>
<read_first>
src/debugmate/results/publisher.py
src/debugmate/results/verifier.py
src/debugmate/results/contracts.py
.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
tests/results/test_publisher.py
</read_first>
<action>
Write RED tests that rebuild full and partial archives twice from the same verified candidates and require byte-identical ZIP and SHA-256. Inspect every `ZipInfo`: stable sorted POSIX name, `(1980,1,1,0,0,0)`, `create_system=3`, fixed readonly mode, deflate level/type, empty extra/comment and no inherited mtime. Test CRC, unsafe name, duplicate member, extra member, bomb ratio/member/total limits, and unpack each archive to verify the exact acyclic graph: result manifest hashes business payload only; checksums includes manifest and every other ZIP member but not itself; publication is absent from ZIP and stores the final ZIP hash/result identity outside. Add RED fixtures for each prohibited edge.

Implement explicit `ZipInfo` + `writestr` construction from the manifest allowlist; never use `ZipFile.write` or `shutil.make_archive`. Freeze member order and metadata. Name archive `debugmate-result.zip` or `debugmate-result-partial.zip`; partial label, manifest status and member set must agree. Verify archive from disk before adding its hash to outer `publication.json`. Include `publication.json` outside the ZIP to keep the hash graph acyclic. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Two builds from identical verified members are byte-identical with equal SHA-256.
- Every ZipInfo field/order/path follows the frozen contract and contains no filesystem timestamp or absolute path.
- ZIP CRC, member count, individual/total uncompressed size, ratio, manifest and checksums verify after unpack.
- Full/partial filename and archive manifest status cannot disagree.
- ZIP contains no temp, publication record, raw screenshot/provider/log/key or itself.
- Result manifest, checksums and outside publication record satisfy the exact frozen acyclic graph; each prohibited cycle edge rejects.
</acceptance_criteria>
<done>Full and partial evidence downloads are deterministic, safe to unpack and cryptographically bound without a manifest/hash cycle.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_publisher.py -k "zip or deterministic or crc or bomb"</automated>
</verify>
</task>

<task type="auto" id="04-05-04">
<name>Task 4: Fresh-verification download resolver and reusable verifier CLI</name>
<files>
src/debugmate/results/verifier.py
src/debugmate/cli.py
tests/results/test_publisher.py
</files>
<read_first>
src/debugmate/results/verifier.py
src/debugmate/results/publisher.py
src/debugmate/cli.py
src/debugmate/hashing.py
tests/test_probe_cli.py
tests/results/test_publisher.py
</read_first>
<action>
Write RED TOCTOU tests that render a valid member ID, mutate/swap/delete/symlink the file or manifest, then call download resolution and require failure. Test arbitrary paths, labels treated as paths, unlisted member IDs and stale generation versions. Add CLI tests for `result-verify --root <controlled-root> --case-id ... --result-id ...` returning machine-readable safe status without accepting an arbitrary member path.

Implement `resolve_verified_download(results_root, case_id, result_id, member_id) -> Path`: derive confined final directory from strict IDs, call full `verify_result_bundle` at call time, map only fixed member IDs (`bundle`, `report`, `card`, `audio`, `citations`) present in manifest, resolve/no-symlink/hash-check the selected file again, then return. Add CLI that uses the same verifier and emits no absolute path/provider values. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Download resolver accepts strict case/result/member IDs only and no user filesystem path.
- Manifest and selected bytes are freshly verified immediately before return; post-render tampering fails.
- Missing/unavailable partial members do not resolve; partial bundle resolves under its explicit label.
- CLI and callable verifier share one implementation and produce fixed safe machine-readable results.
- No failure output includes absolute result/temp path or secret/provider body.
</acceptance_criteria>
<done>Every UI/download/Phase 5 consumer has one reusable fresh on-disk verifier and cannot fetch an arbitrary or stale server file.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_publisher.py -k download; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m ruff check src\debugmate\results src\debugmate\cli.py tests\results</automated>
</verify>
</task>
</tasks>

<verification>
- Run all consistency/publisher tests, then execute the public verifier against full and partial bundles.
- Build ZIP twice in separate temporary roots and compare bytes/hash.
- Mutate each allowlisted artifact after a successful render and prove download resolution fails.
- Run full offline suite and confirm no file under Phase 3 `evidence/` changed.
</verification>

<success_criteria>
- MULTI-04 identity is enforced across every artifact and disk boundary.
- UX-02 produces deterministic verified full/partial downloads only.
- T4-01/02/03/09/11/13/14 paths are directly abuse-tested.
</success_criteria>

<output>Create `.planning/phases/04-multimodal-results-ui/04-05-SUMMARY.md`.</output>
