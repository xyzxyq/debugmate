---
phase: 04-multimodal-results-ui
plan: 11
type: execute
wave: 2
depends_on: ["04-10"]
gap_closure: true
requirements: [MULTI-01, MULTI-02, MULTI-03, MULTI-04, MULTI-05, UX-01, UX-02, UX-03, UX-04]
autonomous: true
v01_scope: course-demo
---

# Phase 04-11: V0.1 界面与下载闭环收尾

## Objective

完成可在 Windows 桌面浏览器稳定演示的课程 V0.1。保留同源文字、PNG、MP3、隐私预览、回放标识和结果下载，不再建设公开发布或生产级视觉证据系统。

## Tasks

### 04-11-01 已完成：长内容与完成态布局

- 已验证长报告、长命令、长引用和高图卡可查看。
- 已验证完成态关键结果在桌面演示视口可达。

### 04-11-02：代表性无障碍收尾

- 增加受控状态播报区域和稳定键盘焦点样式。
- 只读事实、引用、命令改为不会困住 Tab 键的 Markdown 表格。
- 隐藏 Gradio 生成的不可见重复标签页控件。
- 运行单元测试、VQ-13、VQ-15、VQ-14 和一个长内容回归样例。

### 04-11-03：一个正向同次运行下载验证

- 从浏览器实际下载 `debugmate-result.zip`。
- 仅验证状态、文件名、ZIP 可读性、manifest、校验值和可见 `source_run_id` 一致。
- 不增加恶意 ZIP、故障注入、原子发布或完整攻击矩阵。

### 04-11-04：小型课程证据集

- 选取 3-5 个代表性案例：成功、长内容、语音降级/部分完成、安全失败，必要时增加纠错案例。
- 保存真实截图和简单哈希清单，供 PPT 与视频使用。
- 不要求完整 VQ-01..15 重跑，不构建不可变 generation/pointer 体系。

## Verification

```powershell
.\.venv\Scripts\python.exe -m pytest -q tests\ui\test_app.py tests\ui\test_view_state.py tests\ui\test_callbacks.py
.\.venv\Scripts\python.exe -m pytest -q -m browser tests\ui\test_browser.py -k "vq_13 or vq_15 or v01_download or long_content"
.\scripts\run-phase4-truth-state-qa.ps1 -TestExpression 'vq_14'
git diff --check
```

## Done When

课程演示能完成“输入/回放 -> 隐私确认 -> 诊断 -> 文字/PNG/MP3 -> ZIP 下载”，并已有少量真实证据可直接进入课程材料。
