---
phase: 04-multimodal-results-ui
plan: 11
type: execute
wave: 2
depends_on: ["04-10"]
gap_closure: true
files_modified:
  - fixtures/replay/index.json
  - fixtures/replay/long-content/outcome.json
  - fixtures/replay/long-content/source/
  - src/debugmate/ui/app.py
  - src/debugmate/ui/presentation.py
  - tests/ui/test_app.py
  - tests/ui/test_browser.py
  - scripts/run-phase4-visual-qa.ps1
  - scripts/phase4-visual-evidence.psm1
  - scripts/test-phase4-visual-evidence-transaction.ps1
  - tests/ui/test_visual_evidence_transaction.py
  - evidence/ui/phase4/visual-qa.commit.json
  - evidence/ui/phase4/generations/<generation_id>/generation-manifest.json
  - evidence/ui/phase4/generations/<generation_id>/visual-qa.json
  - evidence/ui/phase4/generations/<generation_id>/screenshots/
  - evidence/ui/phase4/archive/
requirements: [MULTI-01, MULTI-02, MULTI-03, MULTI-04, MULTI-05, UX-01, UX-02, UX-03, UX-04]
autonomous: true
must_haves:
  truths:
    - "A strict allowlisted long-content replay renders real diagnosis commands, locally scrolls long report/code, preserves a tall card aspect ratio and never creates body horizontal overflow."
    - "Completed layouts, keyboard operation, non-color state cues and 200% zoom satisfy VQ-04/05/11/12/13/14/15 in real Edge."
    - "Browser download bytes form a verified ZIP for the same visible run; no server path or unallowlisted byte is trusted."
    - "One bounded single-owner runner publishes an immutable generation containing a complete 15-row visual-qa.json plus required screenshots, then atomically switches the sole root pointer only after every row passes; any failure restores the prior committed generation and cleans staging, backup, runtime, PID and port."
  artifacts:
    - path: "scripts/run-phase4-visual-qa.ps1"
      provides: "Bounded full-matrix Edge runner and set-level evidence transaction"
      contains: "visual-qa.json"
    - path: "evidence/ui/phase4/visual-qa.commit.json"
      provides: "Sole atomic pointer to one immutable, tested visual-evidence generation"
      contains: "generation_manifest_sha256"
    - path: "evidence/ui/phase4/generations/<generation_id>/generation-manifest.json"
      provides: "Immutable manifest binding the 15-row ledger and generation-local screenshots"
      contains: "VQ-15"
  key_links:
    - from: "fixtures/replay/long-content/outcome.json"
      to: "src/debugmate/ui/app.py"
      via: "strict replay index and ResultApplicationService verification"
      pattern: "long-content"
    - from: "tests/ui/test_browser.py"
      to: "scripts/run-phase4-visual-qa.ps1"
      via: "runner-injected staging directory and ledger path"
      pattern: "DEBUGMATE_UI"
    - from: "evidence/ui/phase4/visual-qa.commit.json"
      to: "evidence/ui/phase4/generations/<generation_id>/generation-manifest.json"
      via: "exact generation ID plus manifest SHA-256; every reader follows this pointer only"
      pattern: "generation_manifest_sha256"
---

# Phase 04 content, accessibility and atomic visual evidence plan

<objective>
Complete the remaining browser matrix, verify real downloads and same-run identity, then publish all VQ evidence as one validated transaction. VQ-01 is rerun against current code (or imported only after current-code validation proves its bound code/evidence identity); old JSON is never concatenated blindly.
</objective>

<execution_context>
@.planning/phases/04-multimodal-results-ui/04-UAT.md
@.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
@.planning/phases/04-multimodal-results-ui/04-10-PLAN.md
@scripts/run-phase4-local-live-qa.ps1
</execution_context>

<threat_model>
security_enforcement: true
threats:
  - "Long-content replay is a committed strict fixture selected by an enum/allowlist; UI and QA interfaces accept neither paths nor arbitrary outcome JSON."
  - "Every browser download is read as bytes, bounded by size, checked for approved MIME/filename/hash, then ZIP members/checksums/manifest/same-run identity are reverified."
  - "The ledger allowlists fields and hashes identities/screenshots; it records no raw sensitive text, absolute path, capability token or server response."
  - "Set promotion uses same-volume unique staging/backup names, validates all 15 unique IDs and required files before rename, and restores exact previous bytes on any pre-commit failure."
  - "`generation_id` matches exactly `gen_[0-9a-f]{32}` and names an exclusive-create immutable directory below `evidence/ui/phase4/generations/`; an existing target is accepted only after byte-for-byte manifest/content verification and is never overwritten."
  - "The root `visual-qa.commit.json` exact allowlist is only `schema_version`, `generation_id`, `generation_manifest_sha256`, `tested_commit_sha`, `published_at`. Its tested commit is the clean commit used by the runner."
  - "Legacy root visual-qa.json and VQ screenshots are archived and are not a truth source. Application tests, gate runner and independent verifier must resolve ledger/screenshots only by commit pointer -> generation manifest -> generation-local relative member."
</threat_model>

<tasks>

<task id="04-11-01" name="RED long-content, commands and completed geometry">
  <read_first>04-UAT.md tests 8/9/15; 04-UI-SPEC.md VQ-04/05/11/12; fixtures/replay/index.json; src/debugmate/ui/app.py</read_first>
  <action>Add RED tests requiring a strict long-content fixture and real command rendering from DiagnosisRecord rather than static safety copy. Generate/commit the fixture through the existing deterministic fixture tooling, add it to the strict index, bind commands to a read-only accordion/table with platform, impact, expected result and rollback, and keep the safety note. Assert report/code local scroll, uncut command text, tall-card aspect ratio and completed 1024x768/768x1024 geometry.</action>
  <acceptance_criteria>The initial test fails because fixture/command binding is absent; GREEN accepts only the allowlisted fixture, displays exact verified commands, four literal tabs, correct region order and zero body overflow.</acceptance_criteria>
  <verify><command>.\.venv\Scripts\python.exe -m pytest -q tests\ui\test_app.py tests\ui\test_browser.py -k "long_content or commands or tall_card or completed_responsive"</command><expected>Focused RED is recorded, then all VQ-04/05/11/12 assertions pass.</expected></verify>
  <done>Commit fixture, minimal command/UI wiring and tests as `feat(04-11): verify long content and completed layouts`.</done>
</task>

<task id="04-11-02" name="RED accessibility and minimum live-status wiring">
  <read_first>04-UI-SPEC.md VQ-13/14/15 and Accessibility Contract; src/debugmate/ui/presentation.py accessible_status; tests/ui/test_browser.py</read_first>
  <action>Write RED real-browser tests for keyboard-only tab/accordion/audio/download use, visible focus, four state labels under test-side grayscale, and completed state at actual browser zoom factor 2.0. Only if RED proves the current announced status is absent, minimally connect `accessible_status` to one visible/aria-live-compatible native status component; do not add custom positive tabindex or JS-only semantics.</action>
  <acceptance_criteria>DOM order is logical, focus is visible, native controls activate by keyboard, every status has icon plus text without color, and at 200% status/primary actions/content remain reachable without overlap or body overflow.</acceptance_criteria>
  <verify><command>.\.venv\Scripts\python.exe -m pytest -q -m browser tests\ui\test_browser.py -k "vq_13 or vq_14 or vq_15"</command><expected>RED evidence precedes the smallest required wiring; all three real Edge tests pass.</expected></verify>
  <done>Commit tests and only proven-minimum UI changes as `fix(04-11): close browser accessibility contracts`.</done>
</task>

<task id="04-11-03" name="Verify browser downloads and same-run ZIP identity">
  <read_first>src/debugmate/ui/app.py content store; src/debugmate/results/verifier.py; src/debugmate/results/publisher.py; tests/ui/test_browser.py</read_first>
  <action>Add RED then GREEN Edge assertions that click full and partial download controls, capture response bytes without trusting a path, enforce size/filename/MIME/hash allowlists, and inspect ZIP CRC, member allowlist, checksums and manifests. Cross-check case/source-run/result/diagnosis hash with the visible state and screenshot ledger row. Add tampered/extra/member-mismatch negatives.</action>
  <acceptance_criteria>Completed and partial ZIPs verify from downloaded bytes; all displayed modalities and archive members bind to one result; unsafe filename, oversized bytes, extra member or identity mismatch fails before evidence write.</acceptance_criteria>
  <verify><command>.\.venv\Scripts\python.exe -m pytest -q tests\ui\test_browser.py tests\results\test_publisher.py -k "download or zip or same_run"</command><expected>All byte-level and identity assertions pass.</expected></verify>
  <done>Commit tests/helpers as `test(04-11): verify browser evidence downloads`.</done>
</task>

<task id="04-11-04" name="Atomic full visual-matrix runner and publication">
  <read_first>scripts/run-phase4-local-live-qa.ps1 transaction functions; scripts/run-phase4-browser-layout-qa.ps1 ownership; tests/ui/test_browser.py; 04-UI-SPEC.md Visual QA Matrix</read_first>
  <action>Separate success execution from transaction fault injection. `run-phase4-visual-qa.ps1 -VqSelector all` alone owns the server/browser path: reserve/release one literal loopback port, start one captured hidden serve PID with QA enabled, pass the fresh capability only as a Playwright request header, poll `/config` for 30s, run exactly all VQs, restore QA/base/evidence environment values, terminate only the captured PID and prove port closed. Put transaction primitives in `phase4-visual-evidence.psm1`; the fault script imports only that module and never invokes the runner/browser. Build `evidence/ui/phase4/generations/.staging-<token>` with `visual-qa.json`, `screenshots/<VQ-ID>.png` and `generation-manifest.json`; the manifest uses controlled relative members and hashes every byte. Derive a safe `gen_[0-9a-f]{32}` from the canonical generation manifest, exclusive-move staging to the immutable final generation, then atomically replace root `visual-qa.commit.json` as the unique commit point. The pointer exact fields are `schema_version,generation_id,generation_manifest_sha256,tested_commit_sha,published_at`. Every reader resolves only through pointer and rehashes the generation manifest before members. Archive legacy root ledger/screenshots and prohibit their use. The explicit fault matrix covers every old-pointer backup, staged generation promotion, commit-pointer replacement, and cleanup operation. Pre-commit faults preserve the prior pointer/generation byte-for-byte; post-commit cleanup faults retry/reconcile to zero residue while retaining one complete new generation. No observation may combine generations.</action>
  <acceptance_criteria>The success selector publishes exactly one immutable generation with VQ-01..VQ-15 and required generation-local screenshots; VQ-01 is newly executed or cryptographically revalidated against `tested_commit_sha`. Pointer and generation manifests pass exact-field/path/hash checks; unsafe generation IDs, overwrite attempts and root legacy reads fail. The fault command never starts a server or recursively invokes the success runner. Every injected pre-commit fault proves the old committed collection byte-identical; commit/cleanup cases prove the visible collection is either the whole old generation or whole new generation, never mixed, with staging/backup/runtime/port residue zero after reconciliation.</acceptance_criteria>
  <verify><command>.\scripts\run-phase4-visual-qa.ps1 -VqSelector all; .\scripts\test-phase4-visual-evidence-transaction.ps1 -Fault all; .\.venv\Scripts\python.exe -m pytest -q tests\ui\test_visual_evidence_transaction.py -k "backup_move or promotion_move or commit_point or cleanup_reconcile or no_recursive_runner"</command><expected>The success path publishes all 15 rows once; the isolated backup/promotion/commit/cleanup fault matrix passes byte-identity, no-mixed-generation, no-recursion and zero-residue assertions.</expected></verify>
  <done>Resolve and inspect all required screenshots through the committed pointer, then commit runner, tests, pointer and forced-added immutable generation as `test(04-11): publish complete Phase 4 visual matrix`.</done>
</task>

</tasks>

<verification>
Run focused RED/GREEN per task, full UI/result tests and Ruff. Independently resolve pointer -> generation manifest -> ledger/screenshots and parse ZIPs from disk; direct root-ledger reads must fail. Re-run the isolated transaction fault matrix and confirm the previous committed generation is byte-identical after rollback.
</verification>

<success_criteria>
All 15 VQ rows have real Edge evidence tied to current code, required screenshots are inspectable, downloads are byte-verified and same-run, and the formal evidence directory is a complete atomic fact rather than a mixture of runs.
</success_criteria>
