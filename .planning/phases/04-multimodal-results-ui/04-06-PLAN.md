---
phase: 04-multimodal-results-ui
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - src/debugmate/results/service.py
  - src/debugmate/ui/__init__.py
  - src/debugmate/ui/presentation.py
  - src/debugmate/ui/app.py
  - src/debugmate/ui/serve.py
  - fixtures/replay/index.json
  - tests/results/test_service.py
  - tests/ui/__init__.py
  - tests/ui/test_view_state.py
  - tests/ui/test_app.py
  - tests/ui/test_callbacks.py
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04]
must_haves:
  truths:
    - "One application service is the only UI facade for diagnose-and-compose, replay, restore, correction, retry and verified download; UI never constructs or supplies a DiagnosisRunOutcome."
    - "Replay verifies indexed full outcome/source, imports the outcome store record, composes and publishes a new replay=true Phase 4 result, freshly verifies it, then returns state."
    - "Live/replay outcomes are stored as complete strict redacted records by run ID; refresh/correction rereads and revalidates them, while confirmed correction creates new immutable Phase 3/4 identities."
    - "Gradio 6 renders the exact compact three-region state contract with native safe components and no path/shell/raw-HTML boundary."
  artifacts:
    - path: "src/debugmate/results/service.py"
      provides: "ResultApplicationService live diagnosis facade, outcome repository, replay/correction/restore/idempotency"
      contains: "class ResultApplicationService"
    - path: "src/debugmate/ui/presentation.py"
      provides: "Pure ResultViewState-to-component mapping"
      contains: "def render_view_state("
    - path: "src/debugmate/ui/app.py"
      provides: "Gradio 6 Blocks diagnostic workbench"
      contains: "def build_app("
    - path: "src/debugmate/ui/serve.py"
      provides: "Runnable loopback Gradio server CLI"
      contains: "def main("
  key_links:
    - from: "src/debugmate/ui/app.py"
      to: "src/debugmate/results/service.py::ResultApplicationService"
      via: "private callbacks accept strict IDs/drafts and return strict view states/verified paths"
      pattern: "ResultApplicationService.*render_view_state"
    - from: "src/debugmate/gateway.py::rerun_diagnosis_json"
      to: "src/debugmate/results/service.py::correct_and_compose"
      via: "confirmed correction creates a new source run then a new result without overwriting either prior bundle"
      pattern: "rerun_diagnosis_json.*publish_result_bundle"
---

<objective>
Expose verified multimodal results through a replay/correction-aware application service and compact Gradio 6 single-page workbench.

Purpose: turn strict result bundles into an honest, usable and recoverable course demonstration without weakening domain safety.
Output: service facade, pure view mapping, native Gradio app and structural/integration tests.
</objective>

<execution_context>
@.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
@.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
@.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
@.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
@docs/superpowers/specs/2026-07-13-phase4-multimodal-results-ui-design.md
@.planning/phases/04-multimodal-results-ui/04-05-SUMMARY.md
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [view-state truth, replay provenance, immutable correction history, verified downloads, UI callback boundary]
trust_boundaries: [replay selection to source loader, UI draft to correction rerun, manifest to component paths, exception to visible state]
threats:
  - id: T4-04
    severity: high
    vector: "Untrusted diagnosis text becomes raw HTML/unsafe Markdown in the page."
    control: "safe report renderer and native Markdown/text/table only; no untrusted gr.HTML"
  - id: T4-08
    severity: high
    vector: "Displayed repair commands gain an execution/terminal callback."
    control: "display-only command metadata and AST/component tests prohibiting shell/subprocess actions"
  - id: T4-09
    severity: high
    vector: "File/DownloadButton returns a user-selected or stale server path."
    control: "service member IDs and immediate resolve_verified_download"
  - id: T4-10
    severity: high
    vector: "Fixture replay is represented as a live cloud success."
    control: "allowlist identity cross-check and mandatory replay labels in state, manifest and download metadata"
  - id: T4-11
    severity: high
    vector: "Double click/race publishes duplicate runs/results or overwrites history."
    control: "per-case lock, idempotency key, Gradio once/concurrency controls and immutable directories"
  - id: T4-12
    severity: high
    vector: "Raw exception/provider/path/secret reaches visible UI."
    control: "typed safe failure mapper and fixed copy contract"
  - id: T4-14
    severity: high
    vector: "Refresh restores stale or tampered result as current."
    control: "restore only through fresh public result verifier and exact versions"
</threat_model>

<tasks>
<task type="auto" id="04-06-01">
<name>Task 1: Pure complete/partial/failed/replay/fallback view-state mapping</name>
<files>
src/debugmate/ui/__init__.py
src/debugmate/ui/presentation.py
tests/ui/__init__.py
tests/ui/test_view_state.py
</files>
<read_first>
.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
src/debugmate/results/contracts.py
src/debugmate/results/verifier.py
src/debugmate/results/presentation.py
tests/results/test_contracts.py
</read_first>
<action>
Write RED visibility-matrix tests for idle, each of seven ordered running stages, completed, PNG-partial, TTS-partial, failed, replay+each outcome and fallback+completed/partial. Assert exact Chinese badges/copy, enabled/visible controls, available tabs/artifacts, seven failure-detail labels, full/partial download label, replay in top/result/download metadata, final backend/reason and absence of raw exceptions/paths. Treat replay and fallback as orthogonal fields, never enum substitutes.

Implement immutable `ComponentViewModel` records and pure `render_view_state(state: ResultViewState) -> ComponentViewModel`. Map state only; never inspect filesystem existence or call a service. Use exact UI-SPEC copy and availability. Running shows fixed stage and completed count without percentages; partial preserves verified artifacts; failed hides unverified result/download. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Matrix covers every outcome plus orthogonal replay/fallback combinations and all seven queue stages.
- Exact badges/actions/failure labels/download copy match UI-SPEC.
- No state is inferred from missing files; only strict manifest-derived fields drive visibility.
- Replay appears in top bar, result summary and download metadata and contains no live cloud success wording.
- Partial never says completed or offers full download; failure exposes safe code/stage/retry only.
</acceptance_criteria>
<done>Every legal ResultViewState has one deterministic honest component mapping independent of Gradio and filesystem state.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\ui\test_view_state.py</automated>
</verify>
</task>

<task type="auto" id="04-06-02">
<name>Task 2: Application service, replay, restore and confirmed correction</name>
<files>
src/debugmate/results/service.py
fixtures/replay/index.json
tests/results/test_service.py
</files>
<read_first>
src/debugmate/results/loader.py
src/debugmate/results/presentation.py
src/debugmate/results/report.py
src/debugmate/results/card.py
src/debugmate/results/audio.py
src/debugmate/results/consistency.py
src/debugmate/results/publisher.py
src/debugmate/results/verifier.py
src/debugmate/results/outcome_store.py
src/debugmate/gateway.py
src/debugmate/diagnosis/correction.py
fixtures/replay/index.json
tests/diagnosis/test_workflow_e2e.py
</read_first>
<action>
Write RED service tests for `diagnose_and_compose` from `ApprovedRedactedInput` and strict approved JSON, indexed replay, invalid/unindexed/path-like replay, verified refresh/restore, tampered outcome/result restore, per-case duplicate request, stage retry, unconfirmed/no-op correction, confirmed one-field correction and preservation of old/new source/outcome/result records. Add complete chains `live diagnose → refresh by run/result → correct`, `replay → correct`, and tamper the stored full outcome between display and correction. Assert UI-facing methods reject a caller-created DiagnosisRunOutcome, queue events use exact seven Phase 4 stages after Phase 3 completes, and errors become safe view states.

Implement `ResultApplicationService` with UI-facing `diagnose_and_compose(approved: ApprovedRedactedInput | strict-approved-payload)`, `load_replay(fixture_id)`, `restore_result(case_id, result_id)`, `correct_and_compose(previous_run_id, draft, confirmed)`, `retry_stage`, and `resolve_download`. `diagnose_and_compose` invokes Phase 3 from approved input, validates completed outcome, publishes Phase 3 evidence, writes full outcome store, then runs Phase 4; UI cannot pass/construct outcome. Freeze replay exactly: `load_replay` resolves only indexed controlled relative paths; strict-loads and validates full outcome; verifies its Phase 3 source bundle and cross-identity; imports the complete outcome into server store; creates PreparedGenerationContext; composes/publishes a new Phase 4 result whose machine manifest has `replay=true`, fixture ID/name and source run; calls public `verify_result_bundle` on that newly generated final directory; only then returns replay view state. It never restores an old prebuilt Phase 4 result and never uses live wording. Refresh reads run ID from verified result manifest, then rereads/revalidates store/source. Correction loads full outcome by run ID, applies confirmed rerun, publishes new source/outcome/result while preserving history. Commit RED/GREEN by live/store, replay and correction review units.
</action>
<acceptance_criteria>
- Replay strict-verifies indexed outcome plus Phase 3 source, imports full outcome store, publishes a new `replay=true` Phase 4 result, freshly verifies that result, then returns state.
- Live diagnosis accepts only approved redacted input/payload, invokes Phase 3 itself, and publishes evidence/full outcome before Phase 4; no UI outcome-construction boundary exists.
- Replay flag/fixture/source run appears in state, manifest and download metadata; no live wording is generated.
- Same in-flight idempotency key performs work once; concurrent duplicate receives deterministic running/existing result behavior.
- Correction requires at least one changed six-field value and explicit confirmation, creates distinct source run/result IDs and preserves old bundles.
- Refresh and correction reread the complete strict stored outcome by run ID; tampered/missing/mismatched records fail before rerun or publication.
- Restore/retry/download all reverify disk state and accept strict IDs/scopes, not paths.
- Exceptions map to fixed safe state; no command execution or raw provider/error content exists.
</acceptance_criteria>
<done>One service safely composes, replays, restores, corrects, retries and downloads while preserving immutable history and truth labels.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_service.py</automated>
</verify>
</task>

<task type="auto" id="04-06-03">
<name>Task 3: Gradio 6 native three-region application structure</name>
<files>
src/debugmate/ui/app.py
src/debugmate/ui/serve.py
tests/ui/test_app.py
</files>
<read_first>
.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
src/debugmate/ui/presentation.py
src/debugmate/results/service.py
pyproject.toml
tests/ui/test_view_state.py
</read_first>
<action>
Write RED structural tests that build the app offline and inspect its config/component tree with all required regions/native components/safety constraints. Add subprocess CLI tests for exact command `.\.venv\Scripts\python.exe -m debugmate.ui.serve --host 127.0.0.1 --port <reserved>`: reject non-loopback host, invalid/in-use port, expose no share mode, and serve `GET /config` within 30 seconds with HTTP 200 and JSON containing the Gradio app config/version. Capture child PID and terminate it in test `finally`; assert port closes afterward.

Implement `build_app(service: ResultApplicationService) -> gr.Blocks` with native components/private callbacks only and approved-input service boundary. Add `src/debugmate/ui/serve.py` with `argparse` requiring literal host `127.0.0.1` and integer 1024–65535 port, constructing configured local roots/service and calling app launch with `server_name=host`, `server_port=port`, `share=False`, no browser open. This is the sole runnable UI entry: `python -m debugmate.ui.serve --host 127.0.0.1 --port N`. Readiness contract is official Gradio `GET http://127.0.0.1:N/config`: status 200, JSON object containing app `version` and `components`; no custom health endpoint. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Offline app build contains the exact three regions, native tabs/components and six-field confirmation workflow.
- Status/results/actions are first-screen oriented; CSS contains 1366 grid and 1024/768 responsive rules with no body horizontal overflow rule violation.
- No raw diagnosis HTML, arbitrary path/upload/microphone, shell/terminal/installer, external JS/CDN or marketing hero exists.
- UI/callback config contains no DiagnosisRunOutcome input/state; live work begins only through approved redacted input passed to `diagnose_and_compose`.
- Queue/concurrency/once controls are configured and callbacks are private; loopback/share-false is default.
- Exact module command starts a real app; `/config` readiness contract succeeds within 30 seconds; invalid host/port fails and captured process/port clean up.
- Exact safety/privacy and command display-only copy appears.
</acceptance_criteria>
<done>The Gradio app is a reproducible native workbench structure that cannot bypass service, path or command safety boundaries.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\ui\test_app.py</automated>
</verify>
</task>

<task type="auto" id="04-06-04">
<name>Task 4: Callback integration, correction confirmation and TOCTOU-safe downloads</name>
<files>
src/debugmate/ui/app.py
tests/ui/test_callbacks.py
</files>
<read_first>
src/debugmate/ui/app.py
src/debugmate/ui/presentation.py
src/debugmate/results/service.py
src/debugmate/results/verifier.py
.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
tests/results/test_service.py
tests/ui/test_app.py
</read_first>
<action>
Write RED callback tests for approved-input live diagnosis, loading replay, running stage events, draft-only field edits, old→new summary, no rerun on blur/change, no-op disabled confirmation, explicit create-new-run, retry scope, refresh restore, completed/partial/failed component values and full/partial verified download. Require live callback to call `diagnose_and_compose` and omit/reject any outcome payload. Cover compose→refresh→correct and replay→correct through the server-side outcome store; tamper outcome/ZIP/member before callback and require safe failure with no path returned. Assert callbacks accept approved payload or strict IDs/drafts only and never arbitrary paths/outcomes.

Implement thin callbacks: strict parse → service call/generator events → `render_view_state` → one atomic component update set. Preserve last verified result during running only with literal `上一结果`. Resolve every Image/Audio/File/DownloadButton path through the service immediately before returning; clear a component if verification fails. Never return exception text. Add accessible labels/alt/transcript/focus-compatible status outputs from UI-SPEC. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Editing fields updates draft/count only; rerun occurs solely after explicit confirmation and creates new IDs.
- Running emits seven ordered stage labels, disables duplicate actions and shows no invented percentage.
- Every component value/visibility/action matches pure mapping for completed/partial/failed/replay/fallback.
- Tampering after render causes download/media callback failure and returns no stale path.
- Callback signatures contain no arbitrary filesystem path input and never expose raw exceptions/provider values.
- Transcript/alt labels and icon-plus-text statuses keep essential content nonvisual/nonaudio.
</acceptance_criteria>
<done>All UI events preserve strict service and fresh-file-verification boundaries, including correction, replay, recovery and downloads.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\ui\test_callbacks.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m pytest -q tests\ui tests\results\test_service.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m ruff check src\debugmate\ui src\debugmate\results\service.py tests\ui</automated>
</verify>
</task>
</tasks>

<verification>
- Build app offline and execute the full pure state visibility matrix.
- Exercise compose/replay/correction/restore/retry/download against verified on-disk bundles.
- AST/config scan confirms no shell, raw HTML, arbitrary path or command execution boundary.
- Full offline suite and Ruff pass before browser visual work begins.
</verification>

<success_criteria>
- UX-01 presents all verified diagnostic and multimodal content in one native Gradio page.
- UX-02 downloads only freshly verified full/partial bundles.
- UX-03 replay is allowlisted, verified and unmistakably labeled.
- UX-04 exposes exact stage/completed/inherited/retry/fallback truth and preserves immutable correction history.
</success_criteria>

<output>Create `.planning/phases/04-multimodal-results-ui/04-06-SUMMARY.md`.</output>
