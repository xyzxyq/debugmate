---
phase: 04-multimodal-results-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/debugmate/results/__init__.py
  - src/debugmate/results/contracts.py
  - src/debugmate/results/font.py
  - src/debugmate/results/loader.py
  - src/debugmate/results/outcome_store.py
  - tests/results/__init__.py
  - tests/results/conftest.py
  - tests/results/test_contracts.py
  - tests/results/test_loader.py
  - fixtures/replay/index.json
  - fixtures/replay/module-not-found/outcome.json
  - fixtures/replay/module-not-found/source/extraction.json
  - fixtures/replay/module-not-found/source/case-facts.json
  - fixtures/replay/module-not-found/source/sufficiency.json
  - fixtures/replay/module-not-found/source/routing.json
  - fixtures/replay/module-not-found/source/retrieval.json
  - fixtures/replay/module-not-found/source/diagnosis.json
  - fixtures/replay/module-not-found/source/manifest.json
  - scripts/generate-replay-fixture.py
autonomous: true
requirements: [MULTI-04, UX-04]
must_haves:
  truths:
    - "Phase 4 dependencies are exactly pinned and all slow, browser, local-media and network gates are excluded from the default offline suite."
    - "Result contracts are frozen, strict and extra-forbid; identity and publication status cannot be inferred from filenames or missing files."
    - "One frozen PreparedGenerationContext is created before projection and contains both GenerationProfile and its exact verified ResolvedFont."
    - "Only a completed, strictly revalidated DiagnosisRunOutcome whose immutable Phase 3 source bundle verifies can enter presentation."
    - "A strict server-side outcome store persists the full redacted outcome by run ID, and the committed replay source is reproducibly generated and passes the current Phase 3 verifier."
    - "Loader failure occurs before any result temporary directory exists and exposes only a fixed safe code and stage."
  artifacts:
    - path: "src/debugmate/results/contracts.py"
      provides: "PreparedGenerationContext, GenerationProfile, ArtifactIdentity, ResultManifest, ResultViewState and typed failure contracts"
      contains: "class ArtifactIdentity"
    - path: "src/debugmate/results/loader.py"
      provides: "Strict source outcome and Phase 3 evidence loader"
      contains: "def load_verified_outcome("
    - path: "src/debugmate/results/outcome_store.py"
      provides: "Strict run-ID DiagnosisRunOutcome repository"
      contains: "class DiagnosisOutcomeStore"
    - path: "tests/results/conftest.py"
      provides: "Verified completed outcome/source bundle and tamper builders"
      contains: "completed_source_bundle"
  key_links:
    - from: "src/debugmate/diagnosis/workflow.py::DiagnosisRunOutcome"
      to: "src/debugmate/results/loader.py::load_verified_outcome"
      via: "strict round-trip, validate_diagnosis_outcome and source verify_bundle before projection"
      pattern: "validate_diagnosis_outcome.*verify_bundle"
    - from: "src/debugmate/results/contracts.py::ArtifactIdentity"
      to: "src/debugmate/contracts.py::DiagnosisRecord"
      via: "canonical diagnosis JSON SHA-256 and exact case/run/schema identity"
      pattern: "diagnosis_sha256.*schema_version.*generation_version"
---

<objective>
Create the Phase 4 dependency/test foundation, strict result identities and a fail-closed loader for verified Phase 3 outcomes.

Purpose: prevent forged, stale or incomplete source material from reaching any renderer or result directory.
Output: pinned runtime, markers/fixtures, strict contracts and source loader with typed safe failures.
</objective>

<execution_context>
@.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
@.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
@.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
@.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
@docs/superpowers/specs/2026-07-13-phase4-multimodal-results-ui-design.md
@.planning/phases/03-traceable-diagnosis-workflow/03-VERIFICATION.md
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [source outcome, immutable Phase 3 evidence, result identity, safe UI failure]
trust_boundaries: [serialized outcome to strict model, source bundle to result loader, loader failure to UI state]
threats:
  - id: T4-01
    severity: high
    vector: "A forged or modified outcome bypasses Phase 3 validation and becomes a course artifact."
    control: "strict round-trip, public outcome validator, source bundle verifier and diagnosis hash cross-check"
  - id: T4-12
    severity: high
    vector: "Loader exceptions disclose provider bodies, secrets, stack traces or absolute paths."
    control: "fixed error enum and value-free safe messages; raw exception retained only in redacted local development logging"
  - id: T4-14
    severity: high
    vector: "An old Schema/generation version or incomplete result is restored as current success."
    control: "literal supported versions, explicit state contract and unknown-version fail closed"
</threat_model>

<tasks>
<task type="auto" id="04-01-01">
<name>Task 1: Pin dependencies, markers and Wave 0 fixture foundation</name>
<files>
pyproject.toml
src/debugmate/results/__init__.py
tests/results/__init__.py
tests/results/conftest.py
tests/results/test_contracts.py
fixtures/replay/index.json
fixtures/replay/module-not-found/outcome.json
fixtures/replay/module-not-found/source/extraction.json
fixtures/replay/module-not-found/source/case-facts.json
fixtures/replay/module-not-found/source/sufficiency.json
fixtures/replay/module-not-found/source/routing.json
fixtures/replay/module-not-found/source/retrieval.json
fixtures/replay/module-not-found/source/diagnosis.json
fixtures/replay/module-not-found/source/manifest.json
scripts/generate-replay-fixture.py
</files>
<read_first>
pyproject.toml
.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
.planning/phases/03-traceable-diagnosis-workflow/03-VERIFICATION.md
src/debugmate/diagnosis/workflow.py
src/debugmate/evidence.py
tests/diagnosis/test_workflow_evidence.py
fixtures/cases/module_not_found/diagnosis.json
</read_first>
<action>
Use strict TDD/setup verification as one reviewable unit. First add a failing import/version/marker test in `tests/results/test_contracts.py` that requires installed `gradio==6.20.0`, `edge_tts==7.2.8` and `playwright==1.61.0`, and asserts the default pytest expression excludes `cloud`, `ocr`, `network`, `browser` and real-local `tts`. Run it RED before changing dependencies. Pin all three packages in project dependencies, add marker descriptions, and set `addopts` to `-ra -m "not cloud and not ocr and not network and not browser and not tts"`; fake TTS and deterministic media tests remain unmarked. Install with `python -m pip install -e ".[dev]"`, then assert imports and versions. Browser execution is frozen to Playwright's explicit `channel="msedge"` on this Windows target; the explicit browser gate fails if that channel is unavailable and never relies on a global Python 3.10/runtime.

Create the results packages and shared fixture builders that produce a strict completed Phase 3 outcome, publish it with the existing publisher, verify it from disk, clone/tamper manifests safely, and create the state matrix/fake-attempt inputs used later. Add deterministic `scripts/generate-replay-fixture.py`: fixed fictional approved/redacted input and fixture providers recreate a complete strict `outcome.json` plus fully populated `source/` Phase 3 evidence bundle, run current `verify_bundle()`, and write canonical bytes without identity-changing wall-clock data. Commit the generated `fixtures/replay/module-not-found/` outcome and source bundle. The replay index stores display label/stable fixture ID plus controlled relative paths to both records, never an absolute/user path. Tests load the committed outcome strictly, verify the committed source with current `verify_bundle()`, regenerate into a temp root and compare canonical outcome/artifact identities. Do not create report/PNG/audio/result production code in this task. Commit only these files with message `test(04-01): establish reproducible result wave zero` after the focused GREEN run.
</action>
<acceptance_criteria>
- `pyproject.toml` contains exact pins `gradio==6.20.0`, `edge-tts==7.2.8` and `playwright==1.61.0`.
- Default pytest expression excludes `cloud`, `ocr`, `network`, `browser` and `tts` while an unmarked fake-media test is collected normally.
- `tests/results/conftest.py` can build and verify a completed Phase 3 source bundle without cloud or network.
- `fixtures/replay/index.json` contains a display label, fixture ID, case ID, run ID and controlled relative outcome/source paths, contains no drive-qualified path, and points only within `fixtures/replay`.
- The committed full outcome strict-validates, the committed source bundle passes current `verify_bundle()`, and deterministic regeneration preserves canonical identities.
- Import/version smoke and `pip check` exit 0.
</acceptance_criteria>
<done>The pinned environment imports Gradio/edge-tts at exact versions, default tests remain offline, and reusable verified source/tamper/replay fixtures exist.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_contracts.py -k "dependency or marker or fixture"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -c "import gradio,edge_tts; import importlib.metadata as m; assert gradio.__version__ == '6.20.0'; assert edge_tts.__version__ == '7.2.8'; assert m.version('playwright') == '1.61.0'"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m pip check</automated>
</verify>
</task>

<task type="auto" id="04-01-02">
<name>Task 2: Strict result identity and state contracts</name>
<files>
src/debugmate/results/contracts.py
src/debugmate/results/font.py
tests/results/test_contracts.py
</files>
<read_first>
.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
docs/superpowers/specs/2026-07-13-phase4-multimodal-results-ui-design.md
src/debugmate/contracts.py
src/debugmate/diagnosis/workflow.py
src/debugmate/evidence.py
src/debugmate/hashing.py
tests/diagnosis/test_contract_v11.py
tests/results/test_contracts.py
</read_first>
<action>
Write RED tests for strict primitive types, extra fields, frozen models, unknown schema/manifest/generation versions, malformed case/run/hash IDs, illegal completed/partial/failed artifact availability, replay without fixture identity, fallback without attempts, duplicate artifact members and raw error/path fields. Implement focused Pydantic models in `results/contracts.py`: `GenerationProfile`, `ResolvedFont`, `PreparedGenerationContext`, `ArtifactIdentity`, `ArtifactRecord`, `ArtifactAvailability`, `SafeFailure`, `AudioAttempt`, `AudioResult`, `ResultStatus`, `ResultMode`, `ResultManifest` and `ResultViewState`. Implement only `prepare_generation_context(...) -> PreparedGenerationContext` in `results/font.py`: resolve project-font-first/Windows allowlist, confine/reject links, hash current bytes, build GenerationProfile from exact report/card/recap contract versions plus font name/SHA, and return one frozen context containing that profile and matching resolved font. Validators require profile font name/SHA to equal context font and canonical profile hash determines `generation_version`; callers cannot combine separately prepared objects. Keep replay orthogonal to outcome status. Freeze the acyclic publication graph: `result-manifest.json` hashes/lists business payload only and excludes itself, `checksums.sha256`, ZIP and `publication.json`; `checksums.sha256` covers every ZIP member except itself and may include `result-manifest.json`; outside-only `publication.json` alone stores final ZIP hash plus result identity. No model accepts arbitrary paths or raw exceptions. Commit RED and GREEN separately.
</action>
<acceptance_criteria>
- Every public result model uses strict and extra-forbid validation; identity models are frozen.
- `ArtifactIdentity` requires exact case ID, source run ID, 64-hex diagnosis hash, schema 1.1.0 and nonblank generation version.
- `PreparedGenerationContext` is strict/frozen and contains the only matching GenerationProfile+ResolvedFont pair; preparation resolves/verifies once before projection and any component change alters or invalidates generation identity.
- Replay requires fixture ID/name/source identity independently from completed/partial/failed.
- Partial/failed states require failed stage, safe error code and retry scope; no raw exception/body/path field exists.
- Result manifest excludes manifest/checksums/ZIP/publication hashes; checksums covers all ZIP members except itself; only outside `publication.json` stores final ZIP hash/result identity.
- Contract tests reject all forged and unknown-version cases and round-trip valid models byte-stably.
</acceptance_criteria>
<done>Strict contracts represent all result identities and UI states without filename inference, coercion, mutable fields or hash cycles.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_contracts.py</automated>
</verify>
</task>

<task type="auto" id="04-01-03">
<name>Task 3: Verified completed-outcome loader and safe failure boundary</name>
<files>
src/debugmate/results/loader.py
src/debugmate/results/outcome_store.py
tests/results/test_loader.py
</files>
<read_first>
.planning/phases/03-traceable-diagnosis-workflow/03-VERIFICATION.md
.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
src/debugmate/results/contracts.py
src/debugmate/results/outcome_store.py
src/debugmate/diagnosis/workflow.py
src/debugmate/evidence.py
src/debugmate/hashing.py
src/debugmate/privacy/output_scan.py
tests/diagnosis/test_workflow_evidence.py
tests/results/conftest.py
</read_first>
<action>
Write RED integration/abuse tests for a valid completed outcome and for non-completed status, missing diagnosis, extra/coerced fields, forged run/idempotency/stage/facts/schema/build metadata, missing/tampered/unlisted source artifacts, wrong source directory identity, symlink/reparse source, and arbitrary path input. Add strict outcome-store tests for atomic full-redacted-outcome write/read by run ID, duplicate overwrite, tampered bytes, wrong directory/run identity, extra/coerced fields, symlink/reparse and arbitrary paths. Assert every rejection happens before a supplied result-root factory is invoked and returns only fixed `source_bundle_invalid`, `source_outcome_invalid`, `outcome_store_invalid` or `diagnosis_identity_mismatch` codes.

Implement `DiagnosisOutcomeStore(root)`: `write(outcome)` strict-validates and atomically persists the complete redacted `DiagnosisRunOutcome` at an ID-derived run record, while `read(run_id)` confines/rejects links, rereads canonical JSON, strict-validates and calls `validate_diagnosis_outcome`; it never stores provider bodies or raw input. Implement `load_verified_outcome(outcome: DiagnosisRunOutcome, *, evidence_root: Path) -> LoadedDiagnosisSource`: strict round-trip, call `validate_diagnosis_outcome`, require completed, derive only `evidence_root/case_id/run_id`, enforce confinement/no symlink, call `verify_bundle`, re-read `diagnosis.json` as strict `DiagnosisRecord`, compare manifest/outcome case/run/facts/schema/workflow/prompt/knowledge/stage identity, and compute `diagnosis_sha256` from canonical `model_dump(mode='json')`. `LoadedDiagnosisSource` contains typed values and an allowlisted source-manifest summary, never an arbitrary source path exposed to UI. Convert expected boundary failures to `ResultLoadError(code, stage)` with value-free messages. Commit RED/GREEN atomically.
</action>
<acceptance_criteria>
- `load_verified_outcome` calls both `validate_diagnosis_outcome` and `verify_bundle`.
- Only `WorkflowStatus.COMPLETED` with a diagnosis is accepted.
- Case/run/facts/schema/workflow/prompt/knowledge/stage identity mismatch fails before any `results/` temp path exists.
- `diagnosis_sha256` equals SHA-256 of canonical strict DiagnosisRecord JSON and changes on any diagnosis mutation.
- Arbitrary paths, symlinks/reparse points, missing/unlisted/tampered files and unknown versions fail closed.
- Outcome store atomically persists and rereads the complete redacted strict outcome by run ID; duplicate/tampered/mismatched records fail closed.
- Public failures expose fixed code/stage only and contain no traceback, provider body, absolute path or secret value.
</acceptance_criteria>
<done>A valid verified source loads to one immutable typed input; every forged, stale, incomplete or unsafe source fails before result generation with a safe typed error.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_loader.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m ruff check src\debugmate\results tests\results</automated>
</verify>
</task>
</tasks>

<verification>
- Run `\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_contracts.py tests\results\test_loader.py`.
- Run the full default offline suite and Ruff; no cloud/network/browser/local-TTS test is collected.
- Confirm failed loader cases leave no child under a fresh `results/` root.
</verification>

<success_criteria>
- Phase 4 has exact dependency and marker contracts.
- Result identities and state are strict and acyclic.
- No renderer can receive unverified or non-completed Phase 3 data.
</success_criteria>

<output>Create `.planning/phases/04-multimodal-results-ui/04-01-SUMMARY.md`.</output>
