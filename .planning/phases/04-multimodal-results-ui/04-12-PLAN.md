---
phase: 04-multimodal-results-ui
plan: 12
type: execute
wave: 3
depends_on: ["04-11"]
gap_closure: true
files_modified:
  - .planning/phases/04-multimodal-results-ui/04-VERIFICATION.md
  - .planning/phases/04-multimodal-results-ui/04-VALIDATION.md
  - .planning/phases/04-multimodal-results-ui/04-UAT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - scripts/run-phase4-verification-gates.ps1
  - tests/verification/test_phase4_gate_manifest.py
  - evidence/verification/phase4/gate-manifest.json
  - evidence/verification/phase4/controlled-source-hashes.json
  - evidence/verification/phase4/logs/
requirements: [MULTI-01, MULTI-02, MULTI-03, MULTI-04, MULTI-05, UX-01, UX-02, UX-03, UX-04]
autonomous: true
must_haves:
  truths:
    - "Current-code full tests, real Edge VQ-01..15, result/ZIP verification, Ruff, dependency, whitespace, privacy and secret gates pass before any requirement is marked complete."
    - "An independent verifier checks the repository and generated bytes, not execution summaries, and alone creates 04-VERIFICATION.md."
    - "The visual commit pointer resolves one immutable generation whose visual-qa.json has an exact safe schema, 15 unique IDs, valid generation-local screenshot hashes and truthful state/mode/backend/viewport semantics."
    - "Dify and edge external gates remain open and independent; SAPI human listening remains human_needed with a concrete pre-recording sign-off step."
    - "A strict persistent gate manifest and redacted hash-bound command logs prove every required automated gate passed; no skipped/empty automated command can be normalized to pass."
  artifacts:
    - path: ".planning/phases/04-multimodal-results-ui/04-VERIFICATION.md"
      provides: "Independent Phase 4 requirement, threat and evidence verdict"
      contains: "human_needed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement state synchronized only from passed verification"
      contains: "MULTI-01"
    - path: "evidence/verification/phase4/gate-manifest.json"
      provides: "Strict hash-bound Phase 4 command, artifact, residue, external-gate and human-listening evidence"
      contains: "phase4-gates-1.0.0"
    - path: "evidence/verification/phase4/logs/"
      provides: "Allowlisted redacted stdout/stderr files whose bytes are bound by the gate manifest"
      contains: ""
    - path: "evidence/verification/phase4/controlled-source-hashes.json"
      provides: "Canonical tested-commit hashes for the controlled implementation/source/test/script allowlist"
      contains: "tested_commit_sha"
  key_links:
    - from: "evidence/ui/phase4/visual-qa.commit.json"
      to: ".planning/phases/04-multimodal-results-ui/04-VERIFICATION.md"
      via: "pointer, immutable generation manifest, ledger and screenshot hash reconciliation"
      pattern: "generation_manifest_sha256"
    - from: ".planning/phases/04-multimodal-results-ui/04-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "pass-only traceability update"
      pattern: "MULTI-01.*UX-04"
---

# Phase 04 full gate and independent verification plan

<objective>
Run the complete current-code matrix and engineering gates, obtain an independent evidence-based Phase 4 verdict, and synchronize planning truth only when every automated Phase 4 requirement passes. External Dify/edge probes and physical listening remain explicitly separate gates.
</objective>

<execution_context>
@.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
@.planning/phases/04-multimodal-results-ui/04-UAT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@evidence/ui/phase4/visual-qa.commit.json
</execution_context>

<threat_model>
security_enforcement: true
threats:
  - "Verification rereads downloaded/result bytes through public verifiers and rejects symlinks, traversal, extra members, hash drift and same-run identity mismatch."
  - "Ledger validation uses an exact allowlist and rejects raw IDs/text, absolute paths, tokens, secrets, duplicate VQ IDs and unbound screenshots."
  - "Gate manifest top-level allowlist is exactly schema_version, tested_commit_sha, generated_at, commands, artifacts, residue, external_gates, human_listening. Each command is exactly command_id, exit_code, outcome, started_at, duration_ms, stdout_path, stdout_sha256, stderr_path, stderr_sha256, artifact_sha256; each artifact is exactly artifact_id, path, sha256, bytes. Outcome is pass/fail/open_external/human_needed and required automated commands may only be pass. Paths are repository-relative under evidence/verification/phase4/logs or an explicit controlled artifact allowlist."
  - "`tested_commit_sha` is the clean commit created after gate-runner/test RED/GREEN. `controlled-source-hashes.json` exact top-level fields are schema_version, tested_commit_sha, files; each file is exactly path, sha256, bytes and covers src/debugmate/**, tests/**, scripts/**, contracts/**, knowledge/**, fixtures/** and pyproject.toml. Later evidence/planning/docs commits may advance HEAD, but no controlled implementation byte may differ from the tested commit."
  - "Only redacted allowlisted command output is persisted. Before promotion, each log passes output privacy and secret scans; the manifest records its SHA-256, never raw sensitive input, secret, capability, absolute path or provider body."
  - "Planning documents cannot be advanced by skipped tests, summaries, screenshots alone or external-gate substitution."
</threat_model>

<tasks>

<task id="04-12-01" name="Run complete current-code gates">
  <read_first>04-VALIDATION.md; pyproject.toml pytest markers; scripts/run-phase4-visual-qa.ps1; evidence/ui/phase4/visual-qa.commit.json</read_first>
  <action>Use TDD before producing evidence: add RED tests for missing gate runner, dirty-tree refusal, exact manifest fields, skipped/zero-collected rejection, tested-commit binding and controlled-source hash drift; run them to fail. Implement the fail-closed runner and turn RED GREEN, then commit only `scripts/run-phase4-verification-gates.ps1` and `tests/verification/test_phase4_gate_manifest.py` as `test(04-12): add fail-closed Phase 4 gates`. Require `git status --porcelain` to be empty after that commit and have the runner itself refuse a dirty tree; capture `git rev-parse HEAD` as `tested_commit_sha`. Only then execute: full default pytest; all 15 Edge rows through `run-phase4-visual-qa.ps1 -VqSelector all`; explicit local SAPI `pytest -q -m tts tests/results/test_tts_live.py -k "real_local_sapi or real_sapi"` with a nonzero collected/pass assertion; Ruff; pip check; diff check; public result and downloaded full/partial ZIP verifier calls; two deterministic builds with byte-for-byte ZIP hash comparison; pointer-resolved visual generation exact-schema/15-ID/screenshot-hash/truth-field validation; repository/evidence privacy and secret scans; and process/owned-port/runtime/staging/backup zero-residue assertions. Dify/edge probes are separate `open_external` records and do not satisfy local SAPI. Human listening is a separate `human_needed` record. Sanitize stdout/stderr into controlled relative log files, reject unsafe bytes, hash the controlled implementation allowlist, then atomically publish the exact-allowlist manifest bound to `tested_commit_sha`.</action>
  <acceptance_criteria>No skipped, deselected-only, zero-collected or missing required automated command is pass; all nine Phase 4 requirements have direct current-code evidence; public result/full/partial ZIP verification and deterministic byte comparison pass; ledger hashes/schema pass; privacy/secret scans are clean; all lifecycle residue values are zero; every persisted log and artifact hash recomputes to the manifest.</acceptance_criteria>
  <verify><command>.\.venv\Scripts\python.exe -m pytest -q tests\verification\test_phase4_gate_manifest.py -k "runner_missing or dirty_tree or exact_allowlist or no_skip_as_pass or tested_commit or source_drift"; git add -- scripts/run-phase4-verification-gates.ps1 tests/verification/test_phase4_gate_manifest.py; git commit -m "test(04-12): add fail-closed Phase 4 gates"; if (git status --porcelain) { throw 'Gate execution requires a clean committed worktree.' }; $tested = git rev-parse HEAD; .\scripts\run-phase4-verification-gates.ps1 -TestedCommitSha $tested; .\.venv\Scripts\python.exe -m pytest -q tests\verification\test_phase4_gate_manifest.py -k "exact_allowlist or no_skip_as_pass or tts_machine_vs_human or public_result_zip or deterministic_bytes or pointer_generation_hashes or privacy_secret or zero_residue or recompute_hashes or tested_commit"</command><expected>RED is recorded before implementation; the runner/tests have their own clean commit; the runner refuses dirt, records that clean commit as tested_commit_sha, and all required gate/hash/residue checks pass.</expected></verify>
  <done>The gate runner/tests are independently committed and tested_commit_sha is a clean commit. Generated manifest/logs remain the evidence handoff for Task 02; requirements and verdict are unchanged.</done>
</task>

<task id="04-12-02" name="Independent Phase 4 verifier verdict">
  <read_first>All 04-01..04-11 plans/summaries; 04-CONTEXT.md; 04-UI-SPEC.md; 04-VALIDATION.md; REQUIREMENTS.md; source/tests/evidence named by each requirement</read_first>
  <action>Use a fresh independent verifier with no implementation role. It strictly parses the full gate manifest and controlled-source-hashes ledger, rejects unknown/missing fields, requires every automated command outcome=pass, verifies `git merge-base --is-ancestor tested_commit_sha HEAD`, and recomputes redacted log/artifact hashes. It obtains `git diff --name-only tested_commit_sha..HEAD`, permits only evidence/**, .planning/** and docs/** descendant changes, and rejects changes under the controlled implementation allowlist; it also rehashes every controlled path recorded in the source ledger against both the tested commit and current worktree. It resolves visual evidence only through root pointer -> immutable generation manifest -> generation-local ledger/screenshots. It then maps MULTI-01..05 and UX-01..04 to code/evidence, audits T4-01..T4-14 and creates 04-VERIFICATION.md. Summaries are navigation hints only. Commit generated evidence plus the verifier report afterward; HEAD may now differ from tested_commit_sha.</action>
  <acceptance_criteria>A pass requires all nine requirements and automated threats closed. Dify and edge are recorded `open_external`, not failed or passed by SAPI; SAPI machine validity is pass while listening remains `human_needed`.</acceptance_criteria>
  <verify><command>$m = Get-Content -Raw -LiteralPath evidence/verification/phase4/gate-manifest.json | ConvertFrom-Json; git merge-base --is-ancestor $m.tested_commit_sha HEAD; if ($LASTEXITCODE -ne 0) { throw 'tested commit is not an ancestor' }; $forbidden = @(git diff --name-only "$($m.tested_commit_sha)..HEAD" | Where-Object { $_ -notmatch '^(evidence/|\.planning/|docs/)' }); if ($forbidden.Count) { throw "controlled source changed: $($forbidden -join ',')" }; .\.venv\Scripts\python.exe -m pytest -q tests\verification\test_phase4_gate_manifest.py -k "independent_recompute or exact_allowlist or all_required_pass or ancestor or controlled_source_unchanged or pointer_only_reader"; Test-Path -LiteralPath .planning\phases\04-multimodal-results-ui\04-VERIFICATION.md; rg -n "MULTI-0[1-5]|UX-0[1-4]|VQ-15|open_external|human_needed|gate-manifest" .planning\phases\04-multimodal-results-ui\04-VERIFICATION.md</command><expected>tested_commit_sha is an ancestor, only evidence/planning/docs descendants changed, controlled source hashes and pointer-resolved visual evidence recompute, and the report covers every item.</expected></verify>
  <done>Commit only the verifier-authored report and immutable verification inputs as `docs(04-12): record independent Phase 4 verification`.</done>
</task>

<task id="04-12-03" name="Pass-only planning synchronization and recording sign-off">
  <read_first>evidence/verification/phase4/gate-manifest.json and every referenced redacted log/artifact; 04-VERIFICATION.md; 04-UAT.md; 04-VALIDATION.md; REQUIREMENTS.md; ROADMAP.md; STATE.md</read_first>
  <action>First strictly read the complete gate manifest and controlled-source-hashes ledger, require `git merge-base --is-ancestor tested_commit_sha HEAD`, allow descendant changes only below evidence/**, .planning/** and docs/**, rehash the controlled implementation/source/test/script allowlist against the tested commit and current worktree to prove it is byte-identical, recompute every referenced log/artifact hash, require every automated command item pass and every residue value zero, and confirm Dify/edge=open_external plus listening=human_needed remain separate. Do not require tested_commit_sha to equal current HEAD because Task 02 commits evidence/report. If and only if those checks and 04-VERIFICATION.md pass, mark MULTI-01..05 and UX-01..04 complete, set Phase 4 complete with 12 plans, update STATE counts/current focus and validation execution status, and close automated UAT gaps while retaining physical listening blocked/human_needed. Add the final pre-recording listening step. If any manifest/verifier item fails, keep Phase 4 executing and write exact gap facts instead.</action>
  <acceptance_criteria>Requirements, roadmap, state, validation and UAT agree exactly with the independent verdict; no external/manual check is falsely completed; the Phase 4 plan count is an exact integer after pass.</acceptance_criteria>
  <verify><command>$m = Get-Content -Raw -LiteralPath evidence/verification/phase4/gate-manifest.json | ConvertFrom-Json; git merge-base --is-ancestor $m.tested_commit_sha HEAD; if ($LASTEXITCODE -ne 0) { throw 'tested commit is not an ancestor' }; $forbidden = @(git diff --name-only "$($m.tested_commit_sha)..HEAD" | Where-Object { $_ -notmatch '^(evidence/|\.planning/|docs/)' }); if ($forbidden.Count) { throw "controlled source changed: $($forbidden -join ',')" }; .\.venv\Scripts\python.exe -m pytest -q tests\verification\test_phase4_gate_manifest.py -k "ancestor or controlled_source_unchanged or every_command_pass or every_hash or zero_residue or external_and_human_separate"; rg -n "Phase 4|MULTI-0[1-5]|UX-0[1-4]|human_needed|open_external|12/12" .planning\REQUIREMENTS.md .planning\ROADMAP.md .planning\STATE.md .planning\phases\04-multimodal-results-ui\04-VALIDATION.md .planning\phases\04-multimodal-results-ui\04-UAT.md</command><expected>tested commit remains an ancestor, only permitted evidence/planning/docs descendants changed, controlled bytes/hashes/outcomes/residue pass, and planning facts are consistent.</expected></verify>
  <done>Commit only synchronized planning documents as `docs(04-12): synchronize verified Phase 4 completion`.</done>
</task>

</tasks>

<verification>
Re-run planning consistency grep and `git diff --check`; compare requirement checkboxes/counts to the independent report. A failed verifier leaves Phase 4 open and must not be papered over by document edits.
</verification>

<success_criteria>
Phase 4 has a reproducible current-code test/evidence record, an independent verifier verdict, exact planning traceability, and honest separation of automated completion from Dify/edge external probes and the final human listening sign-off.
</success_criteria>
