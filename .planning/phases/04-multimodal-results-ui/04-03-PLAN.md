---
phase: 04-multimodal-results-ui
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/debugmate/results/card.py
  - tests/results/test_card.py
  - tests/results/golden/card-layout.json
autonomous: true
requirements: [MULTI-02, MULTI-05, UX-04]
must_haves:
  truths:
    - "The diagnostic card is measured then painted by Pillow at exactly 1600 px width from the same PresentationModel."
    - "Card rendering accepts only the complete `PreparedGenerationContext`, verifies its font/profile exactly match PresentationModel identity, and never resolves, splits or substitutes a font."
    - "Published PNG bytes are RGB, single-frame, metadata-free and reopen-verified; no browser/generative renderer is involved."
    - "Oversize content produces typed png_layout_failed without clipping, unreadable shrinking or placeholder output."
  artifacts:
    - path: "src/debugmate/results/card.py"
      provides: "PreparedGenerationContext-bound font verification, deterministic layout, Pillow rendering and PNG verification"
      contains: "def render_card("
    - path: "tests/results/golden/card-layout.json"
      provides: "Font-hash-qualified structural layout golden"
      contains: "canvas_width"
  key_links:
    - from: "src/debugmate/results/presentation.py::PresentationModel"
      to: "src/debugmate/results/card.py::render_card"
      via: "fixed sections, IDs and content budgets feed measure-before-paint layout"
      pattern: "PresentationModel.*measure.*paint"
    - from: "src/debugmate/results/font.py::ResolvedFont"
      to: "src/debugmate/results/contracts.py::ArtifactIdentity"
      via: "font SHA-256 and renderer version determine generation identity"
      pattern: "font_sha256.*generation_version"
---

<objective>
Generate and verify a deterministic, readable and safe Pillow diagnostic card with explicit partial failure semantics.

Purpose: satisfy the visual modality without fabricated evidence, renderer drift or silent truncation.
Output: font resolver, layout tree, PNG renderer/verifier and boundary tests.
</objective>

<execution_context>
@.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
@.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
@.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
@docs/superpowers/specs/2026-07-13-phase4-multimodal-results-ui-design.md
@.planning/phases/04-multimodal-results-ui/04-02-SUMMARY.md
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [PNG diagnosis card, font identity, readable diagnosis content, partial state]
trust_boundaries: [font filesystem to renderer, presentation text to layout, rendered bytes to result candidate]
threats:
  - id: T4-05
    severity: high
    vector: "Untrusted text causes image resource exhaustion/clipping, or PNG carries metadata/multiple frames/hidden payload."
    control: "constrained font allowlist, input/height/pixel caps, measure-before-paint, pixels-only RGB re-encode and disk reopen verification"
</threat_model>

<tasks>
<task type="auto" id="04-03-01">
<name>Task 1: Prepared-context font verification and deterministic measured layout</name>
<files>
src/debugmate/results/card.py
tests/results/test_card.py
tests/results/golden/card-layout.json
</files>
<read_first>
.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
docs/superpowers/specs/2026-07-13-phase4-multimodal-results-ui-design.md
src/debugmate/results/presentation.py
src/debugmate/results/contracts.py
src/debugmate/results/font.py
src/debugmate/hashing.py
src/debugmate/evidence.py
tests/results/test_presentation.py
</read_first>
<action>
Write RED tests for `PreparedGenerationContext`, changed font bytes after preparation, mismatched presentation/context generation profile, path/name/SHA mismatch, traversal/symlink values, 1600px canvas, multilingual wrap, stable sections and measured bounds. The layout and renderer receive the complete frozen context; Plan 04-01 owns all resolution.

Implement `verify_prepared_font(presentation, context: PreparedGenerationContext)`: strict-revalidate the complete context, re-confine/no-link/hash `context.resolved_font` bytes, and require exact equality with `context.generation_profile` plus the profile/font identity embedded in PresentationModel; no lookup/fallback/recombination/new identity is allowed. Add token-aware wrap, bounded budgets and pure `measure_card(presentation, context)`. No painting until full layout validates. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Card accepts only the complete prepared context whose resolved-font bytes/name/path and generation profile match PresentationModel; mismatch/link/traversal fails and no fallback selection occurs.
- Canvas width is exactly 1600 and every line/box stays within measured bounds.
- Section order and content/ID selection are deterministic; identical font hash and presentation produce identical layout JSON.
- Chinese, English, command, path and long-token wrap cases do not overflow.
- Prepared context font hash participates in generation identity; card cannot mutate that identity or fall back to Pillow default bitmap font.
</acceptance_criteria>
<done>A deterministic bounded layout tree is fully validated before painting and tied to the exact Chinese font bytes.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_card.py -k "font or layout or deterministic or wrap"</automated>
</verify>
</task>

<task type="auto" id="04-03-02">
<name>Task 2: Pillow paint, pixels-only sanitation and disk verification</name>
<files>
src/debugmate/results/card.py
tests/results/test_card.py
</files>
<read_first>
src/debugmate/results/card.py
src/debugmate/results/presentation.py
src/debugmate/results/contracts.py
src/debugmate/evidence.py
src/debugmate/hashing.py
tests/test_evidence.py
tests/results/test_card.py
</read_first>
<action>
Write RED artifact tests requiring PNG signature, `format='PNG'`, `mode='RGB'`, `n_frames=1`, exact measured dimensions, `image.info == {}`, no EXIF/ICC/text/animation chunks, deterministic bytes for identical fixed-font input, and changed hash for a meaningful presentation change. Seed malicious metadata/APNG-like inputs into the verifier and require rejection.

Implement `render_card(presentation, context: PreparedGenerationContext, target=target)` using only Pillow and validated layout; first call `verify_prepared_font(presentation, context)`. The renderer receives the one indivisible prepared context, reads its resolved font internally, and cannot resolve/substitute/recombine identity. Save/re-encode/reopen pixels-only RGB, return bound identity/font metadata/hash, and use no generative/browser/graph renderer. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Valid output has PNG signature, RGB mode, one frame, 1600px width and empty Pillow metadata.
- PNG bytes are deterministic for the same PresentationModel/font hash/version.
- EXIF, ICC, text chunks, APNG/multi-frame or modified bytes reject during disk verification.
- Returned card identity equals PresentationModel identity and includes font/renderer version metadata.
- Renderer imports Pillow only for visual generation and does not import browser, graph or image-generation clients.
</acceptance_criteria>
<done>The card candidate is a deterministic disk-reverified pixels-only PNG with exact identity and no hidden metadata.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_card.py -k "png or metadata or frame or bytes"</automated>
</verify>
</task>

<task type="auto" id="04-03-03">
<name>Task 3: Explicit layout failure and partial-modality contract</name>
<files>
src/debugmate/results/card.py
tests/results/test_card.py
</files>
<read_first>
src/debugmate/results/card.py
src/debugmate/results/contracts.py
src/debugmate/results/presentation.py
.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
tests/results/test_card.py
</read_first>
<action>
Write RED boundary tests at maximum accepted height/text/item/pixel values and immediately above each limit. Assert above-limit input yields `CardRenderFailure(code='png_layout_failed', failed_stage='card', retry_scope='card')`, writes neither PNG nor placeholder, preserves report/audio eligibility in `ArtifactAvailability`, and exposes no rejected text/path. Simulate paint and verification exceptions and require controlled temp cleanup plus `png_verify_failed` where appropriate.

Implement typed card failure results and fail-closed cleanup. Never shrink below fixed readable font sizes, silently omit required sections, crop the canvas, split to multiple images or generate placeholder art. Optional content omission is allowed only where the PresentationModel already records bounded omission counts and the card visibly states that count. Commit the boundary behavior with focused tests.
</action>
<acceptance_criteria>
- Every exact limit is accepted and each limit+1 case returns a fixed safe card failure.
- No failed render leaves a target/temp PNG or success-looking artifact record.
- Layout overflow never clips, shrinks required text, creates a second page or emits placeholder bytes.
- PNG failure remains orthogonal to report/audio availability and maps to an explicit partial result contract.
- Failure data contains code/stage/retry scope only, with no presentation value or absolute path.
</acceptance_criteria>
<done>Unrenderable visual content fails visibly and safely while other independently verified modalities remain eligible.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_card.py -k "oversize or partial or cleanup or failure"; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m ruff check src\debugmate\results\card.py tests\results\test_card.py</automated>
</verify>
</task>
</tasks>

<verification>
- Run all card tests twice with the same font and compare PNG/layout hashes.
- Open every produced test PNG from disk and assert dimensions/frame/mode/info.
- Full offline suite and Ruff remain green; Phase 3 evidence MP3 rejection is unchanged.
</verification>

<success_criteria>
- MULTI-02 has a faithful deterministic PNG derived from PresentationModel.
- PNG failures satisfy MULTI-05/UX-04 without deception or loss of other modalities.
- T4-05 resource and metadata paths are fully tested.
</success_criteria>

<output>Create `.planning/phases/04-multimodal-results-ui/04-03-SUMMARY.md`.</output>
