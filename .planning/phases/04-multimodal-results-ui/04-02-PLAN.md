---
phase: 04-multimodal-results-ui
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/debugmate/results/presentation.py
  - src/debugmate/results/report.py
  - tests/results/test_presentation.py
  - tests/results/test_report.py
  - tests/results/golden/module-not-found-report.md
autonomous: true
requirements: [MULTI-01, UX-01]
must_haves:
  truths:
    - "One frozen PresentationModel, built from one PreparedGenerationContext, is the only semantic input to report, card and recap renderers."
    - "The Chinese Markdown report has fixed sections, preserves English technical literals and adds no diagnosis claim or command."
    - "Grounded and inferred causes are visibly distinct and every displayed citation maps to a verified diagnosis evidence anchor."
    - "Provider/user Markdown, HTML, URL or instruction text cannot escape the safe report boundary or leak a secret."
  artifacts:
    - path: "src/debugmate/results/presentation.py"
      provides: "Deterministic immutable diagnosis projection"
      contains: "class PresentationModel"
    - path: "src/debugmate/results/report.py"
      provides: "Safe fixed-section Markdown and citations renderer"
      contains: "def render_report("
    - path: "tests/results/golden/module-not-found-report.md"
      provides: "Reviewed deterministic report fixture"
      contains: "# DebugMate 诊断报告"
  key_links:
    - from: "src/debugmate/results/loader.py::LoadedDiagnosisSource"
      to: "src/debugmate/results/presentation.py::build_presentation"
      via: "strict deterministic projection with one PreparedGenerationContext retaining stable IDs and ArtifactIdentity"
      pattern: "LoadedDiagnosisSource.*PresentationModel"
    - from: "src/debugmate/results/presentation.py::PresentationModel"
      to: "src/debugmate/results/report.py::render_report"
      via: "report and citations consume no provider, filesystem or LLM input"
      pattern: "render_report\(presentation: PresentationModel"
---

<objective>
Build the single presentation projection and deterministic safe Chinese Markdown/citation artifacts.

Purpose: make the human-readable result faithful to the strict diagnosis graph and reusable by all later modalities.
Output: frozen PresentationModel, report renderer, citation export and golden tests.
</objective>

<execution_context>
@.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
@.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
@.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
@docs/superpowers/specs/2026-07-13-phase4-multimodal-results-ui-design.md
@.planning/phases/04-multimodal-results-ui/04-01-SUMMARY.md
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [diagnosis semantics, Markdown report, citations, command safety metadata]
trust_boundaries: [verified diagnosis to presentation, presentation strings to Markdown/UI, evidence URL to citation export]
threats:
  - id: T4-04
    severity: high
    vector: "Untrusted Markdown/HTML/URL content injects UI elements or makes unsupported evidence look official."
    control: "deterministic escaping, no raw HTML, verified evidence-only URLs, golden and adversarial output tests"
  - id: T4-12
    severity: high
    vector: "Rendering exceptions or unsafe values expose raw provider data, local paths or secrets."
    control: "bounded fields, output privacy scan and value-free typed render failures"
</threat_model>

<tasks>
<task type="auto" id="04-02-01">
<name>Task 1: Frozen deterministic PresentationModel</name>
<files>
src/debugmate/results/presentation.py
tests/results/test_presentation.py
</files>
<read_first>
src/debugmate/results/contracts.py
src/debugmate/results/loader.py
src/debugmate/results/font.py
src/debugmate/contracts.py
src/debugmate/privacy/output_scan.py
.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
docs/superpowers/specs/2026-07-13-phase4-multimodal-results-ui-design.md
tests/results/conftest.py
</read_first>
<action>
Write RED tests that shuffle equivalent diagnosis list/input ordering and require byte-identical projection, stable IDs/literals/labels/metadata/citations/confidence/limitations/recap units. Reject duplicate/dangling IDs, anything except `LoadedDiagnosisSource`, missing/non-Prepared context, context with mismatched profile/font, changed font bytes after preparation, and attempts to pass profile/font separately.

Implement small frozen strict records and `build_presentation(source: LoadedDiagnosisSource, context: PreparedGenerationContext) -> PresentationModel`. Strictly revalidate the whole context, verify its resolved font bytes still match its embedded profile, and bind canonical `generation_version`, renderer contract versions and font SHA into PresentationModel/ArtifactIdentity before projection. Preserve semantic order where meaningful and stable-ID sorting for set-like displays; never synthesize a cause/citation/command/conclusion. Commit RED then GREEN.
</action>
<acceptance_criteria>
- `PresentationModel` is strict/frozen and constructible only through `build_presentation(source, context)` with one prepared, internally matching context.
- Same diagnosis/source identity yields byte-identical canonical projection regardless of irrelevant mapping order.
- Every stable ID and support relation is retained; grounded/inference labels match `claim_kind` exactly.
- English traceback/error/package/version/path markers and command strings are byte-identical to DiagnosisRecord values.
- Generation version is the context profile hash and binds report/card/recap versions plus context font SHA with no timestamp/machine path.
- No LLM, adapter, network, filesystem renderer or new diagnostic conclusion exists in presentation code.
</acceptance_criteria>
<done>The verified diagnosis has one immutable deterministic projection that fully supplies all three renderers without semantic invention.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_presentation.py</automated>
</verify>
</task>

<task type="auto" id="04-02-02">
<name>Task 2: Fixed-section safe Markdown report</name>
<files>
src/debugmate/results/report.py
tests/results/test_report.py
tests/results/golden/module-not-found-report.md
</files>
<read_first>
src/debugmate/results/presentation.py
src/debugmate/results/contracts.py
src/debugmate/privacy/output_scan.py
src/debugmate/contracts.py
.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
tests/results/test_presentation.py
tests/results/conftest.py
</read_first>
<action>
Write RED golden/adversarial tests for the exact nine sections: case/version summary, observed facts, cause/evidence, checks, fixes, verification, missing information, confidence/applicability/limitations, citations. Require grounded/inference literals, stable IDs, safe Markdown escaping, a command fence longer than any embedded backtick run, and adjacent platform/impact/expected-result/rollback. Test injected headings, links, HTML/script, image syntax, code fences, secret/path markers and instruction-like text; rejected output returns `report_render_failed` without echoing the value.

Implement `RenderedReport(identity, markdown, sha256)` and `render_report(presentation: PresentationModel) -> RenderedReport` using only fixed local templates. Preserve command bytes inside dynamically safe fences and English technical strings; escape all other untrusted Markdown text. Run `assert_export_safe` on the final Markdown. Compare the reviewed module-not-found report byte-for-byte, normalize only LF and UTF-8, and do not embed timestamps or absolute paths. Commit RED/GREEN separately.
</action>
<acceptance_criteria>
- Report has all nine fixed sections in the specified order and a byte-stable golden fixture.
- Root causes begin with literal `有依据` or `推断` and show stable support IDs.
- Commands remain unchanged in safe fenced blocks and show platform, impact, expected result and rollback.
- User/provider headings, links, HTML, image syntax and fence sequences cannot create unintended Markdown structure.
- Final report passes output privacy scan and rendering errors expose only `report_render_failed`.
- Report contains no claim, evidence URL or command absent from PresentationModel.
</acceptance_criteria>
<done>The report is deterministic, faithful, safe to pass to native Gradio Markdown and independently hashable.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_report.py -k "not citation"</automated>
</verify>
</task>

<task type="auto" id="04-02-03">
<name>Task 3: Verified citation export and identity binding</name>
<files>
src/debugmate/results/report.py
tests/results/test_report.py
</files>
<read_first>
src/debugmate/results/report.py
src/debugmate/results/presentation.py
src/debugmate/diagnosis/evidence_binding.py
src/debugmate/knowledge/retrieval.py
src/debugmate/privacy/output_scan.py
tests/diagnosis/test_evidence_binding.py
tests/results/test_report.py
</read_first>
<action>
Write RED tests requiring `citations.json` rows to contain only evidence ID, official title/source URL, version range/source locator, chunk/source/build identity and supported candidate/fact IDs already present in the strict projection. Test unsupported URL schemes, mismatched support IDs, invented URL/title, duplicate evidence IDs and injection/secret strings.

Implement `render_citations(presentation) -> RenderedCitations` with deterministic stable-ID order and canonical JSON bytes. Permit only `https` URLs already present in verified evidence metadata; do not perform network lookup or repair. Cross-check every grounded report reference against an emitted row and every emitted row against PresentationModel. Apply output safety scanning and share the same ArtifactIdentity as the report. Add a cross-artifact test proving a changed identity or citation graph rejects. Commit as one focused feature commit after RED/GREEN.
</action>
<acceptance_criteria>
- Citation rows are canonical, stable-ID ordered and contain only verified PresentationModel evidence metadata.
- Only verified HTTPS source URLs pass; `file`, `javascript`, `data`, drive paths and invented URLs reject.
- Every grounded root-cause support ID resolves to an emitted fact/evidence pair.
- Report and citations carry exactly equal ArtifactIdentity.
- Citation output passes privacy scan and contains no provider raw body/full chunk/reasoning.
</acceptance_criteria>
<done>Markdown and canonical citations form one identity-bound, fully cross-referenced textual modality with no unsupported source.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_report.py -k citation; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m ruff check src\debugmate\results\presentation.py src\debugmate\results\report.py tests\results\test_presentation.py tests\results\test_report.py</automated>
</verify>
</task>
</tasks>

<verification>
- Run all presentation/report tests and compare the tracked golden file after a clean repeat render.
- Grep the renderer for provider/LLM/network imports; none are permitted.
- Run output privacy/adversarial cases before the full offline suite and Ruff.
</verification>

<success_criteria>
- MULTI-01 is fully represented by one faithful Chinese report from the strict diagnosis.
- UX-01 has safe report/citation display data with exact evidence provenance.
- Later PNG/audio/UI layers can consume PresentationModel without reading DiagnosisRecord or provider data directly.
</success_criteria>

<output>Create `.planning/phases/04-multimodal-results-ui/04-02-SUMMARY.md`.</output>
