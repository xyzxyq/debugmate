---
phase: 04-multimodal-results-ui
plan: 07
status: none_observed_yet
updated: 2026-07-13
---

# Plan 04-07 Gaps

## GAP-01 — VQ-01 real-browser three-region layout collapse (blocking)

- **Requirement / threat:** UX-01, UX-04; VQ-01 and VQ-11 through VQ-15
  depend on the same three-region layout boundary.
- **Reproducer:** `..\\.venv\\Scripts\\python.exe -m pytest -q -m browser
  tests\\ui\\test_browser.py` at commit `3e75eea` plus the uncommitted
  verification-only browser test.
- **Expected:** at a real 1366 × 768 Microsoft Edge render, the three native
  workbench regions are visible and at least 280 px, 360 px and 440 px wide,
  respectively, with their headings/action reachable without horizontal body
  scrolling.
- **Actual:** the real loopback Gradio render reports region widths
  `[67, 67, 67]`; the nested `.workbench-grid` CSS selector applies its
  3-column grid recursively, placing all regions in the first 296 px column
  and stacking them to a 3857 px page.  The fixed replay combobox is not
  visible/clickable.  The screenshot is a real current-app render at
  `evidence/ui/phase4/VQ-01.png`, SHA-256
  `12be2e55e45f78ddee0f8c6cdbc9cce4ffdd4c494192d63c2c22c6ef61fd10cc`.
- **Owning files:** `src/debugmate/ui/app.py` (`workbench-grid` structure and
  CSS selector); subsequent corrective work must be in a new explicit gap
  plan, not Plan 04-07.
- **Stopped gate:** browser VQ acceptance. VQ-02 through VQ-15 screenshots
  and Phase 4 independent handoff are blocked until the UI layout is repaired
  and all browser evidence is regenerated.

This plan remains verification-only: no production/CSS change was made here.
