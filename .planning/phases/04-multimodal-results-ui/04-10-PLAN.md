---
phase: 04-multimodal-results-ui
plan: 10
type: execute
wave: 1
depends_on: ["04-09"]
gap_closure: true
files_modified:
  - src/debugmate/ui/qa_scenarios.py
  - src/debugmate/ui/serve.py
  - src/debugmate/ui/app.py
  - src/debugmate/ui/presentation.py
  - tests/ui/test_qa_scenarios.py
  - tests/ui/test_app.py
  - tests/ui/test_callbacks.py
  - tests/ui/test_browser.py
  - tests/ui/test_qa_capability.py
requirements: [MULTI-04, MULTI-05, UX-01, UX-02, UX-03, UX-04]
autonomous: true
must_haves:
  truths:
    - "A test-only, loopback-only scenario factory can deterministically drive replay, running, partial, failed and fallback truth states without becoming a production UI choice or accepting a filesystem path; ordinary startup registers no QA route or handler."
    - "Partial TTS/PNG states expose only the existing scoped retry contract; source_bundle_invalid exposes no unverified artifact or download."
    - "A real Edge correction flow preserves the old run until explicit confirmation and creates a distinct verified run only after 创建新运行."
    - "VQ-02/03/06/07/08/09/10 tests begin RED and may write only staged audit evidence; this plan does not claim matrix completion."
  artifacts:
    - path: "src/debugmate/ui/qa_scenarios.py"
      provides: "Strict QA-only scenario registry and deterministic stage gates"
      contains: "QaScenario"
    - path: "tests/ui/test_browser.py"
      provides: "Real Edge truth-state and correction acceptance scenarios"
      contains: "VQ-10"
  key_links:
    - from: "src/debugmate/ui/qa_scenarios.py"
      to: "src/debugmate/ui/serve.py"
      via: "dual gate: runner-owned process enablement plus an in-memory loopback request header capability"
      pattern: "DEBUGMATE_QA"
    - from: "src/debugmate/ui/app.py"
      to: "src/debugmate/results/service.py::retry_stage"
      via: "typed case/result IDs and the existing UiCallbacks.retry facade"
      pattern: "retry"
---

# Phase 04 truth-state browser harness and recovery plan

<objective>
Close the UAT truth-state gaps for VQ-02/03/06/07/08/09/10 with deterministic, auditable browser scenarios while preserving the production approval, path, download and provenance boundaries. Evidence remains staged until Plan 04-11 publishes the complete set.
</objective>

<execution_context>
@.planning/phases/04-multimodal-results-ui/04-UAT.md
@.planning/phases/04-multimodal-results-ui/04-UI-SPEC.md
@.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
@.planning/phases/04-multimodal-results-ui/04-09-SUMMARY.md
</execution_context>

<threat_model>
security_enforcement: true
threats:
  - "QA scenarios are enumerated IDs and require both runner-owned process enablement and a fresh high-entropy capability supplied only as a literal-loopback request header. The capability remains server-side: it never enters Gradio State, /config, HTML/DOM, URL/query, log, screenshot, ledger or error text."
  - "Ordinary startup does not register the QA route or handler. Missing/wrong headers, non-loopback requests and disabled process state return a value-free denial with zero scenario/workflow/result/download side effects."
  - "Download assertions consume server-issued capability bytes and verify filename/MIME/hash against the result allowlist; browser input never selects a server path."
  - "Screenshots and ledgers contain only redacted copy, safe error codes and hashed identities; no raw traceback secrets, absolute paths, tokens or approval material."
</threat_model>

<tasks>

<task id="04-10-01" name="RED truth-state contracts and QA isolation">
  <read_first>04-UAT.md; 04-UI-SPEC.md VQ-02/03/06/07/08/09; src/debugmate/ui/serve.py; src/debugmate/ui/app.py; tests/ui/test_browser.py</read_first>
  <action>Add failing tests first for an enum-only QA registry, dual runner-process plus loopback-header capability, ordered seven-stage gate, replay/partial/failed/fallback outcomes, and production-start route/handler absence. Assert unknown IDs, missing/wrong capability, non-loopback requests, extra fields and path-like values fail safely before workflow/result/download calls. Assert the capability value is absent from app config, component values, DOM-facing payloads, URLs, logs and serialized evidence.</action>
  <acceptance_criteria>RED failures identify missing QA registry and dual gate; tests prove no public dropdown/path textbox/live approval bypass, no registered ordinary-start handler, zero denied-request side effects and no capability disclosure.</acceptance_criteria>
  <verify><command>.\.venv\Scripts\python.exe -m pytest -q tests\ui\test_qa_scenarios.py tests\ui\test_qa_capability.py tests\ui\test_app.py -k "qa or scenario or production or capability"</command><expected>Fails only for the not-yet-implemented strict QA harness and dual capability gate.</expected></verify>
  <done>Commit tests alone as `test(04-10): define RED truth-state harness contracts` and record exact failures.</done>
</task>

<task id="04-10-02" name="Implement strict QA scenario factory and stage gate">
  <read_first>tests/ui/test_qa_scenarios.py; src/debugmate/results/contracts.py; src/debugmate/ui/presentation.py; src/debugmate/ui/serve.py</read_first>
  <action>Implement the minimum enum-backed factory for VQ-02/03/06/07/08/09. Compose outcomes through existing strict contracts and verified repository fixtures/results; stage control is an in-memory bounded synchronization primitive, not sleep-based progress. Register the private handler only when the runner-owned child process has QA enabled, then require a constant-time match of a fresh in-memory header capability on literal loopback for every request. Do not expose that value through environment-derived UI state/config/DOM/URL/log/evidence, production choices, paths or JSON.</action>
  <acceptance_criteria>Replay labels are orthogonal to completed; running stages are ordered with conflicting actions disabled and no percentages; partial/failed/fallback state values match UI-SPEC; ordinary startup has no QA route/handler; denied requests have zero side effects.</acceptance_criteria>
  <verify><command>.\.venv\Scripts\python.exe -m pytest -q tests\ui\test_qa_scenarios.py tests\ui\test_qa_capability.py tests\ui\test_view_state.py tests\ui\test_app.py</command><expected>All strict scenario, capability non-disclosure, denied-side-effect and presentation tests pass.</expected></verify>
  <done>Commit only factory/serve/presentation/tests as `feat(04-10): add isolated truth-state QA scenarios`.</done>
</task>

<task id="04-10-03" name="Bind scoped partial recovery after RED">
  <read_first>src/debugmate/results/service.py retry_stage; src/debugmate/ui/app.py UiCallbacks.retry and apply_payload; tests/ui/test_callbacks.py</read_first>
  <action>First add failing callback/component tests showing `tts_failed` and `png_layout_failed` lack a reachable scoped retry. Then minimally render one retry control whose label/scope comes from verified ResultViewState and bind it to existing UiCallbacks.retry. It must use strict case_id/result_id state, be disabled outside partial/retryable terminal states and during running, and never accept a stage/path supplied by the browser.</action>
  <acceptance_criteria>TTS failure keeps report/card/recap and exposes no fake empty audio; PNG failure keeps report/audio and no blank card; retry targets only the server-declared failed stage and invalid/tampered IDs make zero service calls.</acceptance_criteria>
  <verify><command>.\.venv\Scripts\python.exe -m pytest -q tests\ui\test_callbacks.py tests\ui\test_app.py -k "retry or partial or tts_failed or png_layout_failed"</command><expected>New RED becomes GREEN without changing non-partial behavior.</expected></verify>
  <done>Commit app/callback tests as `feat(04-10): expose verified scoped retry`.</done>
</task>

<task id="04-10-04" name="Real Edge state matrix and correction flow">
  <read_first>tests/ui/test_browser.py; src/debugmate/ui/app.py correction callbacks; 04-UI-SPEC.md VQ-02/03/06/07/08/09/10</read_first>
  <action>Add real msedge tests driven by Playwright context extra headers; the capability must never be typed or stored in page state. For VQ-03 hold/release every stage and assert order, disabled actions and no percentage. For VQ-02/06/07/08/09 assert literal truth labels, available/absent artifacts, safe seven-field failure details and download/retry semantics. For VQ-10 edit one field, assert pending old-to-new diff and unchanged identity, click 创建新运行, then assert distinct verified identity and recoverable prior result. Write only runner-injected staging screenshots/row fragments after assertions. Add a scan proving the capability is absent from config, DOM, URLs, logs, screenshots and row fragments.</action>
  <acceptance_criteria>All seven VQ scenarios pass real DOM assertions; VQ-08 exposes no path/traceback/download; correction cannot rerun on blur/change; staged records contain hashed identities only; runner restores/removes both QA environment variables and wrong/missing header tests leave zero side effects.</acceptance_criteria>
  <verify><command>.\.venv\Scripts\python.exe -m pytest -q -m browser tests\ui\test_browser.py -k "vq_02 or vq_03 or vq_06 or vq_07 or vq_08 or vq_09 or vq_10"</command><expected>Seven real Edge scenarios pass against one externally owned loopback server.</expected></verify>
  <done>Commit browser tests only as `test(04-10): exercise truth states and correction in Edge`; do not publish formal evidence or mark VQs complete.</done>
</task>

</tasks>

<verification>
Run focused RED before each production change, then the complete UI suite and Ruff. Inspect staged screenshots for safe copy and truth labels. Confirm ordinary `debugmate.ui.serve` config exposes no QA scenario selector/endpoint.
</verification>

<success_criteria>
VQ-02/03/06/07/08/09/10 have deterministic real-browser acceptance coverage, partial recovery is wired to the existing strict service, and QA control remains a runner-only test boundary. Formal evidence remains pending Plan 04-11.
</success_criteria>
