---
phase: 04-multimodal-results-ui
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/debugmate/results/recap.py
  - src/debugmate/results/media.py
  - src/debugmate/results/audio.py
  - src/debugmate/results/tts/__init__.py
  - src/debugmate/results/tts/base.py
  - src/debugmate/results/tts/dify.py
  - src/debugmate/results/tts/edge.py
  - src/debugmate/results/tts/sapi.py
  - scripts/sapi-synthesize.ps1
  - tests/results/test_recap.py
  - tests/results/test_media.py
  - tests/results/test_tts_chain.py
  - tests/results/test_tts_live.py
autonomous: true
requirements: [MULTI-03, MULTI-05]
must_haves:
  truths:
    - "A deterministic six-part Chinese recap from PresentationModel is the only text accepted by TTS adapters."
    - "Every audio candidate is size-bounded, signature screened and ffprobe-verified as one decodable tag-free audio stream lasting 30–60 seconds."
    - "The fixed fallback order is Dify then edge-tts then Windows SAPI+FFmpeg, with at most one deterministic rate retry per backend and value-free attempts."
    - "The real local Windows SAPI path produces an actual playable Chinese MP3; all-backend failure yields partial with recap text and no placeholder MP3."
  artifacts:
    - path: "src/debugmate/results/recap.py"
      provides: "Safe six-part deterministic Chinese recap"
      contains: "def compose_recap("
    - path: "src/debugmate/results/media.py"
      provides: "Bounded ffprobe MP3 verification"
      contains: "def probe_mp3("
    - path: "src/debugmate/results/audio.py"
      provides: "Ordered TTS fallback orchestration"
      contains: "class TtsFallbackChain"
    - path: "src/debugmate/results/tts/sapi.py"
      provides: "Real local SAPI WAV plus FFmpeg MP3 adapter"
      contains: "class SapiTtsAdapter"
    - path: "scripts/sapi-synthesize.ps1"
      provides: "Fixed SAPI file-to-file synthesis boundary"
      contains: "param("
  key_links:
    - from: "src/debugmate/results/presentation.py::PresentationModel"
      to: "src/debugmate/results/recap.py::SafeRecapText"
      via: "fixed six-part local template plus output privacy scan"
      pattern: "compose_recap.*SafeRecapText"
    - from: "src/debugmate/results/audio.py::TtsFallbackChain"
      to: "src/debugmate/results/media.py::probe_mp3"
      via: "every backend candidate is probed before acceptance or fallback"
      pattern: "synthesize.*probe_mp3"
---

<objective>
Produce a safe 30–60 second Chinese MP3 through a verified three-backend fallback chain, including a real local SAPI route.

Purpose: provide genuine playable audio while preserving same-diagnosis semantics, privacy and explicit degradation truth.
Output: recap composer, media probe, adapters, orchestration, fakes and real-local/external smoke gates.
</objective>

<execution_context>
@.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
@.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
@.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
@docs/superpowers/specs/2026-07-13-phase4-multimodal-results-ui-design.md
@.planning/phases/04-multimodal-results-ui/04-03-SUMMARY.md
</execution_context>

<threat_model>
security_enforcement: true
asvs_level: 1
block_on: high
assets: [safe recap text, TTS request identity, audio bytes, fallback attempts, local process boundary]
trust_boundaries: [recap to remote TTS, HTTP/service bytes to media candidate, SAPI/FFmpeg process to MP3, adapter error to manifest/UI]
threats:
  - id: T4-06
    severity: high
    vector: "Secrets or raw diagnosis data are sent to TTS or persisted in attempts/logs."
    control: "SafeRecapText nominal contract, output scan before every adapter and value-free request/attempt records"
  - id: T4-07
    severity: high
    vector: "Oversized, malformed, multi-stream or invalid-duration service bytes are accepted as MP3."
    control: "response byte cap, controlled temp file, signature screen, bounded ffprobe and exact stream/duration checks"
  - id: T4-08
    severity: high
    vector: "User text or paths inject commands into SAPI, PowerShell or FFmpeg."
    control: "fixed executables/scripts, shell=False argument arrays, controlled temp paths and no user text in script source"
  - id: T4-12
    severity: high
    vector: "HTTP bodies, command lines, absolute paths or secrets leak through fallback failures."
    control: "fixed safe error codes and redacted metadata only"
</threat_model>

<tasks>
<task type="auto" id="04-04-01">
<name>Task 1: Deterministic safe six-part recap</name>
<files>
src/debugmate/results/recap.py
tests/results/test_recap.py
</files>
<read_first>
src/debugmate/results/presentation.py
src/debugmate/results/contracts.py
src/debugmate/privacy/output_scan.py
src/debugmate/contracts.py
.planning/phases/04-multimodal-results-ui/04-CONTEXT.md
tests/results/test_presentation.py
</read_first>
<action>
Write RED tests requiring exactly six semantic units: phenomenon, leading cause plus uncertainty, first check, first fix, verification, remaining limitation. Require the same identity, preserved essential English exception/package names, no verbatim long command recital, deterministic bounded text, use of diagnosis `recap_text` only as safe source material, and rejection of secrets/injection/path leakage. Implement frozen `SafeRecapText(identity, text, sha256, units, word_budget_version)` and `compose_recap(presentation)`. Use local templates and deterministic content budgets; do not call an LLM or estimate/claim duration from characters. Apply output scan before return. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Recap contains all six required semantic units in fixed order and derives only from PresentationModel.
- Essential English error/package names remain; long commands are summarized by purpose rather than spoken character-by-character.
- Same presentation/version yields byte-identical UTF-8 recap and hash.
- Secrets, unsafe markers and instruction injection fail with fixed `recap_unsafe`; no value is echoed.
- Recap code has no network/TTS/LLM dependency and makes no unprobed duration assertion.
</acceptance_criteria>
<done>One identity-bound, privacy-scanned deterministic recap text is ready for every TTS backend and for transcript display.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_recap.py</automated>
</verify>
</task>

<task type="auto" id="04-04-02">
<name>Task 2: Real-media fixtures and bounded ffprobe verification</name>
<files>
src/debugmate/results/media.py
tests/results/test_media.py
</files>
<read_first>
.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
src/debugmate/results/contracts.py
src/debugmate/hashing.py
src/debugmate/evidence.py
tests/results/conftest.py
</read_first>
<action>
Write RED tests that create real ffprobe-decodable mono MP3 fixtures at 29.9, 30, 45, 60 and 60.1 seconds plus corrupt header, non-MP3, nonempty ID3/format tags, nonempty stream tags and multi-audio-stream media. Generate test media deterministically via fixed FFmpeg arguments in pytest temporary directories rather than committing large binaries. Test file/response byte caps, ffprobe timeout, malformed JSON, no audio, multiple audio streams, non-MP3 codec, every nonempty tag map and inclusive duration boundary.

Implement `MediaProbe`, `MediaProbeError` and `probe_mp3(path, *, timeout_seconds, max_bytes)`. Resolve a configured fixed `ffprobe` executable, call with argument list and `shell=False`: `-v error -show_entries format=duration:format_tags:stream=index,codec_type,codec_name,channels:stream_tags -of json`. Pre-screen MPEG signature only as a cheap gate; reject ID3 signatures and any nonempty `format.tags` or `streams[*].tags`. Authority is exactly one audio stream, MP3 codec, mono channel, positive inclusive 30–60 second duration and zero metadata tags. Do not return stdout, stderr, path or command line on failure. Commit RED/GREEN.
</action>
<acceptance_criteria>
- Real 30/45/60 second mono tag-free MP3s pass; 29.9/60.1, corrupt/non-MP3/multi-stream/ID3/format-tag/stream-tag candidates reject.
- Probe enforces file size and timeout before publication.
- ffprobe uses fixed executable, argument sequence and `shell=False`; no user value is interpolated into a shell string.
- Failure records contain fixed code only and no stdout/stderr/absolute path.
- Successful probe records duration milliseconds, codec, channels, byte size and SHA-256.
</acceptance_criteria>
<done>Only bounded, genuinely decodable, single-stream 30–60 second MP3 files can become audio candidates.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_media.py</automated>
</verify>
</task>

<task type="auto" id="04-04-03">
<name>Task 3: TTS adapters, ordered fallback and bounded retry</name>
<files>
src/debugmate/results/audio.py
src/debugmate/results/tts/__init__.py
src/debugmate/results/tts/base.py
src/debugmate/results/tts/dify.py
src/debugmate/results/tts/edge.py
src/debugmate/results/tts/sapi.py
scripts/sapi-synthesize.ps1
tests/results/test_tts_chain.py
tests/results/test_tts_live.py
</files>
<read_first>
src/debugmate/results/recap.py
src/debugmate/results/media.py
src/debugmate/results/contracts.py
src/debugmate/settings.py
src/debugmate/adapters/dify.py
.planning/phases/04-multimodal-results-ui/04-RESEARCH.md
tests/results/test_media.py
</read_first>
<action>
Write RED fake-adapter tests proving exact Dify→edge→SAPI order, success short-circuit, timeout/content-type/oversize/malformed-audio fallback, at most one deterministic rate-profile retry when duration is outside bounds, independent transport/rate attempt counts, temp cleanup and all-failed partial with recap text but no MP3. Assert attempts store backend/rate/status/safe code/duration/hash only and never text, key, response body or absolute path.

Define `TtsAdapter.synthesize(text: SafeRecapText, target: Path, request_identity: TtsRequestIdentity, rate_profile: RateProfile) -> AudioCandidate`. Implement `DifyTtsAdapter` with HTTPX timeouts, Bearer key from settings only, expected audio content type and streaming byte cap; `EdgeTtsAdapter` with fixed approved Simplified Chinese voice and discrete rates. Add fixed repository-owned `scripts/sapi-synthesize.ps1` with strict `param()` accepting only a controlled UTF-8 recap input-file path, controlled WAV output path, approved voice and bounded numeric rate. `SapiTtsAdapter` writes recap bytes to that temp input and invokes fixed `powershell.exe -NoProfile -NonInteractive -File <fixed-script> -InputTextFile <controlled> -OutputWaveFile <controlled> ...` as argv with `shell=False`; never use `-Command`, recap text in argv/script, or dynamic PowerShell source. FFmpeg uses argv/`shell=False` plus `-map_metadata -1 -write_id3v2 0`, then the output must pass tag-free `probe_mp3`. Implement `TtsFallbackChain` to privacy-scan immediately before each call, probe every candidate, retry current backend once using a fixed rate table only for duration, then fall through. Preserve Phase 3 `AudioEvidenceNotReady`; audio is written only to Phase 4 candidate temp space. Commit RED/GREEN by adapter/orchestrator review units.
</action>
<acceptance_criteria>
- Adapter interface accepts only `SafeRecapText` plus request identity and controlled target/rate.
- Fallback and retry order is deterministic; no backend receives more than two calls and non-duration failures receive no rate retry.
- Dify transport caps bytes/content type/timeouts; edge voice/rates are fixed; SAPI uses a fixed `-File` script plus controlled UTF-8 input and FFmpeg strips metadata; both use argv/`shell=False` with no recap text or `-Command` argument.
- Every accepted candidate passes `probe_mp3`; failed candidates and temp WAV/MP3 files are deleted.
- All-failed result is typed partial with `tts_failed`, transcript and attempts, and contains no empty/placeholder MP3.
- Attempt/error/log models contain no recap value, Authorization, response body, command line or absolute path.
</acceptance_criteria>
<done>Fake tests prove every transition, retry limit, safe record and all-failed behavior of the complete TTS chain.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q tests\results\test_tts_chain.py</automated>
</verify>
</task>

<task type="auto" id="04-04-04">
<name>Task 4: Real local SAPI gate and explicit external adapter gates</name>
<files>
src/debugmate/results/tts/sapi.py
scripts/sapi-synthesize.ps1
src/debugmate/results/tts/dify.py
src/debugmate/results/tts/edge.py
tests/results/test_tts_live.py
</files>
<read_first>
src/debugmate/results/tts/sapi.py
src/debugmate/results/tts/dify.py
src/debugmate/results/tts/edge.py
src/debugmate/results/audio.py
src/debugmate/results/media.py
src/debugmate/settings.py
.planning/phases/04-multimodal-results-ui/04-VALIDATION.md
</read_first>
<action>
Add a `@pytest.mark.tts` Windows-only test that uses the real `Microsoft Huihui Desktop` SAPI voice (or the deterministic first approved Simplified-Chinese SAPI allowlist match), fixed recap fixture and actual FFmpeg/ffprobe. It must generate a nonempty mono MP3, pass 30–60 second probe, record voice/rate/backend/hash and clean intermediate WAV. Failure due to missing approved voice/FFmpeg is a real local gate failure on the target Windows machine, not a fake success or unconditional skip.

Add separate `cloud and tts` Dify and `network and tts` edge smoke tests. These may clean-skip only when their explicit credential/network preconditions are absent; when run, assert real content type/bytes, ffprobe result, backend/fallback identity and value-free failure handling. Do not enable either in default pytest and do not purchase/configure a paid service. Run the real local marker now and record exact backend/hash/duration in the plan summary; record external skips as open gates rather than passed. Fix only adapter-contract mismatches found by these smokes, then commit.
</action>
<acceptance_criteria>
- `pytest -m tts tests/results/test_tts_live.py` performs a real SAPI→WAV→FFmpeg→MP3→ffprobe path and passes on this Windows target.
- Real-local output is mono, decodable, 30–60 seconds and records SAPI voice/rate/hash/duration without sensitive values.
- Dify test is marked `cloud and tts`; edge test is marked `network and tts`; neither runs in default suite.
- Missing Dify credentials or unapproved network records an explicit clean skip/open gate, never a pass claim.
- No paid API, subscription, key creation or network dependency is introduced into offline acceptance.
- The fixed SAPI script and adapter pass argv/AST/secret scans proving recap values never enter PowerShell source, command-line arguments or logs.
</acceptance_criteria>
<done>The guaranteed local fallback has fresh real-media evidence; remote adapters have truthful isolated gates and cannot masquerade as tested when unavailable.</done>
<verify>
<automated>\.\.venv\Scripts\python.exe -m pytest -q -m tts tests\results\test_tts_live.py; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m pytest -q -m "(cloud or network) and tts" tests\results\test_tts_live.py; if (($LASTEXITCODE -ne 0) -and ($LASTEXITCODE -ne 5)) { exit $LASTEXITCODE }; .\.venv\Scripts\python.exe -m ruff check src\debugmate\results tests\results</automated>
</verify>
</task>
</tasks>

<verification>
- Run recap, media and fallback fake tests in the default offline suite.
- Run the real local `tts` marker and inspect its MP3 with the public media probe.
- Run external marker commands separately and record pass/skip/fail without weakening local completion.
- Confirm Phase 3 evidence still rejects audio and no TTS code writes into `evidence/`.
</verification>

<success_criteria>
- MULTI-03 produces a real same-diagnosis Chinese MP3 and visible transcript.
- MULTI-05 has a fully tested Dify→edge→SAPI fallback plus truthful partial failure.
- T4-06/07/08/12 are controlled at request, process, media and error boundaries.
</success_criteria>

<output>Create `.planning/phases/04-multimodal-results-ui/04-04-SUMMARY.md` with real SAPI evidence and explicit Dify/edge gate state.</output>
