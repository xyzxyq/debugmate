---
phase: 09-current-evaluation-evidence
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - evaluation/phase9/cases.json
  - evaluation/phase9/prompt-criteria.json
  - src/debugmate/evaluation/__init__.py
  - src/debugmate/evaluation/contracts.py
  - tests/evaluation/test_contracts.py
  - tests/evaluation/test_case_matrix.py
  - tests/evaluation/test_prompt_comparison.py
autonomous: true
requirements: [EVAL-01, EVAL-03]
must_haves:
  truths:
    - "A versioned four-row registry covers live success plus privacy, insufficient information, long content, and the fixed local-fallback partial state without presenting an unavailable live run as passed."
    - "Every V1–V4 comparison row binds exactly the same sanitized input, facts, retrieval trace, knowledge build, schema, and case identity."
    - "Prompt provenance makes generated_live and verified_contract observably different claims, each bound to a safe fixed-case conclusion, accepted hashes, candidate hash, and source evidence."
  artifacts:
    - path: "evaluation/phase9/cases.json"
      provides: "The bounded four-case Phase 09 source registry"
      contains: "P9-C01-live-private"
    - path: "evaluation/phase9/prompt-criteria.json"
      provides: "Versioned V1–V4 objectives, adoption evidence, and limitations"
      contains: "v4-course-release"
    - path: "src/debugmate/evaluation/contracts.py"
      provides: "Strict frozen evaluation, common-input, prompt evidence binding, provenance, and source-manifest contracts"
      exports: ["CaseEvaluation", "PromptComparison", "PromptProvenance", "Phase10SourceManifest"]
  key_links:
    - from: "evaluation/phase9/cases.json"
      to: "src/debugmate/evaluation/contracts.py"
      via: "strict bounded validation and coverage-set equality"
      pattern: "live_success.*insufficient_data.*long_content.*privacy.*fallback_or_failure"
    - from: "evaluation/phase9/prompt-criteria.json"
      to: "PromptComparison.common_input"
      via: "one immutable SHA-256-bound common identity for all four rows"
      pattern: "input_sha256.*facts_sha256.*retrieval_trace_sha256"
---

<objective>
Define the strict Phase 09 evidence contracts and its machine-checkable four-case/V1–V4 source registries before any evidence collection begins.

Purpose: Make every later report prove currentness, same-case prompt identity, and honest terminal-state behavior instead of relying on narrative labels.
Output: TDD-backed frozen Pydantic models plus versioned case and prompt criteria files.
</objective>

<execution_context>
@C:/Users/20795/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20795/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-current-evaluation-evidence/09-RESEARCH.md
@.planning/phases/09-current-evaluation-evidence/09-VALIDATION.md
@.planning/phases/08-dify-unified-live-chain/08-CONTEXT.md
@.planning/phases/08-dify-unified-live-chain/08-07-PLAN.md
@src/debugmate/hashing.py
@src/debugmate/diagnosis/workflow.py
@src/debugmate/results/contracts.py

<interfaces>
From `src/debugmate/diagnosis/workflow.py`:
```python
class DiagnosisRunOutcome(StrictFrozenModel):
    status: WorkflowStatus
    execution_backend: ExecutionBackendValue
    case_id: str
    facts_sha256: str
    run_id: str
    facts: CaseFacts
    evidence: list[EvidenceAnchor]
    knowledge_build_id: str
    schema_version: str
    prompt_version: str
```

From `src/debugmate/results/contracts.py`:
```python
class ResultManifest(StrictFrozenModel):
    result_id: str
    identity: ArtifactIdentity
    mode: ResultMode
    execution_backend: ExecutionBackendValue
    status: ResultStatus
    availability: ArtifactAvailability
    artifacts: tuple[ArtifactRecord, ...]
```

Use `canonical_json_bytes`, `sha256_bytes`, and `sha256_file` from `src/debugmate/hashing.py`; do not add an evaluation-specific canonicalization or hash implementation.
</interfaces>
</context>

<decision_coverage>
| Decision / requirement | Task | Coverage | Enforcement |
|---|---|---|---|
| D-09-01: exactly 3–5 current representative cases | 1 | Full | Registry admits exactly four rows and tests reject any other count. |
| D-09-02: live success + privacy, insufficient, long, fallback/failure | 1 | Full | Exact five coverage tags are asserted across the four rows, with the degradation tag implemented by fixed P9-C04 local-fallback partial state. |
| D-09-03: exact same sanitized input/facts/retrieval for V1–V4 | 2 | Full | One immutable common identity is equality-gated in every row. |
| D-09-04: generated_live differs from verified_contract | 2 | Full | Strict provenance/evidence binding and claim-validation tests reject conflation or hash drift. |
| EVAL-01 | 1 | Full | Bounded case registry plus terminal-state and artifact-availability rules. |
| EVAL-03 | 2 | Full | Four hash-bound prompt rows with objective/adoption/limitation records. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the four-case current-evidence registry and strict case contracts</name>
  <files>evaluation/phase9/cases.json, src/debugmate/evaluation/__init__.py, src/debugmate/evaluation/contracts.py, tests/evaluation/test_contracts.py, tests/evaluation/test_case_matrix.py</files>
  <read_first>src/debugmate/hashing.py, src/debugmate/evidence.py, src/debugmate/diagnosis/workflow.py, src/debugmate/results/contracts.py, src/debugmate/results/verifier.py, src/debugmate/privacy/output_scan.py, fixtures/replay/index.json, tests/fixtures/diagnosis/workflow_cases.json, .planning/phases/09-current-evaluation-evidence/09-RESEARCH.md, .planning/phases/08-dify-unified-live-chain/08-07-PLAN.md</read_first>
  <behavior>
    - Test 1: Models reject extra fields, non-normalized/out-of-root relative paths, links, invalid SHA-256 values, duplicate case keys, and unbounded strings or lists.
    - Test 2: The source registry contains exactly P9-C01-live-private, P9-C02-insufficient, P9-C03-long-replay, and P9-C04-fallback-failure; its union of coverage tags equals live_success, insufficient_data, long_content, privacy, fallback_or_failure.
    - Test 3: P9-C01 is blocked until a verified Phase 08 Plan 07 summary and `evidence/dify-live/phase8/manifest.json` are both present and valid; historical C01–C07 or `evidence/course-v0.1` paths cannot satisfy it.
    - Test 4: the insufficient-information row rejects report, PNG, MP3, and ZIP claims; P9-C04 is exactly `execution_backend=local_fallback`, `status=partial`, with the established audio-unavailable availability and safe retry scope; all other availability states obey the existing result-manifest rules.
  </behavior>
  <action>Write failing tests first, then create `src/debugmate/evaluation/contracts.py` as the single strict, frozen Phase 09 contract module. Define bounded, `extra='forbid'` models for hash-bound repository references, `CaseEvaluation`, modality/citation/privacy summaries, source provenance, and terminal availability. All reference paths must be normalized repository-relative regular-file paths validated under allowlisted current roots; deny raw inputs, approval material, provider bodies/IDs, historical course evidence, and all frozen Phase 10 media paths. Add `evaluation/phase9/cases.json` with exactly four rows: P9-C01 has `live_success` and `privacy` tags but starts as blocked until the formal Phase 08 source exists; P9-C02 is the deterministic insufficient-information fixture with no invented diagnosis artifacts; P9-C03 is regenerated long-content replay; P9-C04 is the deterministic `local_fallback` partial terminal row with the existing audio-unavailable availability and retry semantics. Require `actual_status`, source hashes, safe limitation strings, and machine-checkable coverage rather than prose-only claims. Use existing result/evidence validators as later consumers; do not duplicate their artifact or media checks. Keep all raw Dify values and real personal data outside this registry.</action>
  <acceptance_criteria>
    - The registry has four and only four rows, each with a stable `P9-C0*` key, source-relative paths, hashes, expected/actual terminal status, coverage tags, limitations, and no free-form backend inference.
    - P9-C01 cannot be marked current/live/passed when `08-07-SUMMARY.md` or the exact Phase 08 formal manifest is missing, invalid, historical, or hash-drifted.
    - The exact tag set covers all locked scenarios, including privacy, without adding a fifth speculative row.
    - Strict tests prove expected artifact absence for P9-C02, exact local-fallback/audio-partial behavior for P9-C04, and rejection of every fabricated artifact claim.
  </acceptance_criteria>
  <atomic_commits>
    1. `test(09-01): add failing Phase 09 case-contract and matrix tests`
    2. `feat(09-01): add strict four-case evaluation registry contracts`
  </atomic_commits>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/evaluation/test_contracts.py tests/evaluation/test_case_matrix.py; .\.venv\Scripts\python.exe -m ruff check src/debugmate/evaluation tests/evaluation</automated>
  </verify>
  <done>The project has one strict, TDD-proven four-case registry whose live label is impossible to claim without Phase 08’s formal current evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify hash-bound V1–V4 same-case comparison and truthful provenance</name>
  <files>evaluation/phase9/prompt-criteria.json, src/debugmate/evaluation/contracts.py, tests/evaluation/test_prompt_comparison.py</files>
  <read_first>prompts/README.md, prompts/v1-baseline.md, prompts/v2-evidence-discipline.md, prompts/v3-decision-boundaries.md, prompts/v4-course-release.md, src/debugmate/cloud/workflow.py, src/debugmate/ui/serve.py, platform/dify/app.dsl.yml, src/debugmate/diagnosis/evidence_binding.py, evaluation/phase9/cases.json, .planning/phases/09-current-evaluation-evidence/09-RESEARCH.md</read_first>
  <behavior>
    - Test 1: A `PromptComparison` requires exactly V1–V4 rows, each with the current prompt-file SHA-256, safe fixed-case conclusion, accepted diagnosis/result SHA-256, candidate SHA-256, safe source-evidence reference, declared modification objective, adoption/rejection rationale, and limitation.
    - Test 2: every row compares equal to one immutable common identity containing P9-C01 case ID, sanitized input SHA-256, facts SHA-256, retrieval-trace SHA-256, knowledge-build ID, and schema SHA-256.
    - Test 3: any drift in case/input/facts/retrieval/build/schema/prompt hash, accepted diagnosis/result hash, candidate hash, safe conclusion binding, or source-evidence path/hash rejects the entire comparison with a stable safe rejection code.
    - Test 4: `verified_contract` requires candidate SHA-256 and safe source-evidence reference to bind exactly to the accepted V1 diagnosis/result output; it cannot serialize, render, or otherwise claim `generated_live`.
    - Test 5: `generated_live` requires the row's own provider-run source-evidence reference, exact provider-run prompt SHA-256, accepted diagnosis/result hashes, candidate hash, and safe fixed-case conclusion; missing/mismatched evidence is `rejected` or `blocked`, never rendered as a pass.
  </behavior>
  <action>Extend the strict contract module with `PromptProvenance` (`generated_live`, `verified_contract`, `rejected`, `blocked`), immutable `PromptComparisonInput`, a bounded safe fixed-case conclusion model, hash-bound source-evidence references, per-version comparison rows, and phase-level comparison validation. Require every row to carry its conclusion plus accepted diagnosis SHA-256, accepted result SHA-256, candidate SHA-256, and source-evidence reference; do not accept a display-only comparison row. Add `evaluation/phase9/prompt-criteria.json` that names and hashes the four existing prompt files, records each declared change objective, and provides bounded adoption/rejection/limitation fields. Bind all V1–V4 rows to the one sanitized P9-C01 identity, not four independently re-extracted or retrieved cases. Per the locked provenance boundary, the current product’s V1 output may be `generated_live` only when Phase 08 formal evidence proves the bound prompt hash and its source evidence; V2–V4 default to `verified_contract` only when candidate/source evidence bind exactly to that accepted V1 diagnosis/result output. A generated-live V2–V4 row must instead bind its own separately recorded evaluation provider-run evidence and exact prompt hash. Never republish or mutate `platform/dify/app.dsl.yml` to create comparisons, never invent scores for blocked variants, and never copy provider bodies into the comparison.</action>
  <acceptance_criteria>
    - Exactly four prompt rows map to `v1-baseline.md` through `v4-course-release.md`, with file SHA-256 checks, safe fixed-case conclusion, accepted diagnosis/result/candidate hashes, source-evidence reference, and declared objectives preserved.
    - All common-identity and evidence-binding fields are byte-for-byte valid across the required relationship; any mismatch rejects promotion rather than creating a mixed comparison table.
    - Renderable provenance labels remain explicit: `generated_live` means the row's own provider evidence used that prompt hash; `verified_contract` means the row binds exactly to the accepted V1 output for contract checking only.
    - The contract has no field that permits a blocked, rejected, or contract-verified row to masquerade as a live generated result or to omit its safe conclusion/evidence binding.
  </acceptance_criteria>
  <atomic_commits>
    1. `test(09-01): add failing same-case prompt provenance tests`
    2. `feat(09-01): add V1-V4 immutable comparison contracts`
  </atomic_commits>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/evaluation/test_prompt_comparison.py; .\.venv\Scripts\python.exe -m ruff check src/debugmate/evaluation tests/evaluation</automated>
  </verify>
  <done>V1–V4 comparison provenance and the exact shared sanitized input/facts/retrieval identity are mechanically enforced before any table can be generated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Registry JSON → evaluation contracts | Versioned fixture content is untrusted until strict bounds, roots, hashes, and status rules pass. |
| Prompt files/output identity → comparison | A filename or display claim must not be treated as evidence that a provider used that prompt. |
| Phase 08 formal evidence → P9-C01 | Historical capability artifacts must not become current live proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Spoofing | P9-C01 source reference | mitigate | high | Require `08-07-SUMMARY.md`, exact formal-manifest root, current contract/hash checks, and fail closed on absence or drift. |
| T-09-02 | Spoofing/Repudiation | V1–V4 provenance | mitigate | high | Strict provenance enum, prompt SHA-256, and rendering tests that reject `verified_contract` as `generated_live`. |
| T-09-03 | Tampering | common comparison identity | mitigate | high | One immutable identity with exact sanitized-input/facts/retrieval/build/schema equality gate. |
| T-09-05 | Tampering | case artifact availability | mitigate | high | State-aware contracts reject fabricated media for insufficient/failure rows. |
| T-09-04 | Information disclosure | registry and comparison values | mitigate | high | Allowlisted safe projections; deny raw input, approvals, keys, remote IDs/bodies, and historical media paths. |

All high-severity entries block collection and promotion; no warning-only downgrade is permitted.
</threat_model>

<verification>
Run the two focused TDD suites and scoped Ruff after each atomic commit. Before Wave 2, run `\.venv\Scripts\python.exe -m pytest -q tests/evaluation` and confirm no test accepts historical evidence, prompt identity drift, fabricated unavailable artifacts, or a false live claim.
</verification>

<success_criteria>
- The versioned registry contains the exact four locked case scenarios and validates the full tag set mechanically.
- Every V1–V4 row has the same immutable sanitized input/facts/retrieval identity.
- Prompt generation claims are separated from contract verification claims by a strict model and tests.
- No current live pass can exist without Phase 08 Plan 07’s completed summary and formal manifest.
</success_criteria>

<output>
After completion, create `.planning/phases/09-current-evaluation-evidence/09-01-SUMMARY.md`.
</output>
