---
phase: 09-current-evaluation-evidence
plan: 03
type: tdd
wave: 3
depends_on: [08-07, 09-02]
files_modified:
  - scripts/run-phase9-evaluation.ps1
  - tests/evaluation/test_runner.py
  - evidence/evaluation/phase9/manifest.json
  - evidence/evaluation/phase9/case-results.json
  - evidence/evaluation/phase9/prompt-comparison.json
  - evidence/evaluation/phase9/workflow-source.json
  - evidence/evaluation/phase9/phase10-inputs.json
  - evidence/evaluation/phase9/checksums.sha256
  - docs/course/current-evaluation.md
  - docs/course/current-prompt-comparison.md
autonomous: true
requirements: [EVAL-01, EVAL-03, EVAL-05, EVID-03]
must_haves:
  truths:
    - "The formal Phase 09 runner exits blocked and leaves the last formal ledger untouched unless Phase 08 Plan 07 has a completed summary and a valid zero-skip formal evidence manifest."
    - "After the prerequisite passes, one run validates the exact four current cases, enforces V1–V4 shared identity/provenance, and promotes only a complete privacy-safe ledger atomically."
    - "The promoted ledger identifies only eligible Phase 10 sources, preserves exclusion reasons, and is marked machine-generated/not hand-edited without producing or changing final media."
  artifacts:
    - path: "scripts/run-phase9-evaluation.ps1"
      provides: "Zero-skip formal Phase 09 orchestrator with a Phase 08 hard prerequisite and sibling staging promotion"
      contains: "08-07-SUMMARY.md"
    - path: "evidence/evaluation/phase9/manifest.json"
      provides: "Hash-bound whole-evaluation identity, terminal status, and machine-generated/no-hand-edit metadata"
      contains: "phase9-evaluation-1.0"
    - path: "evidence/evaluation/phase9/phase10-inputs.json"
      provides: "Eligible/ineligible course-source ledger without rendered final media"
      contains: "phase10_eligible"
  key_links:
    - from: "scripts/run-phase9-evaluation.ps1"
      to: ".planning/phases/08-dify-unified-live-chain/08-07-SUMMARY.md"
      via: "hard dependency gate before collection"
      pattern: "phase8_live_evidence_missing"
    - from: "evidence/dify-live/phase8/manifest.json"
      to: "evidence/evaluation/phase9/case-results.json"
      via: "hash-validated P9-C01 import with backend dify"
      pattern: "P9-C01-live-private"
    - from: "evidence/evaluation/phase9/phase10-inputs.json"
      to: "Phase 10 renderers"
      via: "versioned source references, hashes, provenance, eligibility, and exclusions only"
      pattern: "phase10_eligible"
---

<objective>
Execute the formal zero-skip Phase 09 evidence run only after Phase 08 Plan 07 has completed, then atomically publish the validated case/prompt/source ledger.

Purpose: Ensure current representative-case and prompt-comparison evidence is reproducible, fail-closed, and safe for Phase 10 to consume without allowing incomplete or stale data to replace prior evidence.
Output: A hard-gated PowerShell runner, runner tests, and one promoted evidence ledger with deterministic review-only Markdown projections.
</objective>

<execution_context>
@C:/Users/20795/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/20795/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-current-evaluation-evidence/09-RESEARCH.md
@.planning/phases/09-current-evaluation-evidence/09-VALIDATION.md
@.planning/phases/09-current-evaluation-evidence/09-01-PLAN.md
@.planning/phases/09-current-evaluation-evidence/09-02-PLAN.md
@.planning/phases/08-dify-unified-live-chain/08-CONTEXT.md
@.planning/phases/08-dify-unified-live-chain/08-07-PLAN.md
@.planning/phases/08-dify-unified-live-chain/08-07-SUMMARY.md
@evidence/dify-live/phase8/manifest.json
@src/debugmate/evaluation/contracts.py
@src/debugmate/evaluation/collector.py
@src/debugmate/evaluation/reports.py
@scripts/verify-phase9-scope.ps1

<interfaces>
Formal Phase 08 prerequisite artifacts:
```text
.planning/phases/08-dify-unified-live-chain/08-07-SUMMARY.md
evidence/dify-live/phase8/manifest.json
evidence/dify-live/phase8/knowledge-readback.json
evidence/dify-live/phase8/live-run.json
evidence/dify-live/phase8/checksums.sha256
```

Formal Phase 09 projections:
```text
evidence/evaluation/phase9/manifest.json
evidence/evaluation/phase9/case-results.json
evidence/evaluation/phase9/prompt-comparison.json
evidence/evaluation/phase9/workflow-source.json
evidence/evaluation/phase9/phase10-inputs.json
evidence/evaluation/phase9/checksums.sha256
```

The formal runner consumes the Phase 08 artifacts only through the Wave 2 collector and writes Phase 09 outputs only via its sibling staging directory. It must never accept a hand-created substitute summary, a C01–C07 capability bundle, or `evidence/course-v0.1` as the Phase 08 prerequisite.
</interfaces>
</context>

<decision_coverage>
| Decision / requirement | Task | Coverage | Enforcement |
|---|---|---|---|
| D-09-07: no current live pass without completed 08-07 formal evidence | 1 | Full | Hard summary/manifest/checksum/identity gate; absence yields stable blocked status and no promotion. |
| D-09-08: four cases and V1–V4 same identity become current evidence only as one verified run | 2 | Full | Formal runner executes collectors, identity checks, reports, privacy/scope checks, and exact checksums before atomic swap. |
| D-09-09: no PPTX/video/SRT/final screenshot/media-manifest changes | 1, 2 | Full | Mandatory Wave 2 scope gate before and after collection/promotion. |
| EVAL-01 | 2 | Full | Promoted case ledger contains the exact four validated current rows. |
| EVAL-03 | 2 | Full | Promoted V1–V4 table has common identity/hash/provenance checks. |
| EVAL-05 | 2 | Full | Phase 10 ledger has only validated eligible rows and explicit exclusions. |
| EVID-03 | 2 | Full | Current case/prompt/workflow/source projections are generated from real evidence without final-media rendering. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the hard-gated zero-skip Phase 09 formal runner</name>
  <files>scripts/run-phase9-evaluation.ps1, tests/evaluation/test_runner.py</files>
  <read_first>scripts/run-phase8-live-qa.ps1, scripts/verify-phase8-security-scope.ps1, scripts/verify-phase9-scope.ps1, src/debugmate/evaluation/contracts.py, src/debugmate/evaluation/collector.py, src/debugmate/evaluation/reports.py, .planning/phases/08-dify-unified-live-chain/08-07-PLAN.md, .planning/phases/08-dify-unified-live-chain/08-07-SUMMARY.md, evidence/dify-live/phase8/manifest.json, .planning/phases/09-current-evaluation-evidence/09-VALIDATION.md</read_first>
  <behavior>
    - Test 1: absent/malformed/untracked `08-07-SUMMARY.md`, formal Phase 08 manifest, inner artifact, or checksum returns `phase8_live_evidence_missing` or a safe validation code; it never returns `passed` or P9-C01 generated_live.
    - Test 2: a historical C01–C07 bundle, a course-v0.1 manifest, or a manually copied Phase 08 manifest cannot satisfy the prerequisite root/currentness/hash contract.
    - Test 3: formal mode runs the focused evaluation tests, existing privacy/result security checks, scope gate, and all collector/report checks with no skipped/error/failed outcome accepted.
    - Test 4: development-only `-AllowMissingLive` may write a non-promoted blocked diagnostic under a disposable output root, but the formal command has no override and never replaces `evidence/evaluation/phase9` while blocked.
  </behavior>
  <action>Write the runner tests first, then implement `scripts/run-phase9-evaluation.ps1` as the sole formal orchestration path. Use PowerShell `-LiteralPath`, resolved repository roots, strict `$ErrorActionPreference = 'Stop'`, and the project `.venv` executable. Before any collection, require the completed Plan 08-07 summary and the exact formal Phase 08 manifest/readback/live-run/checksum set; call the Wave 2 collector to validate their schema/currentness/hashes/backend `dify`/result identity and reject all historical roots. Run the explicit Phase 09 tests, privacy/evidence/result security suites, deterministic source renderers, checksums, and `verify-phase9-scope.ps1`; inspect their result status and fail on every skipped, error, failed, or unexecuted gate. Support `-AllowMissingLive` solely for local contract development into a disposable non-evidence directory with overall `blocked`; never expose it from the formal phase command and never present it as a pass. Do not call Dify or re-run the Phase 08 browser workflow: this plan consumes the completed 08-07 formal evidence as its live source.</action>
  <acceptance_criteria>
    - The runner has one unambiguous formal path with no user-provided bypass for missing 08-07 evidence.
    - Missing or invalid Phase 08 source returns a stable blocked/validation code before a live claim, report, source ledger, or evidence replacement can occur.
    - Formal execution has zero skip tolerance across the selected test and scope gates, and every command uses the project venv/explicit PowerShell policies.
    - A disposable development diagnosis cannot affect the canonical evidence directory or frozen Phase 10 media roots.
  </acceptance_criteria>
  <atomic_commits>
    1. `test(09-03): add failing Phase 08 prerequisite and zero-skip runner tests`
    2. `feat(09-03): add hard-gated Phase 09 formal evaluation runner`
  </atomic_commits>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/evaluation/test_runner.py tests/evaluation/test_contracts.py tests/evaluation/test_case_matrix.py tests/evaluation/test_prompt_comparison.py; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-phase9-evaluation.ps1 -AllowMissingLive</automated>
  </verify>
  <done>The formal runner cannot claim a current live pass, cannot skip a gate, and cannot alter formal evidence until Phase 08 Plan 07 is complete and valid.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run all gates and atomically promote the current evidence ledger</name>
  <files>tests/evaluation/test_runner.py, evidence/evaluation/phase9/manifest.json, evidence/evaluation/phase9/case-results.json, evidence/evaluation/phase9/prompt-comparison.json, evidence/evaluation/phase9/workflow-source.json, evidence/evaluation/phase9/phase10-inputs.json, evidence/evaluation/phase9/checksums.sha256, docs/course/current-evaluation.md, docs/course/current-prompt-comparison.md</files>
  <read_first>scripts/run-phase9-evaluation.ps1, tests/evaluation/test_runner.py, scripts/verify-phase9-scope.ps1, src/debugmate/evaluation/contracts.py, src/debugmate/evaluation/collector.py, src/debugmate/evaluation/reports.py, evaluation/phase9/cases.json, evaluation/phase9/prompt-criteria.json, .planning/phases/08-dify-unified-live-chain/08-07-SUMMARY.md, evidence/dify-live/phase8/manifest.json, evidence/dify-live/phase8/checksums.sha256</read_first>
  <behavior>
    - Test 1 (RED first): inject a failure after sibling staging writes but before promotion and prove the prior canonical ledger remains byte-identical, staging is removed or safely quarantined with a stable code, and no PPTX/screenshot/narration/MP3 copy/SRT/MP4/final-media manifest is written.
    - Test 2: a fully valid Phase 08 source plus four valid case rows and V1–V4 common identity creates a staged ledger whose manifest hashes each inner JSON/Markdown artifact and whose checksums verify byte-for-byte.
    - Test 3: privacy, source/currentness, identity, artifact, citation, result, checksum, or frozen-scope failure leaves the previously promoted Phase 09 evidence byte-identical and removes or quarantines the sibling staging directory.
    - Test 4: every promoted JSON carries machine-generated/no-hand-edit metadata, every Markdown starts with a machine-generated/no-hand-edit marker, and any hand edit creates checksum drift that blocks the next promotion.
    - Test 5: the promoted `phase10-inputs.json` contains source references/hashes/provenance/eligibility/exclusion only; the run neither writes PPTX, screenshot, narration, MP3 copy, SRT, MP4, nor a final-media manifest.
    - Test 6: V1 is generated_live only when the imported Phase 08 evidence proves the exact bound prompt; each V2–V4 row stays verified_contract, rejected, or blocked unless its own evaluation provider evidence validates generated_live.
  </behavior>
  <action>First extend `tests/evaluation/test_runner.py` and run its new focused failing RED test for an injected post-staging/pre-promotion failure: it must prove prior-ledger byte preservation, staging cleanup or safe quarantine, stable failure code, and zero media writes. Only after RED is recorded, implement the atomic-promotion behavior and rerun the focused test green before invoking the formal runner. After Task 1 is green and Phase 08 Plan 07 has actually completed, invoke the formal runner with no bypass. It must create a sibling staging directory, collect/import P9-C01 from the current Phase 08 formal evidence, regenerate P9-C02/P9-C03 and the fixed deterministic `P9-C04 local_fallback/partial/audio-unavailable` row under current contracts, calculate citations/modalities/privacy/limitations/eligibility, and validate the four prompt rows against their shared immutable identity. Require V1–V4 prompt hashes, source manifest hashes, canonical JSON bytes, `checksums.sha256`, recursive export safety, supported result/ZIP/MP3 verification, and a final frozen-media scope scan. Build `manifest.json` with source commit/worktree-safe identity, evaluation schema version, phase status, Phase 08 source manifest hash, case registry hash, prompt hashes, DSL/schema/knowledge identities, every inner artifact hash, `generated_by=scripts/run-phase9-evaluation.ps1`, and `manual_edits_permitted=false`. Every generated JSON must carry equivalent machine-generated/no-hand-edit metadata; each Markdown starts with a machine-generated/no-hand-edit marker; `checksums.sha256` has a matching machine-generated header while preserving supported checksum verification. Write only the review-source Markdown files named above; they must identify their evidence/provenance and are not slide/video text. Atomically replace `evidence/evaluation/phase9` only after every gate is green; otherwise preserve the last good formal ledger and report the stable blocker/failure code. Do not invoke any PPT/video/media builder and do not alter a PPTX, video, SRT, final screenshot, or media manifest.</action>
  <acceptance_criteria>
    - The focused runner test executes and passes before the formal runner; it proves staged failure preserves the prior ledger, cleans/quarantines staging, and creates no media files.
    - The promoted ledger is derived from one current accepted Phase 08 evidence source plus three deterministic current rows, with P9-C04 fixed as `local_fallback`/`partial`/audio-unavailable, all with exact hashes and explicit terminal/provenance truth.
    - Every V1–V4 row shares one sanitized input/facts/retrieval/build/schema identity; unsupported generated-live claims are absent.
    - Every generated ledger JSON/Markdown/checksum file is explicitly marked machine-generated/not hand-edited; a manual edit is detected by checksum drift and blocks promotion.
    - `phase10-inputs.json` contains only validated source metadata and does not contain or generate final media files.
    - All inner artifacts and checksums verify after the atomic replace; any gate failure leaves the former canonical ledger and all frozen Phase 10 paths intact.
  </acceptance_criteria>
  <atomic_commits>
    1. `test(09-03): add failing staged-promotion preservation cleanup and no-media-write tests`
    2. `feat(09-03): promote verified current Phase 09 evidence ledger`
  </atomic_commits>
  <verify>
    <automated>.\.venv\Scripts\python.exe -m pytest -q tests/evaluation/test_runner.py tests/evaluation/test_reports.py tests/evaluation/test_course_source_manifest.py; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-phase9-evaluation.ps1; .\.venv\Scripts\python.exe -m pytest -q tests/evaluation tests/results/test_security_abuse.py; powershell -NoProfile -ExecutionPolicy Bypass -File scripts/verify-phase9-scope.ps1</automated>
  </verify>
  <done>One complete, privacy-safe, checksum-marked machine-generated Phase 09 ledger is atomically promoted only from completed Phase 08 formal evidence, and it gives Phase 10 a truthful source manifest without modifying media.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Completed Phase 08 acceptance → Phase 09 runner | A live claim is allowed only when Plan 08-07’s final formal evidence is present, current, hash-valid, and validator-reopened. |
| Phase 09 staging → canonical ledger | Partial, private, stale, or invalid generated outputs must never replace the last good evidence directory. |
| Phase 09 projections → Phase 10 | Only explicit eligible sources may be consumed; renderable final media remains outside this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Severity | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Spoofing | Phase 08 prerequisite | mitigate | high | Require completed 08-07 summary, exact formal evidence root, strict hashes/checksums/current identities, and no historical substitutes. |
| T-09-08 | Tampering | atomic promotion | mitigate | high | Sibling staging, complete gate sequence, fsync/atomic replace pattern, and tests preserving prior ledger on every failure. |
| T-09-02 | Repudiation | prompt table provenance | mitigate | high | Enforce provenance enum and exact prompt/common identity during formal collection. |
| T-09-04 | Information disclosure | staged/promoted evidence | mitigate | high | Recursive existing export/result security checks before promotion; safe error codes only. |
| T-09-07 | Tampering | frozen Phase 10 media | mitigate | high | Mandatory before/after scope gate and explicit ban on course-media builders. |
| T-09-SKIP | Denial of service / integrity | runner gate results | mitigate | high | JUnit/command result inspection rejects skipped, unexecuted, error, and failed gates. |

Every high-severity issue produces blocked or failed status and prevents promotion; no partial ledger receives a formal success label.
</threat_model>

<verification>
The definitive command is `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-phase9-evaluation.ps1`. It must be run only after `08-07-SUMMARY.md` and its formal evidence manifest exist. Follow it with all `tests/evaluation`, `tests/results/test_security_abuse.py`, exact checksum validation, and the Phase 09 scope gate. Missing Phase 08 evidence is an external dependency blocker, not permission to create a current-live substitute.
</verification>

<success_criteria>
- Phase 09 is formally blocked until the completed Phase 08 Plan 07 evidence source exists and validates with zero skipped gates.
- Once unblocked, four current cases and a truthful V1–V4 same-case comparison are atomically promoted as one hash-bound ledger.
- Phase 10 receives only an eligibility/source manifest, not early PPTX/video/subtitle/screenshot/media output.
- Failed or partial attempts cannot replace a prior formal ledger or change frozen media assets.
</success_criteria>

<output>
After completion, create `.planning/phases/09-current-evaluation-evidence/09-03-SUMMARY.md`.
</output>
